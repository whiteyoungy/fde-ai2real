# FDE 教程项目 · 运行指南

## 一、切到 bypass permissions（不再弹窗）

**最快：当前会话内切，不丢上下文、不杀正在跑的 agent**

按 `Shift+Tab` 循环权限模式，切到显示 `bypass permissions`。
首次会弹一次确认框。或者输入 `/config` 从面板里选。

> 现在会话卡在 `dontAsk` 模式（不问，直接拒），只能从 UI 切才能解开。
> 改配置文件没用——已经试过，热加载不可靠。

**重开会话时用参数（更可靠）**

```bash
cd /root/fdeengineer
claude -c --permission-mode bypassPermissions
```

- `-c` = 继续当前目录最近一次对话，带上全部上下文
- `--dangerously-skip-permissions` 是等价写法

模式可选值：`bypassPermissions` / `acceptEdits` / `auto` / `manual` / `dontAsk` / `plan`

---

## 二、放到后台（终端断了也不停）

### 推荐：tmux

```bash
tmux new -s fde                                      # 建会话
cd /root/fdeengineer
claude -c --permission-mode bypassPermissions        # 在里面启动
```

**detach（离开不停止）**：按 `Ctrl+B` → **松开** → 再按 `D`

**回来**：
```bash
tmux attach -t fde
```

**其他**：
```bash
tmux ls                    # 列出会话
tmux kill-session -t fde   # 真要停
```

### 回来时用哪个命令？分三种情况

**情况 1 · tmux 里的 claude 还活着（正常情况）**
```bash
tmux attach -t fde
```
就这一条。**不用 `/resume` 也不用 `-c`** —— attach 直接接回正在运行的会话，
上下文和正在跑的 agent 全都在。进程从没停过。

**情况 2 · claude 退了，但 tmux 会话还在**（attach 进去看到空 shell）
```bash
cd /root/fdeengineer
claude -c
```

**情况 3 · tmux 也没了（机器重启）**
```bash
tmux new -s fde
cd /root/fdeengineer
claude -c --permission-mode bypassPermissions
```

### `-c` / `-r` / `/resume` 的区别

| 用法 | 位置 | 作用 |
|---|---|---|
| `claude -c` | 命令行 | 继续**当前目录最近一次**对话，不问 |
| `claude -r` | 命令行 | 打开选择器挑历史会话；也可 `claude -r <session-id>` |
| `/resume` | 会话内 | 已在某会话中，想切到另一个历史会话 |

本项目用 `-c` 就够（这个目录只有一个在推进的对话）。

> **坑**：`-c` 只认当前目录。忘了 `cd /root/fdeengineer` 就跑 `claude -c`，
> 它会去继续别的目录的对话。`cd` 那步别省。

> 坑：`Ctrl+B` 是前缀键。别按住 Ctrl 去按 D，要先松开再按。

### 备选：Claude Code 原生后台

```bash
claude --bg -c --permission-mode bypassPermissions
claude agents --help       # 管理命令
```

**为什么建议 tmux**：这项目有 47 个任务，中间需要你介入决策（阶段二 Task 11
是硬性确认门禁）。tmux 能随时 attach 进去插话、纠偏、回答问题，`--bg` 不方便。

---

## 三、切换前注意

搬 tmux = 重开会话 = **当前正在后台跑的 subagent 会死掉**，未提交的工作白跑。

建议顺序：
1. 先 `Shift+Tab` 切 bypass（当前会话和正在跑的 agent 都保住）
2. 等手头的 task 跑完提交
3. 再决定搬不搬

---

## 四、恢复提示词（新会话接续用）

```
继续执行 /root/fdeengineer 的 FDE 教程项目。

先读 .superpowers/sdd/progress.md 台账确认已完成到哪个 task，
再读 docs/superpowers/plans/2026-08-07-fde-handbook.md 计划文件
（注意 Global Constraint #11 是源 PDF 已证伪内容的禁用清单，必须遵守）。

用 superpowers:subagent-driven-development skill，从第一个未完成的 task
继续往下推，每个 task 走「派实现 agent → 生成 review-package → 派审查
agent → 有 Critical/Important 就派修复 → 记台账」的循环。

阶段二的 Task 11 是用户确认门禁，跑到那里要停下来等我确认。
```

三样东西都在盘上，任何新会话凭这段话都能精确接上：
- 计划：`docs/superpowers/plans/2026-08-07-fde-handbook.md`
- 台账：`.superpowers/sdd/progress.md`
- git 历史

---

## 五、项目文件地图

```
docs/superpowers/specs/2026-08-07-fde-handbook-design.md   设计文档（29章大纲、页数预算）
docs/superpowers/plans/2026-08-07-fde-handbook.md          实施计划（51个task）
docs/research/                                             调研笔记（全书事实底座）
book/                                                      章节 Markdown
labs/                                                      12个动手实验
build/build.sh                                             Markdown → PDF
.superpowers/sdd/progress.md                               执行台账（git-ignored）
```

**查进度**：
```bash
cat .superpowers/sdd/progress.md
git log --oneline
```
