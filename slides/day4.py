#!/usr/bin/env python3
"""第 4 天 · 让它可信

听众：FDE
覆盖：下册第 7–10、12–13 章、四个案例章（14–17）、别册三
上机：Lab-05 企业 Evals 框架 ＋ CI 门禁

第 3 天攒的是证据，今天攒的是控制。顺序被六道关卡锁死——
没有证据就上线，控制保护的是一个没人验证过的东西。
"""
from deck_kit import *   # noqa: F403

# 这一天用到书里的插图（build/figures.tex）。列在这里一次渲染完，
# 也是一份索引：这一天在哪几处直接用了书里的图。
figures.render(['fdtopologies', 'fddrift'])

D = Deck()
DAY = "第 4 天 · FDE 专场"


def sl(num=None, label=None, right=DAY, bg=GROUND):
    s = D.slide(bg)
    if label:
        eyebrow(s, num, label, right)
    return s


def sec(num, title, sub, kicker):
    s = D.slide(PANEL)
    section(s, num, title, sub, kicker)
    return s


def objectives(mod, title, items, foot=None):
    s = sl(mod, "本节目标")
    heading(s, title, size=30)
    body(s, "这一节结束时，你应该能做到下面这几件事。", 1.95, size=15)
    tf = tb(s, ML, 2.5, CW, 3.4)
    for i, it in enumerate(items):
        rich(tf, [("%d　" % (i + 1), {'color': VERIFY, 'bold': True,
                                      'mono': True, 'size': 14}),
                  (it, {})], size=15, color=INK2, first=(i == 0),
             spacing=1.45, space_before=0 if i == 0 else 13)
    if foot:
        footer(s, foot)
    return s


# ══════════════════════════ 开场 ══════════════════════════

s = D.slide(PANEL)
eyebrow(s, None, "FORWARD DEPLOYED ENGINEER · 五天实战课程")
tf = tb(s, ML, 1.75, 11.4, 2.4)
line(tf, "让它可信：", 50, INK, bold=True, first=True, spacing=1.12)
line(tf, "护栏、评估与国产化", 50, INK, bold=True, spacing=1.12)
body(s, "第 4 天　昨天攒的是证据，今天攒的是控制。智能体编排、部署护栏与可观测性、"
        "评估的工程实现、国产模型与信创，以及四个案例串讲。", 4.4, size=17, w=10.6)
rule(s, 5.5)
for i, (v, k) in enumerate([("5", "个模块"), ("4", "个案例"),
                            ("1.5", "小时上机"), ("1", "次红绿时刻")]):
    tf = tb(s, ML + i * 2.55, 5.75, 2.4, 0.9)
    line(tf, v, 30, INK, bold=True, mono=True, first=True)
    line(tf, k, 11, MUTED, space_before=2)
footer(s, "教材：下册第 7–10、12–17 章 ＋ 别册三")

# 今日安排
s = sl("00", "今日安排")
heading(s, "今天六小时怎么走")
timetable(s, 2.05, [
    ["时段", "模块", "内容"],
    ["09:00–10:15", "M20", "生成式系统与智能体编排：Prompt 分层、RAG 进阶、MCP、可靠性"],
    ["10:30–11:30", "M21", "部署、护栏与可观测性：镜像、离线包、缓存、埋点、成本"],
    ["11:30–12:30", "M22", "评估的工程实现：三层评估集、指标、裁判五个坑、CI 门禁"],
    ["12:30–13:10", "M23", "技能：四选一、三要素、萃取三问、命中集、下架判据"],
    ["13:30–14:15", "M24", "国产模型与信创：五步判据、性能损耗的真实数字"],
    ["14:15–14:45", "M25", "四个案例串讲：只讲被否决的方案和可迁移经验"],
    ["15:00–16:30", "上机 ③", "Lab-05 企业 Evals 框架 ＋ CI 门禁"],
])
example(s, 5.85, "上机是这五天里唯一一次有明确红绿时刻的：故意引入一个劣化改动"
                 "（护栏被关闭 ＋ 检索结果被注入噪声 ＋ 一个历史故障被重新引入），"
                 "CI 门禁必须阻断；恢复后必须放行。",
        "下册附录 A；labs/lab-05-evals/，verify.sh 5/5", h=1.3)

# 骨架定位
s = sl("00", "骨架定位")
heading(s, "今天攒的是控制，明天之前必须攒完")
table(s, ML, 1.95, [
    ["模块", "骨架层", "在攒哪一类结果"],
    ["M20 智能体编排", "过程 · 第 3 关到第 5 关", "证据的最后一段 ＋ 控制的第一段"],
    ["M21 护栏与可观测性", "结果 · 控制", "客户能限制它读什么、改什么、连到哪里"],
    ["M22 评估的工程实现", "结果 · 证据 ＋ 资产", "把证据收敛成验收集——那个模型换了也不用重建的东西"],
    ["M24 国产模型与信创", "过程 · 第 2 关的硬约束", "选型判据，第一步就是许可证一票否决"],
    ["M25 四案例串讲", "全部四层", "看别人怎么把这四层走完，以及哪些经验不能直接搬"],
], [2.6, 3.2, 5.6], hl=2, rowh=0.56)
quote(s, 4.85, "没有证据，业务不该信；没有控制，安全部门不该放；客户不能接管，FDE 不该走。"
               "今天做的是中间那一句。", size=15)
example(s, 5.95, "M22 特殊：它同时属于第三层和第四层。评估集既是证据的收敛形式，"
                 "又是唯一那件模型换了也不用重建的资产。",
        "上册 §9.0", h=1.1)

# 承接
s = sl("00", "承接")
heading(s, "昨天到今天，三分法往下走了一层")
table(s, ML, 1.95, [
    ["昨天讲的", "今天讲的是它的下一层"],
    ["三分法：检索失败 / 数据污染 / 模型幻觉", "M22 把第三类做成可计算的忠实度指标"],
    ["数据契约、漂移、脏数据四条路径", "M22 把“分数掉了”也分成三类漂移，各自处置不同"],
    ["七要素落层：动作走执行层，权限走授权层", "M20 讲 Agent 怎么调动作，M21 讲怎么把权限做成护栏"],
    ["可替换优先", "M24 讲国产化替换时，这条判据要付的账"],
], [4.6, 6.8], rowh=0.62)
body(s, "昨天做的是“让它跑对”，今天做的是“让它出错的时候你知道，"
        "并且客户敢让它继续跑”。", 4.75, size=15, w=10.9, color=INK)
example(s, 5.4, "两件事的差别在验收会上很明显：前者能演示，后者能被安全部门放行。"
                "而项目卡在哪一边，取决于今天这四个模块做没做。",
        "上册 §9.0", h=1.3)

# ══════════════════════════ M20 Agent 编排 ══════════════════════════

sec("M20", "生成式系统与智能体编排",
    "它自己发明了一个不存在的工具。这一章讲的全部内容，都是为了让这句话"
    "变成一条能被检出、能被拦下、能被复现的故障，而不是一次玄学。",
    "09:00–10:15 · 下册第 9 章")

objectives("M20", "读完这一节你能做什么", [
    "把一段几百行的系统提示词拆成四层，并说清哪一层改动要走更高的评审门槛。",
    "给上下文窗口做预算分配，并让每类消费者都有上限、不能相互抢占。",
    "在 RAG 进阶四件套里按判据选，并说出每一件“什么情况下不值得上”。",
    "写一个工具描述，让模型知道什么时候该用、什么时候绝对不能用。",
    "在四种编排拓扑里选一种，并区分分支和主管的关键差别。",
    "识别 Agent 的四类故障，并对每一类给出防御手段。",
], "下册第 9 章")

# Prompt 四层
s = sl("M20", "7.1 Prompt 分层")
heading(s, "一整块几百行的提示词，改起来会出三种事")
bullets(s, 1.95, [
    "改一次业务规则就要通读全篇，找哪句话要改。",
    "出了问题说不清是哪一层在起作用。",
    "团队协作时容易出现互相覆盖对方改动的情况。",
], size=15, gap=10)
body(s, "四层结构，从稳定到易变排列。层号越小，改动应该越谨慎、评审门槛应该越高：",
     3.5, size=15, color=INK, w=10.9)
steps(s, 4.0, [
    ("第 1 层", "身份与边界", "系统是谁、绝对不做什么。改动等同于改产品定义。"),
    ("第 2 层", "工具与能力", "有哪些工具、各自什么时候用。改动要同步改工具描述。"),
    ("第 3 层", "业务规则", "这个客户特有的判据与口径。最常改的一层。"),
    ("第 4 层", "当次上下文", "本轮检索结果、对话历史。每次调用都不同。"),
], h=1.75)
example(s, 6.0, "分层之后，“改一条业务规则”变成只动第 3 层的一次改动，"
                "评审范围也随之收窄——这和规约驱动里“重的写一次，每个功能薄薄一层”"
                "是同一种结构。",
        "下册 §9.1；下册 §2.3", h=1.15)

# 结构化输出
s = sl("M20", "7.1 结构化输出")
heading(s, "两条路线，代价不同")
contrast(s, 2.0,
         ["在提示词里给出目标 Schema 和示例，要求模型按格式输出。",
          "实现成本最低，但没有硬保证。",
          "模型仍可能输出格式错误——多一个逗号、漏一个引号。",
          "需要额外的解析容错。"],
         ["在推理层面强制模型只能生成符合给定语法的 token 序列。",
          "从根上排除格式错误的可能性。",
          "多数商用 API 与本地推理框架都提供某种形式的接口。",
          "参数名和支持范围随厂商版本变化快，实现前查当前文档。"],
         h=2.4, wtitle="提示词约束", rtitle="约束解码")
example(s, 4.75, "这条选择和第 2 天讲的“硬约束走确定性代码”是同一种判断："
                 "格式错了只是“答得不好”还是“会赔钱”？"
                 "如果下游是一个直接执行的动作，格式保证就不该建立在“模型这次表现好”上。",
        "下册 §9.1；上册 §14.2", h=1.45)

# 提示链
s = sl("M20", "7.1 提示链")
heading(s, "别指望一次调用把四件事全做完")
body(s, "“理解意图、检索、生成、校验”挤在一次调用里，出问题时你分不清是哪一步坏了。",
     2.0, size=15.5, color=INK, w=10.9)
steps(s, 2.6, [
    ("①", "理解意图", "输出一个结构化的意图对象，而不是一段自然语言。"),
    ("②", "检索", "拿上一步的结构化输出当检索输入。"),
    ("③", "生成", "只做生成这一件事。"),
    ("④", "校验", "对生成结果做独立检查，不复用生成时的上下文。"),
], h=1.7)
body(s, "拆成多个更小的调用，前一步的结构化输出是后一步的输入，"
        "每一步职责单一、容易单独测试和替换。", 4.6, size=15, w=10.9)
example(s, 5.3, "第 ④ 步“不复用生成时的上下文”是关键：用同一份上下文去校验自己刚生成的东西，"
                "校验会倾向于通过。这和裁判模型的自我偏好是同一个问题。",
        "下册 §9.1、§8.3", h=1.4)

# 三层退避
s = sl("M20", "7.1 失败重试")
heading(s, "三层退避，每一层都有明确的退出条件")
steps(s, 2.1, [
    ("第一层", "原地重试", "同样的提示词再试一次。次数用完就必须进入下一层，"
                            "不能无限重试。"),
    ("第二层", "降级", "缩小 Schema、简化任务、换更小的输出结构。"
                       "降级也用完就必须进入下一层。"),
    ("第三层", "转人工", "任何一层都不能无限重试下去——最后必须有一个人接手。"),
], h=2.0)
body(s, "重点不是具体的函数实现，是三层退避的调用顺序，以及“每一层都有明确的退出条件”"
        "这个结构本身。", 4.5, size=15, w=10.9, color=INK)
example(s, 5.15, "这和 M17 那套企业集成的韧性四件套是同构的：幂等决定重试是否安全，"
                 "退避决定节奏，超时预算决定什么时候放弃，熔断决定放弃之后怎么活。"
                 "换到模型调用这一层，判据没变——只是“失败”的形状从超时变成了格式错误。",
        "下册 §9.1、§6.1", h=1.5)

# 上下文预算
s = sl("M20", "7.2 上下文管理")
heading(s, "每类消费者都要有上限，不能相互抢占")
table(s, ML, 1.95, [
    ["消费者", "典型占比", "说明"],
    ["系统指令（四层结构）", "5%–10%", "相对固定，身份与工具层通常不随对话变化"],
    ["检索结果", "40%–60%", "单条查询能拿到的检索预算，直接决定“回答有没有依据”"],
    ["对话历史", "20%–35%", "需要设置滚动窗口或摘要机制，不能无限累积"],
    ["输出预留", "10%–15%", "必须预留。输出预算不足会导致回答被截断"],
], [3.4, 2.2, 5.8], hl=4, rowh=0.62)
body(s, "这张表是设计起点，不是精确配方。不同任务的最优比例没有可直接照抄的实证研究，"
        "需要用自己的评估集实测调整。", 4.6, size=14.5, w=10.9)
example(s, 5.35, "设计的关键不是记住某个百分比，是“每类消费者都要有上限、"
                 "不能相互抢占”这个原则本身。第四行最容易被漏——"
                 "输出被截断的表现不是报错，是回答戛然而止，看起来像模型没话说了。",
        "下册 §9.2", h=1.45)

# 压缩策略
s = sl("M20", "7.2 压缩")
heading(s, "三种压缩策略，按压缩时机分")
bullets(s, 1.95, [
    ("滚动摘要", "对话轮次超过阈值后，把最早的若干轮压缩成一段摘要。"
                  "实现简单，代价是摘要本身可能丢失细节，且生成摘要要占用一次额外调用。"),
    ("检索结果二次浓缩", "混合检索和重排之后仍超预算时，用轻量模型对每篇文档做要点抽取，"
                          "而不是整篇塞入。"),
    ("结构化记忆代替流水账", "把关键事实（用户偏好、已确认的参数）抽取成结构化字段单独维护，"
                              "不依赖模型每次从完整对话记录里“回忆”。"),
], size=13.5, gap=12)
body(s, "第三条的好处是：对话历史本身可以更激进地截断，因为关键信息已经落到了"
        "结构化字段里，不依赖历史文本本身。", 4.85, size=14.5, w=10.9, color=INK)
example(s, 5.6, "第三条其实就是七要素里的“状态”那一栏在会话层的形态——"
                "把状态显式建模，而不是散在一堆自然语言里让模型每次重读。"
                "同一条判据在第 2 天讲本体时出现过一次。",
        "下册 §9.2；上册 §5.3", h=1.35)

# 上下文污染
s = sl("M20", "7.2 上下文污染")
heading(s, "比“上下文不够长”更容易被忽视的问题")
bullets(s, 2.0, [
    ("检索噪声堆积", "候选文档里混着不相关内容，模型分不清哪些该用。"
                      "上下文越长，噪声的绝对量越大。"),
    ("历史错误自我强化", "前一轮的错误结论留在对话历史里，后面每一轮都在它的基础上推。"),
    ("指令被淹没", "系统指令写在最前面，检索结果和历史堆在后面，"
                    "关键约束在长上下文里的权重被稀释。"),
], size=13.5, gap=12)
body(s, "三种成因对应三条不同的处置：改检索、改记忆结构、改分层。"
        "把它们统统归因成“上下文窗口不够”，然后去换一个更长上下文的模型，是最常见的误判。",
     4.85, size=14.5, w=10.9, color=INK)
example(s, 5.65, "第二条最隐蔽：它在单轮测试里完全测不出来。"
                 "评估集如果全是单轮问答，这一类故障永远不会被发现——"
                 "直到生产环境里出现一次长对话。",
        "下册 §9.2", h=1.35)

# RAG 四件套
s = sl("M20", "7.3 RAG 进阶")
heading(s, "四件套，每一件都要问“什么情况下不值得上”")
table(s, ML, 1.95, [
    ["工具", "解决什么", "什么情况下不值得上"],
    ["查询重写", "用户提问简短、口语化、指代不清，直接检索命中率低",
     "查询本身已是结构化精确检索（订单号、型号）；对延迟极敏感"],
    ["混合检索", "向量擅长语义，BM26 擅长精确关键词，两者互补",
     "知识库是短小的结构化记录；查询几乎不含专有名词或编号"],
    ["RRF 融合", "多路检索器的分数量纲不一致，RRF 只用排名不用绝对分数",
     "只有一路检索器；候选池已经很小，直接精排即可"],
    ["Rerank 精排", "第一阶段为了速度牺牲了排序精度，用交叉编码器二次精排",
     "延迟预算吃紧；候选池小到精排收益有限"],
], [1.7, 4.4, 5.3], hl=2, rowh=0.72)
example(s, 5.3, "查询重写的代价要摆到台面上：至少多一次模型调用。"
                "一篇论文的单一企业案例自测显示，端到端延迟从 19.52 秒增至 34.62 秒，"
                "约慢 1.77 倍。这类量级的延迟代价必须和收益一起比较。",
        "下册 §9.3", h=1.4)

# RRF 的 k 值
s = sl("M20", "7.3 一个跨厂商的坑")
heading(s, "RRF 的 k 值不是普适常数，两家默认值差 30 倍")
bignum(s, ML, 2.2, "60", "Elasticsearch 的默认平滑常数", MUTED, 50, w=5.3)
bignum(s, ML + 5.7, 2.2, "2", "Qdrant 的 RRF 默认 k", MUTED, 50, w=5.3)
body(s, "k 越小，头部排名结果的权重占比越高。跨厂商照搬默认参数是常见错误——"
        "接入新的检索后端时要先确认它自己的 k 默认值。", 3.85, size=15, w=10.9, color=INK)
example(s, 4.65, "融合后效果不如某一路单独检索时，先查三件事：k 值是不是照搬了别家的默认；"
                 "两路检索器的候选深度是不是差太多；有没有一路本身质量就很差，"
                 "把它融进来只是在稀释另一路。",
        "下册 §9.3", h=1.4)
quote(s, 6.25, "这类“默认值不一样”的坑没法靠记忆规避，只能靠一条纪律："
               "换后端就重跑一次评测。", size=14)

# RAG 之外
s = sl("M20", "7.4 RAG 之外")
heading(s, "什么时候一次检索不够，要让它自己判断再来一轮")
body(s, "四件套解决的是“一次检索怎么做得更准”。有一类问题它们解决不了："
        "一次检索本身就拿不到足够的信息。", 1.95, size=15, w=10.9)
steps(s, 2.75, [
    ("判断", "检索回来的内容够不够回答这个问题？"),
    ("补检索", "不够就换个角度再检一轮，或者拆成子问题分别检。"),
    ("收敛", "够了就生成；轮次上限到了还不够，就明确说“依据不足”而不是硬答。"),
], h=1.55)
body(s, "这就是循环拓扑的典型用途，也是它必须配迭代上限的原因——"
        "没有上限，“判断—补检索”会变成死循环。", 4.6, size=15, w=10.9, color=INK)
example(s, 5.3, "第三步的“明确说依据不足”比看起来重要：一个会拒答的系统，"
                "才有资格谈忠实度。上册第 4 关那句“已知失效边界要写进验收单”，"
                "在这里就是这一行代码。",
        "下册 §9.4；上册 §13.3", h=1.4)

# MCP 版本现状
s = sl("M20", "7.5 MCP")
heading(s, "版本现状必须先说清楚，否则代码会直接跑不起来", size=30)
table(s, ML, 2.15, [
    ["项", "现状", "对你意味着什么"],
    ["Python SDK", "包名 mcp，当前 2.0.0", "官方 README 警告：保留 <2 的上界，直到你完成迁移"],
    ["stdio 传输", "核心标准传输，未受影响", "本地进程间集成继续用它"],
    ["HTTP+SSE", "自 2025-03-26 版协议起已弃用", "新实现不应再采用"],
    ["Streamable HTTP", "在最新版里又发生破坏性调整", "移除了 GET stream endpoint 与协议级 session，不再支持断线续传"],
], [2.2, 3.6, 5.6], hl={1, 4}, rowh=0.66)
example(s, 5.15, "这一页的意义不在于记住这几个版本号——它们很快会过期。"
                 "在于它示范了一件事：一个还在快速演进的协议，"
                 "把它写进交付物之前要先回答“客户那边升级的时候谁来改”。"
                 "第 5 天那张开源组件进场评估表，第一行问的就是这个。",
        "下册 §9.5", h=1.5)

# MCP Server 三类能力
s = sl("M20", "7.5 Server 设计")
heading(s, "一个 Server 暴露三类能力，鉴权要处理两层")
for i, (k, t2) in enumerate([
        ("工具", "模型可调用的函数。绝大多数集成工作落在这一类上。"),
        ("资源", "模型可读取的数据，通过 URI 模板定位。"),
        ("提示词模板", "预置的对话模板，供宿主端调用。")]):
    card(s, ML + i * 3.85, 2.0, 3.55, 1.5, None, k, t2, bsize=12)
body(s, "协议本身不规定具体鉴权方案。Server 桥接遗留系统时通常要处理两层认证：",
     3.85, size=15, color=INK, w=10.9)
bullets(s, 4.35, [
    "客户端到 Server 这一层，可以用标准的 API Key 或 OAuth2。",
    "Server 到背后遗留系统这一层，可能是遗留系统专有的 mTLS 证书或内部 token 机制。",
], size=14, gap=10)
example(s, 5.65, "第二层正是第 3 天 M17 讲的认证桥接。两层拼起来，"
                 "MCP Server 的本质就是一个带工具语义的协议桥——"
                 "而桥的两头各有各的凭据轮换问题。",
        "下册 §9.5、§6.3", h=1.3)

# MCP 命名陷阱
s = sl("M20", "7.5 三个实测的坑")
heading(s, "报错够响亮的那个反而不危险")
bullets(s, 1.95, [
    ("规范文档是驼峰，SDK 是蛇形", "照规范文档写驼峰属性名会直接抛属性不存在。"
                                    "这个还算好——报错够响亮，跑一次就发现了。"),
    ("包装工具函数必须保留原函数签名", "少了这一步，工具名会变成内层函数名，"
                                        "入参 schema 退化成通用的位置参数和关键字参数。"
                                        "列工具时照常返回，看起来一切正常，只是模型再也传不对参数了。"),
    ("docstring 要清理缩进", "SDK 不会替你去掉缩进，函数体里那几个空格会原样进入模型上下文。"),
], size=13, gap=12)
quote(s, 5.0, "第二条才是危险的那一类：它不报错，只是让模型开始传错参数。", size=16)
example(s, 5.85, "这也是为什么“列工具能列出来”不能当作验收标准。"
                 "Lab-03 的验收判据是“客户端能发现并正确调用全部工具”——"
                 "“正确调用”这三个字挡的就是这一类静默退化。",
        "下册 §9.5；labs/lab-03-mcp-bridge/，verify.sh 8/8", h=1.35)

# structured_content
s = sl("M20", "7.5 一个判据陷阱")
heading(s, "不要拿结构化返回是不是空来判断成败")
table(s, ML, 2.1, [
    ["工具的返回标注", "是否生成输出 schema", "调用成功时的结构化返回"],
    ["-> dict", "否", "空"],
    ["-> dict[str, Any]", "是", "有内容"],
], [3.8, 3.4, 4.2], hl=1, rowh=0.6)
body(s, "它是不是空，取决于你的返回类型标注写没写全——和这次调用成功还是失败无关。",
     4.2, size=15.5, w=10.9, color=INK)
example(s, 4.85, "用一个和成败无关的字段去判断成败，是最难排查的一类 bug："
                 "它在开发环境（标注写全了）一切正常，在某个同事补的新工具上"
                 "（标注只写了 dict）永远返回“失败”，而工具本身跑得好好的。",
        "下册 §9.5，2026-08-08 实测 mcp 2.0.0", h=1.45)
quote(s, 6.45, "判据要选和被判断的事情真正相关的那个信号。", size=14)

# 工具描述
s = sl("M20", "7.5 工具描述")
heading(s, "工具描述的写法直接决定调用准确率")
body(s, "模型没有读过你的代码，它只能根据工具名、docstring、参数说明来判断"
        "什么时候调用、怎么传参。一个反面示例的三处硬伤：", 1.95, size=15, w=10.9)
bullets(s, 2.75, [
    "工具名没有说明查询的是什么对象。",
    "docstring 只有四个字，没说明参数是订单号、用户 ID 还是别的什么，也没说明返回结构。",
    "没有给出典型调用场景或边界条件，比如参数不存在时会发生什么。",
], size=14, gap=10)
quote(s, 4.5, "工具不可逆时，“不适用”那一段要比“适用”那一段写得更长。", size=17)
example(s, 5.4, "查询工具选错了，代价是多一次无谓调用；审批工具选错了，"
                "代价是一笔钱批出去、且没有撤销手段。"
                "这就是第 2 天讲的“不可逆操作强制人工”那条判据在工具层的形态。",
        "下册 §9.5；上册 §13.4", h=1.4)

# 危险工具的验收
s = sl("M20", "7.5 危险工具")
heading(s, "不可逆操作没有“通过率 90%”这种说法")
body(s, "Lab-03 里那个审批工具的验收方式值得单独说：把危险工具的误触发做成一条"
        "可重复运行的检查，而不是靠人读一遍描述觉得“应该没问题”。", 1.95, size=15, w=10.9)
card(s, ML, 2.85, 5.55, 1.75, "普通工具", "允许 8/10",
     "工具选择准确率有通过率门槛。选错一次的代价是多一次无谓调用，可以事后纠正。")
card(s, ML + 5.85, 2.85, 5.55, 1.75, "不可逆工具", "一次误触发就判失败",
     "验收脚本对这一项不设通过率门槛。在诱导性表述下零误触发，"
     "少一次都不算通过。", accent=VERIFY)
body(s, "这个数字要谨慎解读：它说明在这套描述下、对这个模型、这几条用例成立，"
        "不是“这样写就绝对安全”的保证。", 4.9, size=14.5, w=10.9)
example(s, 5.55, "真正该记住的是方法，不是那个数字：把“会不会误触发”从一个主观判断，"
                 "变成一条能重复跑、会给出退出码的检查。这正是下午上机要做的事。",
        "下册 §9.5；labs/lab-03-mcp-bridge/", h=1.35)

# 状态与检查点
s = sl("M20", "7.6 状态设计")
heading(s, "多个节点写同一个字段时，怎么合并要显式声明")
body(s, "状态定义里要给每个字段配一个合并函数，决定多个节点写同一个字段时如何合并——"
        "比如消息列表用“追加”而不是“覆盖”。", 1.95, size=15, w=10.9)
card(s, ML, 2.85, 11.4, 1.35, "检查点：图的状态默认只活到这一次调用结束", None,
     "要支持“暂停后恢复”“跨会话保留状态”，必须在编译时传入一个检查点实现。"
     "调试用内存版，生产用数据库版——后者是独立的包，不随主包安装，需要显式声明依赖。",
     accent=CAVEAT, bsize=14.5)
example(s, 4.45, "“不随主包安装”这一条会在交付现场以一种很典型的方式暴露："
                 "开发机上调试跑得好好的（用的内存版），到客户环境切成生产配置，"
                 "报模块不存在。这正是 M15 讲的“依赖锁到整棵树”要防的事。",
        "下册 §9.6；下册 §1.4", h=1.5)
quote(s, 6.2, "凡是“调试用 A、生产用 B”的东西，都要在交付清单里单独列一行。", size=14)

# 图：四种编排拓扑的形状（书 · 图 8）
s = sl("M20", "7.6 编排拓扑")
figpage(s, "fdtopologies", "四种拓扑的差别是形状上的差别",
        "下册 §9.6",
        note="分支和主管在表里都是“一个节点决定往哪走”，画出来才看得出差在哪："
             "一个判断完就交出控制权，一个每一步之后都回到它手上。")

# 四种拓扑
s = sl("M20", "7.6 编排拓扑")
heading(s, "四种拓扑，以及分支和主管的关键差别")
table(s, ML, 1.95, [
    ["拓扑", "什么时候用"],
    ["顺序", "固定流水线，每一步的输出是下一步的输入。任务边界清晰、步骤间无需动态决策"],
    ["分支", "路由节点根据状态做条件判断。先分类任务类型，再按类型走不同处理路径"],
    ["循环", "节点反复执行直到满足终止条件。必须配迭代上限，否则它就是死循环故障的直接来源"],
    ["主管", "一个协调节点调度多个专家子智能体，决定下一步交给谁、什么时候收尾"],
], [1.4, 10.0], hl=3, rowh=0.56)
body(s, "分支和主管容易混用，区分的关键是决策是一次性的还是持续的：", 4.4, size=15,
     color=INK, w=10.9)
bullets(s, 4.9, [
    "分支的路由节点只判断一次，判断完就把控制权交出去，不再介入后续执行。",
    "主管的协调节点在每个专家完成一步之后都会被重新调用，持续判断接下来该轮到谁。",
], size=14, gap=10)
example(s, 6.15, "选错的代价：把需要持续协调的任务做成分支，表现是"
                 "“第一步走对了，后面全跑偏”，而且没有任何一处会报错。",
        "下册 §9.6", h=1.05)

# 人在回路
s = sl("M20", "7.6 人在回路")
heading(s, "官方明确写了：静态中断不推荐用于人在回路")
body(s, "原文是“静态中断不推荐用于人在回路的工作流，请改用动态中断函数”。"
        "静态中断仍然是合法的编译参数，但定位是调试／测试用的断点，不是动态人机交互机制。",
     1.95, size=14.5, w=10.9)
card(s, ML, 2.95, 11.4, 1.2, "这个机制依赖检查点才能工作", None,
     "源码文档原文写明：要使用中断，必须启用检查点，因为这个特性依赖于把图状态持久化下来。"
     "没有检查点直接调用会报错。", accent=CAVEAT, bsize=14.5)
example(s, 4.35, "一个容易踩的坑：中断恢复是按节点内调用顺序匹配的，"
                 "同一节点内多次调用要保证顺序稳定。单次恢复后节点会从头重新执行——"
                 "节点如果有副作用（写库、调外部 API），需要做幂等设计，"
                 "否则恢复执行会把副作用重复执行一遍。",
        "下册 §9.6", h=1.55)
quote(s, 6.1, "又是幂等。这是它今天第三次出现，也是第 2 天“撤不回来的后果”那张表"
              "在编排层的形态。", size=14)

# Agent 四类故障
s = sl("M20", "7.7 可靠性")
heading(s, "四类故障，各自的防御手段不同")
table(s, ML, 1.95, [
    ["故障", "怎么防"],
    ["工具调用失败", "区分三种失败：可重试的瞬时失败按退避重试；参数错误重新生成或转人工；"
                     "工具本身不可用则降级到备选路径或明确告知能力受限"],
    ["幻觉工具调用", "它自己发明了一个不存在的工具。防御是在调用前校验工具名在注册表内，"
                     "不在就直接拒绝，而不是让它试着调一次"],
    ["死循环", "循环拓扑必须配迭代上限。上限触发时的动作要明确定义，"
               "不能只是停下来不说话"],
    ["状态污染", "多轮之间的状态被前一轮的错误结论污染。用结构化记忆而不是流水账，"
                 "关键字段可被覆盖、可被校验"],
], [1.9, 9.5], hl=1, rowh=0.72)
example(s, 5.2, "第一类是四类里最“正常”的一类。重点不是消除它，"
                "是让 Agent 能够区分失败类型并做出不同反应——"
                "参数错误重试一百次也不会变对。",
        "下册 §9.7；防御手段以工程归纳为主，无学术实证支撑具体阈值", h=1.5)

# 微调 vs RAG vs Prompt
s = sl("M20", "7.8 决策")
heading(s, "微调、RAG、提示词工程：先问缺的是什么")
table(s, ML, 2.0, [
    ["缺什么", "上什么", "为什么"],
    ["缺知识——模型不知道你们公司的事", "RAG", "知识会更新，微调进权重里每次更新都要重训"],
    ["缺格式或风格——输出结构总是对不上", "提示词 ＋ 约束解码", "这是最便宜的一层，先穷尽它"],
    ["缺能力——某类推理它就是做不好", "微调", "这是三者里唯一能改变模型行为本身的"],
], [3.8, 3.0, 4.6], hl=1, rowh=0.62)
body(s, "顺序不能倒：先穷尽提示词，再上 RAG，最后才考虑微调。"
        "微调是三者里成本最高、最难回滚、最容易掩盖真实问题的一条路。",
     4.35, size=15, w=10.9, color=INK)
example(s, 5.15, "“最容易掩盖真实问题”指的是：如果根因其实是检索没找到对的文档，"
                 "微调能让模型在评估集上分数变好看，但生产环境里它依然拿不到正确的依据——"
                 "只是学会了把没有依据的话说得更像那么回事。",
        "下册 §9.8；上册 §11.6 三分法", h=1.5)

# M20 清单
s = sl("M20", "本节清单")
heading(s, "M20 清单：交付一个 Agent 之前")
checklist(s, 1.95, [
    "系统提示词分层了吗？改一条业务规则要动几层？",
    "上下文预算分配表画了吗？输出预留了吗？",
    "RAG 四件套里每上一件，都回答过“什么情况下不值得上”吗？",
    "RRF 的 k 值是这个后端自己的默认值，还是从别家抄来的？",
    "工具描述里，不可逆工具的“不适用”那一段比“适用”那一段长吗？",
    "危险工具的误触发做成可重复跑的检查了吗？还是靠人读一遍觉得没问题？",
    "循环拓扑配迭代上限了吗？上限触发时的动作定义了吗？",
    "人在回路用的是动态中断吗？检查点配了吗？节点副作用幂等吗？",
], size=13, gap=9)
footer(s, "下册第 9 章本章清单")

# ══════════════════════════ M21 护栏与可观测性 ══════════════════════════

sec("M21", "部署、护栏与可观测性",
    "“生产镜像里为什么有 gcc？”——现场安全审计常见的第一个问题。"
    "这一章讲的是控制层：客户能限制它读什么、改什么、连到哪里。",
    "10:30–11:30 · 下册第 10 章")

s = sl("＊", "本节金句")
maxim(s, "控制面比模型聪明更重要。",
      "上生产要满足三条：可观测、可控制、可撤销。**K8s 只是其中一条的实现。**一个能力一般但边界清楚、出事能停能退的系统，比反过来那个可交付。",
      "逐字", "下册 §10.0", size=38)

objectives("M21", "读完这一节你能做什么", [
    "写一份能过安全审计的 Dockerfile，而不是事后用卸载命令补救。",
    "给推理服务定资源配额，并说清 GPU 上“留余量”为什么不成立。",
    "在命令不由你敲的现场，把交付物做成编号脚本 ＋ 预检 ＋ 回滚。",
    "把“支持离线”拆成签和验两问分别验证。",
    "在自己的数据上标定语义缓存阈值，并先确认两组分布分不分得开。",
    "让脱敏在写入之前完成，包括异常信息里的原文。",
], "下册第 10 章")

# 多阶段构建
s = sl("M21", "8.1 容器化")
heading(s, "客户安全团队真的会打开 Dockerfile 逐行看")
bullets(s, 1.95, [
    ("多阶段构建", "编译工具链不出现在运行时镜像里。事后用包管理器卸载补救没用——"
                    "它不会清理已经写入镜像层的文件，体积也不会变小。"),
    ("非 root 运行", "很多客户的安全基线强制要求容器不能以 root 身份跑。"
                      "写 Dockerfile 时就满足，而不是等扫描工具报错才回头改。"),
    ("依赖装到指定目录", "而不是系统目录，方便精确控制拷贝范围，"
                          "避免把缓存和构建残留一起带进最终镜像。"),
    ("基础镜像锁到具体 tag", "不要用 latest。两次分别拉取的 latest 可能指向不同的底层摘要，"
                              "离线环境里出了问题很难排查。"),
], size=13, gap=11)
example(s, 5.5, "第四条在离线打包场景里尤其关键。前三条是“做不做”的问题，"
                "第四条是“出了问题查不查得清”的问题——"
                "而离线环境恰恰是最难查的环境。",
        "下册 §10.1", h=1.35)

# Chart 七个文件
s = sl("M21", "8.1 交付形态")
heading(s, "Helm Chart 七个文件，各自对应一次评审提问")
table(s, ML, 1.95, [
    ["文件", "作用", "评审会问什么"],
    ["Chart 元数据", "名称、版本号、依赖声明", "这个版本对应哪次提交？"],
    ["默认参数", "镜像 tag、副本数、资源配额、探针配置", "资源配额是压测出来的还是拍的？"],
    ["Pod 模板", "容器定义、探针、环境变量注入", "以什么用户运行？"],
    ["服务暴露", "集群内还是需要外部访问", "为什么需要外部访问？"],
    ["非敏感配置注入", "避免把配置硬编码进镜像", "改配置要不要重新构建镜像？"],
    ["敏感配置", "密钥、数据库密码", "交付前要确认客户的密钥管理规范"],
    ["自动扩缩容规则", "水平扩缩容", "扩到上限还不够时会发生什么？"],
], [2.4, 4.6, 4.4], hl=6, rowh=0.44)
example(s, 5.5, "第六行那句“交付前要确认客户的密钥管理规范”不是客套："
                "客户用自建密钥服务还是原生密钥对象，决定了这个文件长什么样。"
                "等到评审当天才问，就是再等一轮。",
        "下册 §10.1", h=1.35)

# K8s 资源
s = sl("M21", "8.1 资源配额")
heading(s, "GPU 上“留余量”这个思路不成立")
table(s, ML, 1.95, [
    ["资源", "怎么定", "注意"],
    ["CPU", "request 基于压测下的 p50 实际占用，limit 通常设为 request 的 1.5–2 倍",
     "这个倍数没有统一标准，要结合自己的压测数据和成本预算标定"],
    ["内存", "压测要看峰值而不是均值", "有 GC 的运行时内存曲线是锯齿状的，"
                                       "线上偶发的 GC 前峰值会把容器直接打挂"],
    ["GPU", "request 与 limit 通常设成相等",
     "K8s 只支持整卡级别分配，不支持像 CPU/内存那样细粒度切分与超卖"],
], [1.2, 5.0, 5.2], hl=3, rowh=0.72)
body(s, "真正的显存精细化管理，是在容器内部由推理引擎自己的参数负责。"
        "K8s 只管“分几张卡”，不管卡内显存怎么切——这两层不要混为一谈。",
     4.5, size=14.5, w=10.9, color=INK)
example(s, 5.3, "探针配置对推理服务也要特殊处理：模型加载可能耗时数十秒到数分钟。"
                "存活／就绪探针如果沿用 Web 服务惯用的短超时，"
                "会在模型还没加载完时就被判定为不健康并反复重启。"
                "生产实践通常额外加一个宽松超时的启动探针。",
        "下册 §10.1", h=1.5)

# 命令不由你敲
s = sl("M21", "8.2 交付物形态")
heading(s, "执行的人不是你的时候，交付物的形态要变")
bullets(s, 1.95, [
    "你的操作要经过堡垒机，会话有审计和录屏。",
    "你没有集群凭据，命令由客户运维代你执行，你只能在旁边看着念。",
    "生产变更只在固定窗口内做，错过等下一轮。",
], size=15, gap=10)
quote(s, 3.5, "这三条改变的不是部署方式，是交付物的形态。", size=17)
body(s, "交付物要变成：编号的执行脚本、每一步的验证命令、一份预检脚本、一份回滚脚本。",
     4.4, size=15, w=10.9, color=INK)
example(s, 5.05, "窗口的价值密度很高，别拿它当发现问题的时间。"
                 "预检和回滚这两件东西存在的意义，就是把“发现问题”和“解决问题”"
                 "挪到窗口之外——窗口里你只有回滚的时间，没有排查的时间。",
        "下册 §10.2；上册 §14.4", h=1.45)

# 预检与回滚
s = sl("M21", "8.2 窗口作业")
heading(s, "窗口里你只有回滚的时间，没有排查的时间")
card(s, ML, 2.0, 5.55, 2.2, "预检脚本", "在窗口之前跑，不占窗口",
     "确认目标环境的前置条件全部满足：磁盘空间、端口占用、依赖版本、"
     "证书有效期、目标服务当前状态。任何一条不满足就不进窗口。", accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.2, "回滚脚本", "地位比安装脚本更高",
     "上线前在预生产环境完整执行一次，记录耗时。"
     "这个耗时是你在事故中真正拥有的预算——回滚要 40 分钟，"
     "“发现问题后立刻回滚”在业务方听来就是“再坏 40 分钟”。", accent=VERIFY)
body(s, "交付物还要包括：编号的执行脚本，以及每一步之后的验证命令。"
        "客户运维照着念，你在旁边看着。", 4.6, size=14.5, w=10.9)
example(s, 5.35, "“每一步之后的验证命令”是最容易被省掉的一件："
                 "没有它，执行到第七步出错时，没人知道前六步到底成功了几步。",
        "下册 §10.2；上册 §14.4", h=1.35)

# 离线包五步
s = sl("M21", "8.2 离线部署包")
heading(s, "五步，任何一步遗漏这条验收标准都达不成")
steps(s, 2.1, [
    ("①", "镜像导出与多架构构建", "客户现场的 CPU 架构不一定和开发机一致，"
                                   "尤其国产化环境常见 ARM64。"),
    ("②", "签名", "离线场景不能用依赖在线服务的无密钥签名，必须改用密钥对签名。"),
    ("③", "生成软件物料清单", "物理隔离场景客户通常要求同时留存两种主流格式。"),
], h=1.8)
steps(s, 4.05, [
    ("④", "离线依赖打包", "安装脚本、Chart、以及任何安装时才会去外网拉取的依赖，"
                           "都要提前下载好一起打进介质。"),
    ("⑤", "离线安装脚本", "导入镜像 → 验证签名 → 校验物料清单 → 安装，串成一条不依赖外网的流程。"),
], h=1.5)
example(s, 5.85, "顺序不能改：先验签，再加载。反过来等于把一个还没验证的镜像"
                 "先塞进守护进程，验签失败时它已经躺在本地了。",
        "下册 §10.2；labs/lab-07-offline-bundle/，verify.sh 7/7", h=1.2)

# 签能离线，验不能
s = sl("M21", "8.2 一个反直觉的发现")
heading(s, "“支持离线”要拆成两问：签能不能离线，验能不能离线", size=29)
contrast(s, 2.2,
         ["用自己的密钥对签名，断网下可以签出来。",
          "初稿以为到这里就完了。"],
         ["用同一份公钥去验签，工具会先去联网拉信任根，断网直接失败。",
          "失败发生在验签之前——连“签名对不对”都还没开始算。"],
         h=1.55, wtitle="签：能离线", rtitle="验：默认不能离线")
body(s, "所以完整的离线链路需要一份额外产物：一个本地信任根。实测只有 73 字节，"
        "作用是让工具不去联网拉信任根。", 4.05, size=15, w=10.9, color=INK)
example(s, 4.75, "这条链路在无网络命名空间里实测通过，且两个反例都会失败："
                 "篡改文件报签名无效，换一把错的公钥同样报签名无效。验签确实在验，不是走过场。",
        "下册 §10.2 实测记录", h=1.2)
quote(s, 6.1, "交付现场断网的是客户那一侧，也就是验签那一侧——恰恰是更容易出问题的那一半。"
              "真到现场才发现验不了签，那时候补救的余地非常小。", size=13.5)

# 推理三档
s = sl("M21", "8.3 推理优化")
heading(s, "无 GPU 环境的三档降级，门槛比想象的高")
steps(s, 2.1, [
    ("档位 ①", "有 GPU，标准路径", "显存预留比例、上下文上限、量化方式三个参数是主要旋钮。"),
    ("档位 ②", "无 GPU 但 Linux ＋ 容器可用", "用官方 CPU 镜像，免去手动处理动态库预加载和编译器版本。"),
    ("档位 ③", "无 GPU 且 Windows 或资源受限", "换用轻量推理工具，均提供兼容的接口形态。"),
], h=2.0)
body(s, "档位 ② 的门槛比“装上然后启动”高得多：只支持 Linux 原生；"
        "预编译包装完之后，必须手动把两个库加入动态库预加载才能正常跑。"
        "这一步很容易被漏掉，表现为“装完了但跑不起来”。", 4.45, size=14, w=10.9)
example(s, 5.4, "还有一条容易被忽略：CPU 路径下 KV cache 空间默认是 0，必须显式设置，"
                "否则服务无法正常提供缓存空间。GPU 路径下它由显存预留比例间接控制，"
                "不需要单独设——两条路径的操作方式不一样，照抄会翻车。",
        "下册 §10.3；labs/lab-06-inference-cache/", h=1.45)

# 查参数
s = sl("M21", "8.3 一个小坑")
heading(s, "照着老教程查参数默认值，会一无所获")
body(s, "某推理框架的启动命令帮助信息只列出配置分组（十几个），不列具体参数。"
        "想看到带默认值的完整列表要用另一个参数——实测输出 1831 行。", 1.95, size=15, w=10.9)
quote(s, 2.95, "照着老教程直接管道过滤会一无所获，误以为参数不存在。", size=16)
body(s, "这类坑的共同形状：工具的行为变了，而网上的教程停在了旧版本。"
        "它们不报错，只是让你得出一个错误的结论。", 3.85, size=15, w=10.9, color=INK)
example(s, 4.65, "本书自己在这里纠正过一次：初稿把某个默认值标为存疑（不确定是否改过），"
                 "后来在具体硬件和具体版本上实测确认了当前值——"
                 "但仍然写明“具体哪个版本改的未逐版本核实”。"
                 "确认了一半就只说一半，是这本书处理版本类事实的一贯做法。",
        "下册 §10.3", h=1.55)

# 语义缓存阈值
s = sl("M21", "8.4 语义缓存")
heading(s, "阈值不能拍脑袋定，而且要先问分不分得开")
table(s, ML, 2.0, [
    ["阈值", "命中率", "误命中"],
    ["0.85", "7/10", "4/12"],
    ["0.90", "5/10", "2/12"],
    ["0.94", "1/10", "1/12"],
], [2.6, 4.4, 4.4], hl=3, rowh=0.56)
body(s, "初稿给的建议是“找到错误复用比例明显上升的拐点，把阈值定在拐点以下留安全边际”。"
        "实测下来，这个建议预设了一个不一定成立的前提——两组分布分不分得开。",
     4.05, size=14.5, w=10.9)
example(s, 4.85, "看最后一行：把阈值提到 0.94，误命中只从 2 降到 1，"
                 "但命中率从 5/10 掉到 1/10。这时候缓存基本没在工作，"
                 "却还留着一个误命中。画完分布图先问“这两组分得开吗”——"
                 "分不开的时候，调阈值只是在两种坏结果之间挪。",
        "下册 §10.4；Lab-06 实测", h=1.6)

# 语义缓存三条防错
s = sl("M21", "8.4 语义缓存")
heading(s, "三条防错命中的机制")
bullets(s, 1.95, [
    ("业务上下文必须成为缓存键的一部分", "缓存键里要带上租户 ID、模型版本、提示词模板版本。"
                                          "缺了这层隔离，语义缓存会变成跨租户串答案的安全事故。"),
    ("高风险操作直接跳过语义缓存", "“退款政策是否适用于第 8 天”和“第 15 天”在向量空间里"
                                    "可能非常接近，但答案可能完全相反。"),
    ("命中之后再做一次轻量一致性检查", "对命中结果做关键字段抽取（金额、日期、条款编号），"
                                        "和当前请求比对，不一致就放弃缓存转真实推理。"),
], size=13, gap=12)
body(s, "第三条的额外开销远小于一次完整推理，但能拦住相似度分数打分虚高导致的错误复用。",
     4.9, size=14.5, w=10.9, color=INK)
example(s, 5.6, "什么情况下不该上：请求高度个性化、几乎不存在真正的语义重复；"
                "或者团队没有能力维护“阈值标定—回放错误案例—调整阈值”这套持续运营流程。"
                "投入不了持续运营精力的团队，宁可只做精确匹配缓存。",
        "下册 §10.4", h=1.4)

# 多级缓存
s = sl("M21", "8.4 多级缓存")
heading(s, "三层各自解决不同粒度的重复计算")
table(s, ML, 1.95, [
    ["层", "处理什么", "特点"],
    ["L1 精确匹配", "完全相同的重复请求，比如前端重试导致的重复提交", "命中成本最低，风险最小"],
    ["L2 语义缓存", "语义相近但字面不同的请求", "命中收益最大，风险也最高，需要防错命中机制兜底"],
    ["L3 前缀缓存", "同一次会话内、共享前缀的多轮对话", "由推理引擎自己管理，不需要业务层介入"],
], [2.0, 4.6, 4.8], hl=2, rowh=0.66)
body(s, "投入不了持续运营精力的团队，宁可只做 L1，也不要上一套自己看不住的 L2。",
     4.5, size=15, w=10.9, color=INK)
example(s, 5.15, "权重缓存是另一件事，要解决“重复下载”和“首次加载竞态”两个问题。"
                 "权重文件通常几十 GB，每个副本各自维护本地缓存的话，每次扩容都要重下一遍。"
                 "做法是挂共享持久卷；多副本同时冷启动会有竞态，"
                 "通常用初始化容器预热或提前打进只读挂载卷来规避。",
        "下册 §10.4", h=1.6)

# 埋点脱敏
s = sl("M21", "8.5 可观测性")
heading(s, "装饰器在你背后又抄了一份原文送出去")
body(s, "埋点 SDK 的装饰器默认会自动把函数的入参和返回值写进 span。这意味着——"
        "只要原文作为参数传进被装饰的函数，它就绕过了你所有的脱敏逻辑。",
     1.95, size=15, w=10.9)
quote(s, 2.85, "脱敏中间件写得再仔细，装饰器在背后又抄了一份原文送出去。", size=16.5)
body(s, "解法是把闸门收敛到少数几个函数：让写 span 属性这件事只发生在一两个地方，"
        "脱敏就挂在那里。原文只存在于最外层的局部变量里。", 3.85, size=14.5, w=10.9)
example(s, 4.75, "任何跨出进程的路径都得先过脱敏：进 span 属性、进检索索引、"
                 "发给模型厂商 API、以及异常信息。最后这条最容易漏——"
                 "异常消息里带的原文照样会被 SDK 上报上去。",
        "下册 §10.5；labs/lab-08-observability/，verify.sh 7/7", h=1.45)

# 脱敏两类手段
s = sl("M21", "8.5 脱敏")
heading(s, "脱敏必须在写入之前完成，不是在展示层")
body(s, "如果原始个人信息已经落进日志文件或 trace 存储，事后再“脱敏展示”解决不了"
        "合规问题——底层数据已经带着敏感信息落盘了。", 1.95, size=15, w=10.9)
table(s, ML, 2.75, [
    ["脱敏方式", "覆盖范围", "局限"],
    ["正则规则", "结构化、格式固定的：身份证号、手机号、邮箱、银行卡号",
     "对格式不固定的信息（人名、地址、公司名）基本无效"],
    ["命名实体识别模型", "非结构化的：人名、地址、机构名",
     "需要额外的模型推理开销，存在误判（漏掉变体写法或过度脱敏正常文本）"],
], [2.4, 4.6, 4.4], rowh=0.72)
body(s, "两类各有覆盖范围，通常需要组合使用。", 4.5, size=15, color=INK)
example(s, 5.05, "Lab-08 的验收标准里有一句值得注意：“PII 零泄漏且业务字段零误伤”。"
                 "两个条件缺一不可——过度脱敏把订单号也打上码，"
                 "在合规上没问题，在业务上等于把日志毁了。",
        "下册 §10.5；labs/lab-08-observability/", h=1.4)

# 审计留痕
s = sl("M21", "8.5 审计留痕")
heading(s, "三条通用设计原则，具体法条以客户所在行业为准")
for i, (k, t) in enumerate([
        ("留存期限要有明确策略", "不同行业的最低留存年限差异很大，需要按客户所在行业的"
                                  "官方法规确认，不能凭经验估一个数字。"),
        ("审计记录本身要防篡改", "常见做法是只追加写入，或者用哈希链固化历史记录的完整性，"
                                  "避免“审计日志被事后修改”这个更严重的问题。"),
        ("访问控制要有独立的授权层级", "谁能查看审计记录本身也是一项需要被审计的操作，"
                                        "不能所有工程师默认都有权限。")]):
    card(s, ML + i * 3.85, 2.0, 3.55, 2.5, "原则 %d" % (i + 1), k, t, bsize=12)
body(s, "本节不逐条列具体法条——没有针对特定行业法规原文做过核查，"
        "不在这里编造具体条款。", 4.85, size=14.5, w=10.9)
example(s, 5.45, "第二条的必要性来自一个反直觉的场景：出事之后，"
                 "最有动机修改审计日志的，恰恰是有权限修改它的人。"
                 "所以“审计记录能不能被改”这个问题，比“审计记录全不全”更靠前。",
        "下册 §10.5；上册 §5.3 第七要素", h=1.4)

# 成本
s = sl("M21", "8.6 成本")
heading(s, "单位经济模型要算两层，只算第一层会低估")
card(s, ML, 2.0, 5.55, 1.85, "第一层", "每次调用成本",
     "按 token 计价，输入和输出通常单价不同，按当次用量加权求和。")
card(s, ML + 5.85, 2.0, 5.55, 1.85, "第二层", "每个业务动作成本",
     "一个业务动作往往不是一次调用就能完成：检索、多轮推理、失败重试、"
     "甚至触发人工复核前的多次候选生成。汇总起来才是真实成本。", accent=VERIFY)
body(s, "看板至少要有四条线：按模型／租户／业务功能拆分的成本、缓存节省的成本、"
        "延迟与 token 用量分布、以及业务动作粒度的单位成本趋势。", 4.25, size=14.5, w=10.9)
example(s, 5.1, "第二条容易被忽略：缓存带来的成本节省要单独算一条线。"
                "被总成本曲线的自然下降悄悄吸收掉，就看不出缓存策略本身的贡献——"
                "然后下一次成本压力来的时候，没人知道这块还有多少空间。",
        "下册 §10.6", h=1.45)

# 内容标识
s = sl("M21", "8.7 内容标识")
heading(s, "生成内容要带两种标识，缺一种都不算合规")
card(s, ML, 2.0, 5.55, 2.0, "显式标识", "用户能直接看到或听到的提示",
     "要同时带上“人工智能”或“AI”，以及“生成”或“合成”。"
     "两个要素都要有——只写“AI 辅助”不够。", accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.0, "隐式标识", "写进文件元数据",
     "包含生成合成标签、服务提供者、内容制作编号这几类要素。", accent=VERIFY)
body(s, "这一条经常在项目后期才被提出来，而它影响的是输出层的每一个出口："
        "界面、导出的文件、发出去的消息、生成的报告。", 4.35, size=14.5, w=10.9)
example(s, 5.15, "所以它属于第 2 关定界阶段就要确认的事项之一——"
                 "和备案、数据出境、关基认定同一批。"
                 "留到上线前才发现，要改的是所有输出通道。",
        "下册 §10.7；上册 §11.1、第 13 章", h=1.4)

# M21 清单
s = sl("M21", "本节清单")
heading(s, "M21 清单：交付一套部署包之前")
checklist(s, 1.95, [
    "多阶段构建了吗？运行时镜像里还有编译工具链吗？",
    "容器以非 root 运行吗？基础镜像锁到具体 tag 了吗？",
    "推理服务配了宽松超时的启动探针吗？还是沿用 Web 服务的短超时？",
    "离线包五步齐了吗？验签能不能在断网环境完成——不只是签能不能？",
    "语义缓存的阈值是在自己的数据上标定的吗？先确认过两组分布分得开吗？",
    "埋点装饰器的自动捕获关掉了吗？异常信息里的原文过脱敏了吗？",
    "成本看板有没有把缓存节省单独算一条线？",
    "生成内容的显式和隐式标识都做了吗？",
], size=13, gap=9)
footer(s, "下册第 10 章本章清单")

# ══════════════════════════ M22 评估 ══════════════════════════

sec("M22", "评估的工程实现",
    "那份没人跑的评估集。上册第 4 关给了判据和责任划分，这一章给实现——"
    "评估集怎么造、指标怎么算、裁判模型有哪五个坑、怎么挂进持续集成。",
    "11:30–12:30 · 下册第 8 章")

s = sl("11", "本节金句")
maxim(s, "模型可以换，“什么算对”不能跟着换。",
      "芯片、操作系统、数据库、模型全换一遍之后——信创改造经常就是这么换的——**你手上还剩什么不用重建。**",
      "提炼", "提炼自 下册 §8.0；最狠的检验在 §11", size=34)

objectives("M22", "读完这一节你能做什么", [
    "按三层分别造评估集，并说清每一层的构建方法、规模和责任人。",
    "分开测检索指标和生成指标，并识破三个检索指标各自的陷阱。",
    "用四个无需人工参考答案的指标做规模化评估。",
    "识别裁判模型的位置偏差和长度偏差，并给出缓解手段。",
    "分清三类漂移，并说清哪一类补数据没用、必须重做标注。",
    "把评估挂进 CI，定出四条阻断条件。",
], "下册第 8 章")

# 对抗集怎么造
s = sl("M22", "9.1 对抗集")
heading(s, "对抗集测的是系统的下限，不是上限")
steps(s, 2.1, [
    ("①", "从真实失败案例里沉淀", "已经打穿过系统的输入，比凭空设计的攻击用例更靠谱。"),
    ("②", "用模型做变换扩增", "换表述、换语言、加噪声——把一条真实案例扩成一族。"),
    ("③", "按风险场景分类维护", "分类之后才能在门禁上按风险等级分级阻断。"),
], h=1.75)
body(s, "有几个开源工具可以直接用来探测幻觉、数据泄露、提示注入、越狱这类弱点，"
        "以及做红队方法与目标模型的大规模对比。", 4.35, size=14.5, w=10.9)
example(s, 5.1, "第 ③ 步决定了门禁能不能分级：不分类的话，对抗集只能给出一个总分，"
                "而高危项和低危项混在一个分数里，就只能一刀切阻断或者一刀切放行。"
                "上午讲的“高危阻断、中低危降级为告警”，前提是这一步做过。",
        "下册 §8.1、§8.5", h=1.5)

# 三层评估集实现
s = sl("M22", "9.1 三层评估集")
heading(s, "上册给判据，这里给构建方法")
table(s, ML, 1.95, [
    ["层级", "构建方法", "规模", "责任人"],
    ["黄金集", "业务专家标注，按难度／意图／领域三个维度分层采样", "经验值 50–100 条起步", "业务专家主导"],
    ["对抗集", "真实失败案例 → 用模型做变换扩增 → 按风险场景分类维护", "持续增长，无固定终态", "安全／红队角色"],
    ["回归集", "每次故障修复后固化“问题—主题—答案”三元组", "随故障历史线性增长", "谁修复故障谁补用例"],
], [1.2, 5.4, 2.6, 2.2], rowh=0.72)
body(s, "三个维度的分层是工程归纳，没有实证论文验证这个分层框架本身的有效性。"
        "起步规模同样是业界经验值。", 4.5, size=14.5, w=10.9)
example(s, 5.25, "分层采样解决的是一个很具体的问题：黄金集被简单案例撑满。"
                 "不分层地随机采样，采到的分布和生产流量的分布一样——"
                 "而生产流量里绝大多数是简单问题，模型本来就答得对。",
        "下册 §8.1", h=1.4)

# BLEU/ROUGE
s = sl("M22", "9.1 一个被误伤的指标")
heading(s, "BLEU/ROUGE 在企业场景失效，但别把它整个丢掉")
body(s, "这两个指标衡量的是生成文本与参考答案的表面重叠，适合评测“有相对固定标准答案”"
        "的任务。企业场景里，同一个业务问题往往有多种合理措辞的正确答案。",
     1.95, size=14.5, w=10.9)
quote(s, 2.95, "一份企业级检索增强系统的一手运营结论说得很直接："
               "通用基准评估技术，对评估“新颖的真实用户问题”价值有限。", size=15)
contrast(s, 3.95,
         ["用它评估新颖问题的首次回答质量。",
          "——失效。"],
         ["用它做回归测试：同一个问题、同一份已确认的答案。",
          "——有用，而且便宜。"],
         h=1.35, wtitle="不该这么用", rtitle="它的用武之地")
example(s, 5.6, "不要因为它在“评估新回答”上失效，就连“回归测试”这个用武之地"
                "也一起丢掉。这是一类很常见的过度纠正——"
                "一个工具在某个场景不适用，被推广成“这个工具没用”。",
        "下册 §8.1", h=1.35)

# 检索指标三个陷阱
s = sl("M22", "9.2 检索指标")
heading(s, "三个检索指标，各自的陷阱")
table(s, ML, 1.95, [
    ["指标", "陷阱"],
    ["Recall@k", "块级和文档级可能严重不一致——长文档切成多块后，只要命中其中一块，"
                 "文档级召回就会显得虚高，掩盖了“实际有用的那一块没被检索到”"],
    ["MRR", "只关心第一个相关结果的位置，不适合需要综合多篇文档才能回答的多跳问答——"
            "分数很高不代表系统真的能拼凑出完整答案"],
    ["nDCG", "能处理分级相关性，且天然对“用户只看前几条”的真实使用习惯建模，"
             "是三者里最贴近生产场景的一个"],
], [1.6, 9.8], hl=3, rowh=0.78)
quote(s, 5.0, "更容易被忽视的一条：k 值应该和生产管线实际塞进提示词的块数对齐。", size=16)
example(s, 5.9, "评估时用远大于生产配置的 k——生产只取前 3，评估却算前 20 的召回——"
                "会得到过于乐观的检索指标。这是工程共识，不是单篇论文的实证结论，"
                "但极容易在实际项目里踩坑。",
        "下册 §8.2", h=1.3)

# 无参考答案评估
s = sl("M22", "9.2 无参考答案")
heading(s, "为什么“不需要人工参考答案”对企业场景格外重要")
body(s, "企业知识库的“标准答案”往往涉及内部机密、法务口径，不可能对每一条生产流量"
        "都配一份人工写好的参考答案。", 1.95, size=15, w=10.9)
quote(s, 2.75, "无参考答案意味着可以直接拿检索到的上下文和生成的答案互相校验，"
               "不依赖额外标注就能规模化跑评估。", size=15.5)
body(s, "只有黄金集这种小规模、高价值的核心用例才需要真正配上人工参考答案。",
     3.85, size=15, color=INK, w=10.9)
example(s, 4.55, "四个核心指标里最实用的是忠实度：把回答拆解成若干条陈述，"
                 "逐条检查能否从检索到的上下文中推出。找不到支持依据的那一条，"
                 "就是幻觉的具体位置——第 3 天讲三分法时的第三步，"
                 "在这里有了可计算的形式。",
        "下册 §8.2、§4.6", h=1.55)
footer(s, "四个公式取自当前文档，与 2023 年论文原始版本有出入——原论文只提出三个指标")

# 四个指标
s = sl("M22", "9.2 四个核心指标")
heading(s, "四个指标，两两一组，分别管生成侧和检索侧")
table(s, ML, 1.95, [
    ["指标", "衡量什么", "管哪一侧"],
    ["忠实度", "回答里的每条陈述，能不能从检索到的上下文里推出来", "生成侧"],
    ["答案相关性", "回答有没有真的在回答这个问题，还是在说别的", "生成侧"],
    ["上下文精确率", "检索回来的内容里，有多少是真正相关的", "检索侧"],
    ["上下文召回率", "该被检索到的内容，有多少真的被检索到了", "检索侧"],
], [2.0, 6.4, 3.0], hl=1, rowh=0.6)
body(s, "检索指标与生成指标必须分开测。两个环节混在一个端到端准确率里评估，"
        "出问题了都不知道该往哪查。", 4.4, size=15, w=10.9, color=INK)
example(s, 5.1, "四个公式取自当前文档，与原始论文有出入：原论文只提出忠实度、"
                "答案相关性、上下文相关性三个指标，上下文精确率和召回率是框架后续迭代加入的。"
                "引用时要注意版本。",
        "下册 §8.2", h=1.45)

# 七类失败点
s = sl("M22", "9.2 失败点分类")
heading(s, "七类失败点，可以直接借用做故障归因分类体系")
table(s, ML, 1.95, [
    ["失败点", "含义"],
    ["missing content", "知识库缺内容"],
    ["search failure", "检索失败"],
    ["context window limitations", "上下文窗口限制"],
    ["poor answer generated by the LLM", "生成质量差"],
    ["incorrect output format", "输出格式错误"],
    ["vague answers", "答案含糊"],
    ["incomplete answers", "答案不完整"],
], [4.6, 6.8], hl={1, 2}, rowh=0.44)
body(s, "前两类正好对应第 3 天讲的三分法前两步。", 5.2, size=15, color=INK)
example(s, 5.65, "这份措辞逐字取自一篇论文对另一篇的复述，不是原论文自己使用的标签——"
                 "原文的七类正式名称措辞与这版复述并不完全对应。"
                 "写评估维度时可以借用这版更直白的措辞，但引用时要标注是“复述”，"
                 "不要冒充原文。",
        "下册 §8.2", h=1.45)

# 裁判五个坑：位置偏差
s = sl("M22", "9.3 位置偏差")
heading(s, "谁先出现谁占便宜，而且比想象的严重")
table(s, ML, 2.0, [
    ["谁当评委", "场景一的顺序交换冲突率", "场景二"],
    ["GPT-4", "46.3%", "5.0%"],
    ["ChatGPT", "82.5%", "52.5%"],
], [3.0, 4.4, 4.0], hl=2, rowh=0.6, accent=WRONG)
body(s, "冲突率是指“交换两个候选答案的呈现顺序后，评委给出的判断会不会跟着变”。"
        "评委模型本身的选型直接决定这个偏差的严重程度。", 4.1, size=14.5, w=10.9)
example(s, 4.9, "论文给出一个更极端的例子：故意操纵答案呈现顺序后，"
                "以 ChatGPT 为评判者，在 80 条测试查询里，"
                "某个模型在其中 66 条上“击败”了对照模型。"
                "缓解手段是每次比较都跑正反两个顺序，不一致的判为平局。",
        "下册 §8.3", h=1.5)

# 长度偏差
s = sl("M22", "9.3 长度偏差")
heading(s, "写得长比写得对更容易得高分")
body(s, "实验方法是把答案故意改写成重复堆砌、看起来详尽实则空洞的列表，"
        "测评委会不会被唬住。三个模型的失败率：", 1.95, size=15, w=10.9)
bignum(s, ML, 2.75, "91.3%", "模型 A 被唬住的比例", WRONG, 42, w=3.6)
bignum(s, ML + 3.9, 2.75, "91.3%", "模型 B", WRONG, 42, w=3.6)
bignum(s, ML + 7.9, 2.75, "8.7%", "模型 C", VERIFY, 42, w=3.4)
body(s, "同一个攻击，不同评委模型的抵抗力差一个数量级。选评委模型不是"
        "“挑一个便宜的”，它直接决定了你的评估结论可不可信。", 4.3, size=14.5, w=10.9)
example(s, 5.1, "一个已验证的缓解手段：加少量锚定示例（给评委看几个已标注好的评分范例），"
                "能把位置一致性从 65.0% 提升到 77.5%。"
                "另有研究提出控制参考答案与被测答案长度落在同一区间内再比较，"
                "从设计层面消除长度这个混杂变量。",
        "下册 §8.3", h=1.55)

# 剩下三个坑
s = sl("M22", "9.3 另外三个坑")
heading(s, "裁判五个坑，前两个讲完了，还有三个")
bullets(s, 2.0, [
    ("自我偏好", "用同一家的模型当裁判去评它自己的输出，倾向于给自己打高分。"
                  "缓解手段是换一家的模型当裁判，或者用多个裁判投票。"),
    ("分数区分度不足", "让裁判给 1–10 分，实际结果会挤在 7–8 分之间。"
                        "改用成对比较（A 和 B 哪个更好）比让它打绝对分更稳。"),
    ("裁判本身会漂移", "厂商悄悄换了模型版本，同一批样本的分数就变了。"
                        "必须锁版本，并且定期用一组固定样本回归裁判本身。"),
], size=13, gap=12)
body(s, "最后一条最容易被忽略：你的评估管道依赖一个会自己变的东西。",
     4.9, size=15, w=10.9, color=INK)
example(s, 5.5, "所以评估管道自己也要有基线：留一组已经人工确认过分数的样本，"
                "每次跑评估前先用它测一遍裁判。裁判在这组样本上的分数变了，"
                "这一轮的评估结论就不能和上一轮比。",
        "下册 §8.3", h=1.4)

# 图：三类漂移在分布图上的三种动法（书 · 图 22）
s = sl("M22", "9.4 漂移")
figpage(s, "fddrift", "“分数掉了”底下是三种完全不同的动法",
        "下册 §8.4",
        note="下一页那张表说的是同三类，但“输入分布变了”和“映射本身变了”的区别，"
             "文字怎么写都不如三张分布图并排放着清楚。")

# 三类漂移
s = sl("M22", "9.4 漂移")
heading(s, "分数掉了——先分清变的是哪一层")
table(s, ML, 1.95, [
    ["类型", "变的是什么", "现场长什么样", "处置方向"],
    ["协变量偏移", "输入分布变了，输入到答案的映射没变",
     "换了一批新客户、上线新业务线、用户开始用另一种问法", "输入端补数据、扩检索覆盖面"],
    ["先验偏移", "答案分布变了，问题本身没变", "某类工单占比从 5% 涨到 30%",
     "重新标定阈值与类别权重。别急着改模型"],
    ["概念偏移", "输入到答案的映射本身变了", "政策改了、业务规则改了、同一段描述现在该给出不同结论",
     "必须重做标注。补数据没用，因为旧标注现在是错的"],
], [1.6, 3.0, 3.6, 3.2], hl=3, rowh=0.8)
example(s, 5.35, "常见的误判是把概念偏移当成协变量偏移——于是往里补更多数据。"
                 "但旧标注在新规则下已经是错的，补进去的数据只会把错误固化得更牢。"
                 "先分类，再决定要不要动模型。",
        "下册 §8.4", h=1.4)

# PSI 的边界
s = sl("M22", "9.4 一个要标注来路的阈值")
heading(s, "PSI 的经验阈值来自信用风控，不是 LLM 场景专属")
body(s, "群体稳定性指数测的是分布距离。它能告诉你“分布变了”，"
        "但告诉不了你“变的是哪一层”——所以要先分类，再上工具。", 1.95, size=15, w=10.9)
quote(s, 2.85, "而且“用 PSI 监控评估分数序列本身的漂移”这个具体应用思路，"
               "目前没有找到直接对应的一手实践案例或论文。"
               "它是一个方法论类比，不是已经被验证过的业界标准做法。", size=14.5)
body(s, "写进方案时要标注这一点。把一个跨领域借来的阈值写成行业标准，"
        "客户方哪怕只有一个人做过风控，当场就会问出来。", 4.2, size=14.5, w=10.9)
example(s, 5.0, "评估管道自己也要当成一个需要交接的生产系统来管，监控要有三层："
                "内部状态监控（裁判调用有没有超时、有没有静默失败）、"
                "端到端行为监控（分数序列有没有不合理跳变）、"
                "告警管理（触发过的告警是不是石沉大海）。",
        "下册 §8.4、§8.5", h=1.55)

# CI 门禁
s = sl("M22", "9.5 CI 门禁")
heading(s, "四条阻断条件，各自的严格程度不同")
table(s, ML, 1.95, [
    ["检查项", "数据来源", "阻断条件"],
    ["黄金集综合分", "黄金集，业务专家标注对齐", "低于设定阈值阻断发布"],
    ["回归集通过率", "回归集，历史故障固化用例", "任何一条历史故障复发，硬性阻断，零容忍"],
    ["对抗集通过率", "对抗集，按风险场景分类", "低于阈值时按风险等级分级：高危阻断，中低危降级为告警"],
    ["检索指标相对回退", "Recall@k / nDCG，k 与生产配置对齐", "相对上一版本下降超过设定百分比，阻断"],
], [2.4, 4.0, 5.0], hl=2, rowh=0.68)
body(s, "第二行是四条里唯一零容忍的：历史故障复发没有“通过率”这种说法。",
     4.7, size=15, w=10.9, color=INK)
example(s, 5.3, "这和上午讲的“不可逆工具一次误触发就判失败”是同一种判据："
                "有些事情不能用比例衡量。下午上机要做的，"
                "就是把这条判据变成一次真实的红灯。",
        "下册 §8.5；上册 §13.2", h=1.35)

# M22 清单
s = sl("M22", "本节清单")
heading(s, "M22 清单：搭一套评估之前")
checklist(s, 1.95, [
    "三层评估集分别有多少条？黄金集是按难度／意图／领域分层采样的吗？",
    "检索指标和生成指标分开测了吗？",
    "评估用的 k 值和生产管线实际塞进提示词的块数对齐了吗？",
    "裁判模型选的是哪一家？跑过正反两个顺序吗？加过锚定示例吗？",
    "裁判本身锁版本了吗？有没有一组固定样本定期回归裁判？",
    "分数掉了的时候，先分清是协变量、先验还是概念偏移了吗？",
    "CI 门禁四条阻断条件都接上了吗？回归集那条是零容忍吗？",
    "评估管道自己有人监控吗？它挂了谁会知道？",
], size=13, gap=9)
footer(s, "下册第 8 章本章清单")

# M22 自检
s = sl("M22", "自检")
heading(s, "两道自检题")
qa(s, 2.0,
   "评估分数掉了三个点，团队打算补一批训练数据。你先问什么？",
   "先问变的是哪一层。如果是概念偏移——政策改了、业务规则改了、"
   "同一段描述现在该给出不同结论——补数据没用，因为旧标注现在是错的，"
   "补进去只会把错误固化得更牢。必须重做标注。")
rule(s, 4.3)
qa(s, 4.55,
   "回归集里有一条历史故障用例没通过，但通过率还有 97%。放行吗？",
   "不放行。回归集是四条阻断条件里唯一零容忍的——历史故障复发没有“通过率”这种说法。"
   "这和不可逆工具一次误触发就判失败是同一种判据：有些事情不能用比例衡量。")
footer(s, "下册第 8 章自检问题")

# ══════════════════════════ M24 国产化 ══════════════════════════

# ══════════════════════════ M23 技能 ══════════════════════════

sec("M23", "技能：把方法变成可执行的资产",
    "上一节讲的是“什么算做对了”。这一节讲“这件事怎么做”——"
    "以及为什么这两样必须绑在一起出现。", "12:30–13:10 · 下册第 7 章")

objectives("M23", "读完这一节你能做什么", [
    "在提示词、技能、代码、文档四种载体之间做选型，并说清选错的代价。",
    "写出三要素齐全的技能，并说明为什么触发描述是唯一会静默失效的一处。",
    "用时机、动作、判据三问从一个人的经验里萃取出一份技能。",
    "判断一份技能有没有越过上册 §6.3 那条抽象层级线。",
    "给团队的技能库定下架判据，并说清不定会发生什么。",
], "下册第 7 章")

# 开场场景
s = sl("M23", "10.0 开场")
heading(s, "三个人，三个客户，三个季度，写了同一段话")
body(s, "同一套“怎么判断一条历史工单该不该进检索库”的做法，被三个 FDE 各自摸索了一遍。"
        "三个人一共花了大约六周，产出了同一样东西的三个版本，没有一个进了团队的库。",
     1.95, size=15, w=10.9)
quote(s, 2.85, "写下来我们是写了的，但写完谁会去翻呢？"
               "下一个人遇到这事的时候，他不知道该翻什么。", size=15.5)
body(s, "这句话里有一个具体的机制问题，不只是态度问题："
        "第二级的产出物是文档，而文档的检索入口是人的记忆。"
        "一个人不知道某份文档存在，那份文档对他而言就不存在。", 3.9, size=15, w=10.9)
example(s, 4.85, "这一节讲的东西，改变的正是这一处——让那份写下来的做法，"
                 "自己知道什么时候该出现。注意它改的不是“写不写”，是“怎么被找到”。",
        "下册 §7.0", h=1.15)

# 梯子
s = sl("M23", "10.0 在梯子上的位置")
heading(s, "技能是第二级到第三级之间少掉的那一截")
table(s, ML, 1.95, [
    ["", "从前", "现在"],
    ["从第二级爬到第三级要做什么", "把做法翻译成代码", "把做法写成一份约定格式的文件"],
    ["下一个人怎么找到它", "记得它存在，然后去翻", "描述匹配上了，它自己被取出来"],
    ["做一份的成本", "以人天计", "以小时计"],
    ["维护一份的成本", "没变", "没变"],
], [3.5, 3.7, 4.2], hl=4, rowh=0.56)
body(s, "最后一行是这一节最该先记住的一行。上册 §6.2 那道梯子第三级的入场条件写的是"
        "维护——“有人愿意维护它，而且这个人被排了工时”。", 4.45, size=15, w=10.9)
example(s, 5.25, "入场条件一条没少，只是门口的队排得更长了。"
                 "当造一样东西变得很便宜，而养一样东西没有变便宜，库存就会失控——"
                 "这一节后面所有关于治理的内容，都是从这一句推出来的。",
        "上册 §6.2；下册 §7.0", h=1.3)

# 加载三阶段
s = sl("M23", "10.0 加载机制")
heading(s, "描述是唯一的入口，而且它不报错")
steps(s, 2.05, [
    ("①", "发现", "启动时，全部技能一次性进来——但只有名称和描述这两行。"),
    ("②", "激活", "任务与某个描述匹配上，才读入完整的说明文件。"),
    ("③", "执行", "按需读取被引用的文件、运行附带的脚本。"),
], h=1.75)
bullets(s, 4.1, [
    ("描述没匹配上，这份技能就等于不存在。", "步骤写得再好也没用，而且它不报错。"),
    ("技能的边际成本不是零，是一条描述。", "库里每多一份，每次会话都要多带一条。"),
], size=14.5)
example(s, 5.5, "这两条后面反复要用。第一条是本节唯一一处静默失效——"
                "现场表现是“agent 像是没有这份技能”，这和“技能写得不好”、"
                "“模型今天状态不好”在观感上完全无法区分。",
        "下册 §7.0；出处 agentskills.io，逐字核对", h=1.35)

# 四选一
s = sl("M23", "10.1 四选一")
heading(s, "不是所有做法都该做成技能")
table(s, ML, 1.95, [
    ["载体", "适合什么", "判据", "选错的代价"],
    ["提示词", "这次之后不再需要的", "只用一次", "每次重打，且每次都不一样"],
    ["技能", "反复出现、需要判断、步骤能写清", "重复 ＋ 有判断 ＋ 可复述",
     "见失效模式那一页"],
    ["代码", "反复出现、不需要判断", "能写出确定性断言",
     "得到一个慢一百倍、还不稳定的函数"],
    ["文档", "需要人读、需要理解背景", "读者是人", "没人读得懂它为什么这么定"],
], [1.5, 3.3, 2.9, 3.7], hl=2, rowh=0.62)
example(s, 4.85, "最容易搞错的是第三行：有确定答案的事写成代码，一行断言就能验；"
                 "写成技能，你得用评估集去验一件本来可以被证明的事。"
                 "判据是上册第 13 章那条——这件事能不能写成一条确定性断言。",
        "下册 §7.1", h=1.35)

# 三要素
s = sl("M23", "10.2 三要素")
heading(s, "一份技能只回答三件事，缺哪件坏法不同")
table(s, ML, 1.95, [
    ["要素", "回答什么", "缺了会怎样"],
    ["触发时机", "什么样的情况该用它", "静默失效：躺在库里，永远不被取出来"],
    ["可执行步骤", "照着走就不跑偏的动作顺序", "变成一句正确的废话（“要仔细核对”）"],
    ["验收护栏", "怎么算做对了，什么不许做", "用两次就没人信了"],
], [1.9, 4.2, 5.3], hl=1, rowh=0.62)
body(s, "步骤那一栏有个当场能用的检验：把它念给一个没做过这件事的同事，让他复述一遍。"
        "复述不出来的那一步，就是写得不够具体的那一步。", 4.15, size=15, w=10.9)
example(s, 4.95, "护栏里“不许做”的那一半最容易被省掉，也最值钱。"
                 "一份没有“不许臆造客户信息”这类约束的技能，偶尔编一次，"
                 "之后所有输出都要重新核——它的可信度不是线性下降的，是一次归零。",
        "下册 §7.2", h=1.35)

# 触发时机
s = sl("M23", "10.2 触发时机")
heading(s, "写场景，不写功能")
contrast(s, 2.0,
         ["“处理商机”",
          "“执行商机健康度诊断”",
          "只写这份技能是什么",
          "不写什么时候不该用它"],
         ["“例会前过单、或某条商机阶段停留超期时”",
          "把一线会说的原话写进去：“这单靠谱吗”“卡在哪了”",
          "写用户会遇到的情形——匹配发生在这上面",
          "写清不适用的情形，挡掉误命中"],
         h=2.35, wtitle="匹配不上的写法", rtitle="能匹配上的写法")
example(s, 4.75, "误命中比不命中更难查：它不静默，但它伪装成了正常工作——"
                 "agent 取出一份不该用的技能，然后一丝不苟地照着一套不适用的流程做完了。"
                 "输出看起来很有章法，只是章法用错了地方。",
        "下册 §7.2、§7.4", h=1.5)

# 萃取三问
s = sl("M23", "10.3 萃取")
heading(s, "方法就是第 4 关那套，只是对象换了")
body(s, "萃取一份技能，用的是上册第 4 关的现场调研——情境访谈加影子跟随。"
        "以前挖的是一个流程，现在挖的是一个人的判断。三问正好落到三要素，不用翻译：",
     1.95, size=15, w=10.9)
table(s, ML, 2.85, [
    ["现场问什么", "具体怎么问", "落成哪一部分"],
    ["时机", "你什么情况下会启动这套做法？什么情况下不会？", "触发时机"],
    ["动作", "你先看什么、再看什么？哪一步会分叉？", "可执行步骤"],
    ["判据", "你凭什么说这次做对了？什么情况下你会推翻自己？", "验收护栏"],
], [1.6, 6.6, 3.2], rowh=0.52)
example(s, 4.95, "三条从第 4 关直接继承的纪律：不要让人写心得，看他做三个真实案例；"
                 "产物离开现场之前必须念给他听一遍，他纠正的那几处往往正是真正的价值所在；"
                 "中间那版“人能照着做的步骤说明”不能跳过——就算最后不做成技能，"
                 "它本身也已经是产出物了。",
        "上册 §10.2、§10.3；下册 §7.3", h=1.6)

# 抽象层级
s = sl("M23", "10.3 边界")
heading(s, "技能比文档更容易越过那条线")
quote(s, 1.95, "把这份技能原样交给这个客户的直接竞争对手，他能不能因此少走弯路？"
               "能，就是不能带的那一层。", size=15.5)
table(s, ML, 3.0, [
    ["可以进技能（方法）", "不能进技能（客户家底）"],
    ["“阶段停留超过基准天数就标风险”", "这家客户各阶段的具体基准天数"],
    ["“按证据校准赢率，不按感觉”", "这家客户历史赢率的分布"],
    ["“抽样看结论字段是不是模板套话”", "这家客户哪几张表的结论字段不可信"],
], [5.7, 5.7], rowh=0.5)
example(s, 4.95, "原因很具体：技能是要被执行的，而带着具体数值的版本执行起来更顺手——"
                 "写“停留超过 14 天”比写“超过基准天数（由各客户标定）”省事，效果当场还更好。"
                 "省事的那个做法恰好是越界的那个。做法：把客户专属的量值全部提到技能之外。",
        "上册 §6.3；下册 §7.3", h=1.55)

# 示例产物
s = sl("M23", "10.2 示例产物")
picpage(s, "skill.png", "一份填完的技能，连同它的命中集和护栏用例",
        "下册 §7.2、§7.5 ｜ 教学示例，非真实客户数据",
        note="三样必须同框。只看左边那份文件，它就还是“一段格式好看的提示词”。"
             "右下角那格“负责人：已指定，维护工时已排”才是判断它在不在第三级的那一条。")

# 验收
s = sl("M23", "10.5 验收")
heading(s, "三层评估集照用，但技能多一层")
table(s, ML, 1.95, [
    ["层", "在技能上验什么", "与第 9 章的差别"],
    ["黄金集", "给定典型输入，输出符不符合护栏", "基本一致"],
    ["对抗集", "边界输入、诱导跳过护栏的输入", "种子仍然要来自真实工单"],
    ["回归集", "这份技能自己造成过的每一次故障", "来源单一，所以纪律更简单"],
    ["命中集", "该命中的命中了吗，不该命中的没命中吧", "技能特有，第 9 章没有"],
], [1.5, 5.2, 4.7], hl=4, rowh=0.58)
body(s, "命中集怎么造：正例二十条来自萃取现场那个人的原话；"
        "反例二十条来自库里描述最接近的那两三份技能的正例。", 4.5, size=15, w=10.9)
example(s, 5.25, "每次改动描述，命中集必须重跑——描述是唯一入口，改它等同于改接口。"
                 "回归集的判据也很直接：一份技能修过三次，回归集里就该有三条；"
                 "对不上，说明有人改了没补。",
        "下册 §7.5", h=1.35)

# 治理
s = sl("M23", "10.6 治理")
heading(s, "下架不是整理工作，是预算")
body(s, "按加载机制，库里每一份技能都在每次会话里占着一条描述。这是常驻成本——"
        "到某个规模之后，下架一份没人用的技能，比新增一份有用的技能收益更大。",
     1.95, size=15, w=10.9)
for i, (k, t, d_) in enumerate([
        ("长期未命中", "连续 N 个月没被取出来",
         "要么它没用，要么它的描述写坏了。两种都得处理，处理方式不同，"
         "但都不该是“放着不管”。"),
        ("负责人不在了", "它掉回第二级了",
         "上册 §6.2 第三级的入场条件就是“有人愿意维护，且被排了工时”。"
         "要么补一个负责人，要么下架。"),
        ("护栏失效", "评估集连续两次不通过而没人修",
         "这时它已经不是资产了，是一份会主动跳出来给错误答案的负债。")]):
    card(s, ML + i * 3.85, 2.95, 3.55, 2.3, k, t, d_)
example(s, 5.45, "阈值按自己的库标定，不要照抄。没有下架机制的技能库，"
                 "最终会退化成一个没人敢删的目录——那时它是技术债，只是换了个存放位置。",
        "下册 §7.6", h=1.15)

# 失效模式
s = sl("M23", "10.7 失效模式")
heading(s, "六种坏法，前两种最难查")
table(s, ML, 1.95, [
    ["失效", "现场表现", "根因"],
    ["静默不命中", "agent 表现得像没这份技能", "描述写成了功能而不是场景"],
    ["误命中", "输出很有章法，但章法用错了地方", "描述没写不适用边界"],
    ["触发词打架", "同类任务每次的行为不一样", "两份技能描述高度重叠"],
    ["技能膨胀", "上百份，没人说得清哪些还在用", "只有新增机制，没有下架机制"],
    ["写死客户信息", "换个客户就不对，而且没人发现", "具体量值写进了技能正文"],
    ["钥匙还在原作者手里", "别人用它出问题，还是回来问他", "停在第二级，只是看着像第三级"],
], [2.5, 4.6, 4.3], hl={1, 2}, rowh=0.5)
example(s, 5.35, "最后一行值得单独记：一份技能看起来是可以自己运转的文件，"
                 "但如果护栏里塞满了只有原作者知道来由的判定口径，"
                 "它就是第 3 天讲的英雄主义陷阱换了个形式——"
                 "依赖从一个人转移到了一份文件，而文件的钥匙还在那个人手里。",
        "下册 §7.7；上册 §3.1", h=1.5)

# 清单
s = sl("M23", "本节清单")
heading(s, "M23 清单：一份技能进库之前")
checklist(s, 2.0, [
    "它是在三个不相关客户身上见过同样结构之后才做的，不是一次漂亮的定制",
    "有一个负责人，而且这个人的维护工时被真的排进去了",
    "触发描述写的是场景和用户会说的话，并且写清了不适用的情形",
    "步骤能被一个没做过这件事的人复述出来",
    "护栏写了“什么算做对”和“什么不许做”两半",
    "客户专属的具体量值全部提到技能之外，技能里只读配置",
    "有命中集：正反各二十条起，改描述必重跑",
    "下架判据事先定好了",
], size=14, gap=11)
example(s, 6.05, "第二条和第六条是最常被跳过的两条，也是区分“资产”和“负债”的两条。",
        "下册第 7 章 本章清单", h=1.0)


sec("M24", "国产模型与信创改造",
    "同一个系列，两份不同的许可证。这一节的第一步判据不是效果，是授权——"
    "许可证一票否决，不进入后续任何一步比较。", "13:10–14:00 · 下册第 12、13 章")

s = sl("＊", "本节金句")
maxim(s, "拿到任何一个“必须”，先让它报出来源并定级。",
      "法律 / 部门规章 / 合同标书 / 内部制度 / 架构规范 / 对接人偏好，六级。定级的动作很简单——**问一句“这条写在哪儿”，看对方能不能拿出那份东西。**",
      "逐字", "下册 §13.1", size=32)

objectives("M24", "读完这一节你能做什么", [
    "按五步判据做一次国产模型选型，并说清第一步为什么是许可证。",
    "区分估算显存和官方显存，并知道估算值要留多少余量。",
    "说清信创约束的三层来源，以及为什么绕不开。",
    "面对“国产芯片性能如何”这个问题，给出诚实的回答而不是挑一个好看的数字。",
    "识破“多语言榜单第一”和“中文榜单第一”这两个不同口径。",
], "下册第 12、13 章")

# 许可证一票否决
s = sl("M24", "10.1 许可证")
heading(s, "档位相邻不代表授权条款相邻")
table(s, ML, 1.95, [
    ["档位", "许可证", "商用限制"],
    ["3B 档", "研究许可协议", "仅限研究与评估，不可商用——和同系列其他档位不是同一份法律文本"],
    ["7B / 32B 档", "Apache 2.0", "可商用，无月活门槛"],
    ["72B 档", "自有许可证", "可商用，但超一定月活规模需申请授权"],
], [1.8, 3.2, 6.4], hl=1, rowh=0.66, accent=WRONG)
body(s, "如果最终交付物要商用，第一档直接从选型集合里删掉，不进入后续任何一步比较。",
     4.3, size=15.5, w=10.9, color=INK)
example(s, 4.95, "这一条是这一章最容易让人栽跟头的地方："
                 "团队按效果和显存选完了型、跑完了评测、写完了方案，"
                 "法务看一眼许可证，整套选型作废。"
                 "把许可证放在第一步，不是因为它最重要，是因为它最不可协商。",
        "下册 §12.1", h=1.55)

# 五步判据
s = sl("M24", "10.2 五步判据")
heading(s, "五步，顺序不变，但每步花多少精力看约束")
steps(s, 2.1, [
    ("①", "商用授权能不能接受", "一票否决。"),
    ("②", "显存预算能不能满足", "先看现场实际能批到的 GPU 型号与数量，再倒推能装下哪些档位。"),
    ("③", "上下文长度是否够用", "配置默认值和官方声明的最大值经常不是一回事。"),
], h=1.85)
steps(s, 4.1, [
    ("④", "是否需要函数调用", "都支持某种形式，但成熟度不一样。推理见长的模型工具调用能力相对弱。"),
    ("⑤", "中文能力是否达标", "没有能直接照抄的权威打分表，只能拿客户真实中文语料实测。"),
], h=1.5)
example(s, 5.9, "第三步的坑很具体：官方声明的长上下文往往是扩展技术开启后的数字，"
                "配置文件默认值要小得多。按默认配置部署拿到的不是文档写的那个上限。"
                "“文档写了就等于开箱可用”不成立。",
        "下册 §12.2", h=1.25)

# 显存口径
s = sl("M24", "10.1 显存口径")
heading(s, "估算值和官方数字，不能用同样的语气呈现")
contrast(s, 2.0,
         ["按每参数 2 字节估算的理论值。",
          "只算权重本身，不含 KV Cache 与实际批处理占用。",
          "拿它做采购预算，会贴着上限配。"],
         ["厂商官方系统卡给出的最低／推荐两档。",
          "包含了官方口径下的实际部署余量。",
          "可以直接对照现场硬件清单。"],
         h=2.0, wtitle="估算值", rtitle="官方数字")
quote(s, 4.3, "只有估算值时，要按至少 20%–30% 的余量往上留，并且用真实压测数据校正。"
              "不能把估算值当成能贴着上限部署的安全线。", size=15)
example(s, 5.5, "这一页和第 1 天讲的可信度分层是同一件事，只是这次的后果是硬件采购单。"
                "把一个估算值写进采购预算，缺的那 20% 到货之后补不上——"
                "GPU 不是加内存条。",
        "下册 §12.1；上册 §2.2", h=1.4)

# 平台层
s = sl("M24", "10.3 平台层")
heading(s, "先说清楚这一节能给到什么确定性")
quote(s, 2.0, "三个国内平台的计费细则本书都没能拿到可逐字核对的官方原文。", size=16)
body(s, "这意味着涉及具体计费规则、私有化条款的表述，凡是没有标注“已核对原文”的，"
        "都不能当成可以直接写进客户合同或报价单的确定数字。", 2.9, size=14.5, w=10.9)
table(s, ML, 3.7, [
    ["能确认的", "不能确认的"],
    ["某平台兼容主流 SDK 形状，降低多平台切换的应用层改造成本", "私有化部署的具体条件、报价、服务等级承诺"],
    ["某平台按输入／输出 token 分别计价，预置与调优模型均可获得专享推理服务", "更具体的批量折扣与缓存折扣细则，复核时未能在当前页面定位到"],
], [5.4, 6.0], rowh=0.72)
example(s, 5.5, "三个平台都以“聚合多模型 ＋ 兼容层”作为卖点，表面上降低了锁定风险。"
                "但真正决定迁移成本的通常不在这一层，而在更深的地方——"
                "这一条建议在真实选型时直接向厂商或已落地案例求证，"
                "不要仅凭搜索结论下断言。",
        "下册 §12.3", h=1.3)
footer(s, "涉密／物理隔离场景从架构原点就排除了这三个平台")

# 中文 RAG
s = sl("M24", "10.4 中文 RAG")
heading(s, "两个不同榜单口径，不能混用")
table(s, ML, 1.95, [
    ["模型", "已核实的评测数字", "缺口"],
    ["某 2023 年中文专项模型", "官方模型卡自称中文子榜均分 64.53（发布时点的自我声明）",
     "最大序列长度只有 512，长合同、长手册会成瓶颈"],
    ["某更新一代多语言系列", "8B 档在多语言榜单排名第一（截至某日，70.58 分，已逐字核对官方博客）",
     "官方博客通篇没有单独公布这一档在中文子榜的分数"],
], [3.0, 5.0, 3.4], rowh=0.85)
quote(s, 4.3, "“多语言榜单第一”和“中文榜单第一”是两个不同的评测口径，"
              "前者不能直接当成后者的替代证据来引用。", size=15)
example(s, 5.4, "榜单分数不适用的两种情况：客户语料严重偏离通用领域"
                "（法律条款、医疗术语、内部产品代号）；以及需要配合重排模型的两阶段架构——"
                "只选好 embedding 就结束，“选型完成”的判断是不完整的。",
        "下册 §12.4", h=1.5)

# 信创三层来源
s = sl("M24", "11.1 信创")
heading(s, "约束从哪来：三层，一层比一层硬")
for i, (k, t) in enumerate([
        ("采购评分项", "招投标文件经常直接写明技术栈需在信创名录内。"
                        "一旦写进标书就是硬门槛，不是加分项。"),
        ("数据本地化的法律驱动", "关键信息基础设施运营者境内产生的个人信息和重要数据"
                                  "原则上境内存储，出境需走安全评估。"),
        ("否决权结构", "银行的科技口卡变更审批、国企的采购口卡流程选择——"
                        "这两个环节的负责人对“是否符合信创要求”通常没有商量余地。")]):
    card(s, ML + i * 3.85, 2.0, 3.55, 2.5, "第 %d 层" % (i + 1), k, t, bsize=12)
body(s, "绕开信创条款去谈业务价值，说服不了卡流程的人。", 4.85, size=15.5, color=INK)
example(s, 5.4, "第 2 天讲三类客户否决权结构时说过：采购口对流程合法性的一票否决权，"
                "比业务口的技术认可更硬。信创是这条判断最具体的一个例子。",
        "下册 §13.1；上册 §17.1", h=1.35)

# 与 CUDA 的差异
s = sl("M24", "11.2 迁移工作量")
heading(s, "编程范式不同，这是最影响工作量的一条")
body(s, "主流 GPU 的工具链是通用异构计算工具链；某国产 NPU 的工具链定位为"
        "定制化 AI 异构计算工具链。开发范式上，后者采用“任务块级抽象”，"
        "区别于前者的“线程级模型”。", 1.95, size=14.5, w=10.9)
card(s, ML, 3.0, 11.4, 1.5, "多篇技术博客给出过具体量化对比数字", None,
     "“能效比提升 32%”“内存带宽利用率 92%”“算子自动转换在某图像分类模型训练中"
     "达到原生生态 85% 的性能”——这些数字均来自单一作者的自述实测，缺乏第三方交叉验证。",
     accent=WRONG, bsize=14)
quote(s, 4.75, "本书不采信，只记录“存在这类说法但未核实”这一事实。", size=16)
example(s, 5.6, "这一页和第 1 天讲可信度分层是同一条纪律，只是这次的后果更直接："
                "把一个未经交叉验证的性能数字写进技术方案，"
                "客户按这个数字批了硬件预算，跑不出来的时候是你在现场。",
        "下册 §13.2；上册 §2.2", h=1.4)

# 国产芯片现状
s = sl("M24", "11.2 算力层")
heading(s, "这张表最该被记住的是它没覆盖的东西")
table(s, ML, 1.95, [
    ["厂商", "适配现状（多方财经媒体交叉确认）", "技术路线"],
    ["A 厂", "2025 年 9 月，某国产大模型在其芯片上实现混合量化推理部署", "专用推理框架适配"],
    ["B 厂", "基于 vLLM 完成适配，新一代 GPU 可在原生 FP8 精度下稳定运行", "vLLM 兼容路线"],
    ["C 厂", "自研计算平台库兼容开源生态，已适配多个主流开源模型，推理场景比训练场景更成熟", "类 CUDA 兼容路线"],
], [1.3, 7.3, 2.8], rowh=0.72)
quote(s, 4.5, "没有任何一家国产芯片厂商公开发布过与主流 GPU 同规格对比的跑分或吞吐量基准。",
      size=16)
example(s, 5.4, "能查到的说法互相矛盾：一篇技术博客称某芯片训练效率可达对标产品的 80%，"
                "另一篇称在特定测试中性能已超越对标产品 20%。"
                "这批数字构不成证据链。挑一个更好看的引用，等于制造一个虚假的确定性。"
                "正确做法是照实说“数据矛盾、无权威基准”。",
        "下册 §13.2", h=1.55)

# 基础软件层
s = sl("M24", "11.3 基础软件层")
heading(s, "操作系统与数据库这一层，官方与转述要分开读")
body(s, "国产操作系统厂商的官方知识平台提供本地模型配置手册，说明安装配置全流程"
        "（版本确认 → 模型选择 → 安装 → 服务确认 → 测试）。这是官方材料。",
     1.95, size=14.5, w=10.9)
card(s, ML, 2.9, 11.4, 1.35, "第三方转述的说法", None,
     "“适配多家大模型，覆盖 40+ 场景”“适配 6000 余家生态伙伴的百万级软硬件”——"
     "这类数字来自第三方转述，不是可逐字核对的官方口径。", accent=CAVEAT, bsize=14.5)
body(s, "对交付的实际影响：官方手册说明的是“能装”，转述的数字说明不了“你这套能不能装”。",
     4.55, size=15, w=10.9, color=INK)
example(s, 5.2, "数据库替换是这一层最容易被低估的一件事：第 2 天讲的复盘二"
                "有一句开场——“交付前两周，被告知数据库要换”。"
                "这类变更改的不是配置，是所有依赖数据库方言的查询。",
        "下册 §13.3；上册第 20 章", h=1.45)

# CPU 降级
s = sl("M24", "11.6 性能损耗")
heading(s, "“CPU 比 GPU 慢多少倍”这个问题没有可信的公开答案", size=29)
table(s, ML, 2.2, [
    ["方案", "平均输出速率", "平均延迟", "备注"],
    ["vLLM-CPU", "36.57 tok/s", "3.50s（首字 539ms）", "200 请求全部成功"],
    ["llama.cpp", "39.34 tok/s", "—", "同等条件下出现请求失败（199/200 成功）"],
], [2.6, 3.0, 3.0, 2.8], rowh=0.62)
body(s, "这是第三方作者原创实测，非本书实测。测试环境 32 vCPU、128GB 内存，"
        "输入 1024 tokens、输出 128 tokens、请求数 200。", 4.15, size=14, w=10.9)
example(s, 4.9, "这组数字没有 GPU 对照组，文章本身也没有给出与 GPU 方案的具体倍数对比。"
                "它只定性描述了 CPU 推理在长上下文预填充阶段开销大、内存带宽是主要瓶颈。"
                "书中如果要给出损耗数量级，必须明确注明这是基于单点第三方实测的估算，"
                "不是权威结论。",
        "下册 §13.6", h=1.55)

# 信创改造检查清单
s = sl("M24", "11.5 改造清单")
heading(s, "信创改造检查清单：按层过一遍")
checklist(s, 1.95, [
    "算力层：目标芯片是哪一家？推理框架走哪条路线——专用框架、兼容层还是类 CUDA？",
    "算力层：模型档位在目标芯片的官方支持清单里吗？清单是哪个版本的？",
    "基础软件层：操作系统、数据库、中间件各自的国产替代确定了吗？",
    "基础软件层：数据库方言差异梳理过吗？依赖特定方言的查询有多少条？",
    "云原生层：容器运行时、编排、镜像仓库的国产替代能不能跑通现有的部署包？",
    "架构层：多架构镜像构建过吗？ARM64 那一版实测跑过吗？",
    "性能：损耗数字是自己压测出来的，还是引用的第三方博客？",
], size=13, gap=10)
example(s, 5.95, "最后一条是这张清单里唯一没有标准答案的：没有可信的公开基准，"
                 "只能自己压。但至少要知道“我引用的这个数字是哪来的”。",
        "下册 §13.5", h=1.05)

# M24 清单
s = sl("M24", "本节清单")
heading(s, "M24 清单：做一次国产化选型")
checklist(s, 1.95, [
    "第一步：每个候选档位的许可证逐一看过了吗？要商用的直接删掉不可商用的档位。",
    "第二步：显存数字是估算还是官方？估算的留了 20%–30% 余量吗？",
    "第三步：官方声明的上下文上限，是默认配置就有，还是要开扩展？",
    "第四步：需要函数调用吗？候选档位的工具调用成熟度实测过吗？",
    "第五步：中文能力用客户真实语料测过吗，还是引用了榜单分数？",
    "信创：三层约束来源各自的具体要求确认了吗？",
    "性能损耗：有自己的压测数据吗？没有的话，方案里的数字标了来源吗？",
], size=13.5, gap=11)
footer(s, "下册第 12、13 章本章清单")

# 评估设计的盲区
s = sl("M22", "9.2 自查")
heading(s, "三条用来自查评估设计有没有留盲区")
bullets(s, 2.0, [
    ("缺少诊断能力", "大多数基准把系统当黑盒，只报端到端准确率，"
                      "出问题时无法定位是检索环节还是生成环节。"),
    ("缺少复合任务评估", "很少评估需要综合理解复杂版式、表格图表、跨文档信息整合"
                          "这类复合能力的任务。"),
    ("缺少证据可追溯性", "大多数基准只看最终输出对不对，忽视了证据可追溯性——"
                          "而这恰恰是企业场景的核心诉求。"),
], size=13.5, gap=12)
body(s, "第三条直接呼应骨架第三层的“证据”：客户要的不是一个分数，"
        "是“这个结论是从哪条材料来的”。", 4.85, size=14.5, w=10.9, color=INK)
example(s, 5.55, "拿这三条对着自己的评估设计过一遍，通常会发现第一条已经做了"
                 "（分开测检索和生成），第三条基本没做——"
                 "而第三条恰恰是客户验收时会问的那一条。",
        "下册 §8.2；上册 §9.0", h=1.4)

# ══════════════════════════ M25 案例串讲 ══════════════════════════

sec("M25", "四个案例串讲",
    "只讲每个案例的两件事：哪些方案被否决了，以及哪些经验不能直接搬。"
    "完整案例在教材里，课上讲的是判断本身。", "14:00–14:40 · 下册第 14–17 章")

# 案例一
s = sl("M25", "案例一")
heading(s, "用 MCP 盘活遗留大型机：它能说出该怎么做，但做不了")
card(s, ML, 2.0, 5.55, 2.2, "可迁移", "统一封装 ＋ 三角色分权",
     "用 MCP 统一封装遗留接口、避免为每个消费方重复写定制适配代码，"
     "任何“业务逻辑深埋在没有现代 API 的遗留系统里”的行业都适用。"
     "发现／操作／合规三角色分权 ＋ 人在回路的架构模式，"
     "可以迁移到保险理赔、政务审批等同样有强监管人工签字要求的场景。", accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.2, "不可直接迁移", "权限映射与工具粒度",
     "大型机侧粗粒度权限模型到工具级权限的映射规则，是这家银行遗留系统的具体产物，"
     "换一家基本要重新梳理。三个工具的粒度划分是这个业务域专属的，"
     "换成理赔场景要按理赔流程重新设计。", accent=CAVEAT)
example(s, 4.65, "这个案例的核心困境值得单独记：“LLM 有脑无手”。"
                 "它能说出该怎么做，但做不了——而中间隔着的不是技术，是权限和责任。"
                 "这正是第 2 天讲本体时那句话的案例版。",
        "下册第 14 章；上册 §5.1", h=1.5)

# 案例二
s = sl("M25", "案例二")
heading(s, "工业质检：准确率 96%，但产线不用")
card(s, ML, 2.0, 5.55, 2.2, "可迁移", "不对称代价 ＋ 可视化判定依据",
     "不对称代价决定阈值策略这一判据，适用于所有“漏检代价远高于误检代价”的检测场景。"
     "轻量化边缘模型 ＋ 云端只做训练迭代的分工模式，可以直接搬到其他产线节拍敏感的场景。"
     "给一线用户可视化判定依据，是任何“AI 替代人工判断”项目都要过的信任建立关卡。",
     accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.2, "不可直接迁移", "阈值与标定",
     "具体的模型骨干网络选择、阈值数值、光照校准周期，"
     "都随产线、缺陷类型、相机硬件变化，换一条产线基本要重新标定。"
     "不存在“一套阈值走天下”的配置。", accent=CAVEAT)
example(s, 4.65, "“给一线用户可视化判定依据”这一条，和第 2 天讲采用率崩塌时"
                 "那个补货建议工具的教训是同一条：系统给出的是一个数字，"
                 "没有解释“为什么”，一线遇到冲突时第一反应是不信任。",
        "下册第 15 章；上册 §15.5", h=1.5)

# 案例三
s = sl("M25", "案例三")
heading(s, "智慧矿山：选好的模型，进不了井")
card(s, ML, 2.0, 5.55, 2.2, "可迁移", "认证边界先于模型选型",
     "先由认证／合规边界确定算力天花板，再做模型选型——这个思路可以迁移到"
     "任何有强制性硬件认证要求的边缘部署场景，不限于矿山。"
     "认证给出的物理约束，永远比模型精度更早决定架构的可行边界。"
     "断点续传加延迟同步的架构也可迁移。", accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.2, "不可直接迁移", "认证体系与网络拓扑",
     "具体的防爆认证标准、审批周期、认证机构的判定尺度，"
     "是这个行业和所在监管辖区特有的。井下网络覆盖的具体拓扑"
     "（哪些巷道有网、哪些完全没信号）是这座矿井特有的。", accent=CAVEAT)
example(s, 4.65, "这个案例是“可行性三角之外还有一道门禁”的极端版本："
                 "第 1 天讲的第四盏红灯是备案和数据出境，在这里变成了硬件认证。"
                 "形状一样——改的不是方案里的某个环节，是整个架构的前提。",
        "下册第 16 章；上册 §11.1", h=1.5)

# 案例四
s = sl("M25", "案例四")
heading(s, "300 毫秒生命线：高并发 RAG 性能手术")
table(s, ML, 2.0, [
    ["环节", "预算（ms）", "主要杠杆"],
    ["网关接入 / 鉴权 / 限流判断", "10", "只要不引入同步远程调用即可"],
    ["语义缓存查询", "5", "与后续检索并行触发，不阻塞判断"],
    ["混合检索（两路并行 ＋ RRF）", "40", "向量库选型与两路并行发起，不是砍检索路数"],
    ["Rerank 精排", "30", "深度与延迟强相关，必须选轻量模型、裁剪候选规模"],
    ["生成（量化模型推理）", "170", "预算里最大的一块，优化空间最大"],
    ["序列化 ＋ 网络回传 ＋ 缓冲", "30", "缓冲不分配给任何具体环节，专门留给排队和抖动"],
], [4.6, 2.4, 4.4], hl=5, rowh=0.46)
example(s, 5.35, "可迁移的是这张表本身，不是里面的数字：任何“用户在等一个实时结果”"
                 "的场景都可以先做一张延迟预算分解表。不可迁移的是 300ms 这个数字、"
                 "各环节的具体分配、RRF 的 k 值、量化方案——"
                 "换一个客户基本要重新分配。",
        "下册第 17 章", h=1.5)

# 被否决的方案
s = sl("M25", "四个案例")
heading(s, "四个案例都从被否决的方案讲起，这不是修辞")
table(s, ML, 1.95, [
    ["案例", "一个被否决的方案", "否决理由的形状"],
    ["MCP 盘活遗留大型机", "为每个消费方单独写定制适配代码", "成本随消费方数量线性增长，且逻辑重复"],
    ["工业质检", "追求整体准确率最高的阈值", "漏检和误检的代价不对称，整体最优不等于业务最优"],
    ["智慧矿山", "先选效果最好的模型，再想办法过认证", "认证给出的物理约束比模型精度更早决定可行边界"],
    ["高并发 RAG", "先优化最容易优化的那一段", "不做延迟预算分解，优化的可能是占比最小的那一段"],
], [2.5, 4.2, 4.7], rowh=0.66)
body(s, "四条否决理由的共同形状：它们都不是“这个方案做不出来”，"
        "而是“这个方案做出来了也不解决问题”。", 4.85, size=14.5, w=10.9, color=INK)
example(s, 5.55, "作品集里最该写的也是这一段。别册三第 2 章那份作品集模板里，"
                 "“被否决的方案（至少写一个，说清楚为什么否决）”是加粗的一条——"
                 "因为它是唯一能证明你做过判断的部分。",
        "下册第 14–17 章、别册三 §2.3", h=1.4)

# 压测方法论
s = sl("M25", "案例四")
heading(s, "四条对所有“倍数提升”都适用的压测纪律")
bullets(s, 2.0, [
    ("基线必须同等并发条件", "不能拿旧链路在日常低并发下的数字当基线。"),
    ("模拟真实流量曲线", "不是恒定并发的稳态测试——真实流量是有峰谷的。"),
    ("同时报 P50/P95/P99", "不能只报均值。均值会被大量命中缓存的快请求拉低。"),
    ("冷启动和缓存命中单独统计", "这两类请求必须单独统计，不能混进主指标。"),
], size=13.5, gap=12)
quote(s, 4.9, "这套方法论对所有声称“倍数提升”的性能优化项目都适用。", size=16)
example(s, 5.75, "第三条和第四条是同一个问题的两面：一个把慢请求藏起来，"
                 "一个把快请求混进来。两者叠加，能把任何一次优化都说成成功。"
                 "第 2 天讲验收口径那四问，在这里换成了压测口径。",
        "下册 §17.4；上册 §13.3", h=1.35)

# ══════════════════════════ M26 90 天与作品集 ══════════════════════════

sec("M26", "90 天上岗与作品集",
    "散读散做的那三个月。这一节给一条能自己走完的路线，"
    "以及一份能证明你做过判断的作品集。", "14:40–15:00 · 别册三")

s = sl("M26", "1.1 时间线")
heading(s, "13 周，每周一个产出物和一个自检点")
table(s, ML, 1.95, [
    ["周", "主题", "Lab", "自检点"],
    ["1–2", "认知定位、需求发现与定界", "Lab-11", "范围文档能不能扛住“拒绝的艺术”里那几句追问"],
    ["3–4", "薄切片与评估、生产推广", "Lab-05", "故意引入劣化改动，门禁是否真的会拦下来"],
    ["5–6", "企业集成、数据管道与本体", "Lab-02、Lab-01", "证书轮换过程中是否真的零失败请求"],
    ["7–8", "Agent 编排、检索质量与高并发", "Lab-03、Lab-04、Lab-06", "注入故障时降级路径是否生效而不是直接崩溃"],
    ["9–10", "离线部署、案例集精读", "Lab-07、Lab-08、Lab-10", "断网容器能否起服务；日志里 PII 是否已脱敏"],
    ["11–13", "中国市场与合规、毕业项目、复盘", "Lab-09、Lab-12", "自评量表能否覆盖六道关卡的全部通过标准"],
], [1.0, 3.6, 2.8, 4.0], rowh=0.62)
example(s, 5.9, "这张表的价值在最后一列。每周的产出物都容易伪装成完成——"
                "自检点问的是“它真的成立吗”。第 13 周那一条最狠："
                "作品集讲的是被否决的方案和为什么否决，还是只秀最后做成了什么。",
        "别册三 §1.1", h=1.35)

s = sl("M26", "2.3 作品集")
heading(s, "作品集按案例章的结构写，七段")
bullets(s, 2.0, [
    "客户背景（可以注明是训练场景）",
    "真实冲突",
    "被否决的方案（至少写一个，说清楚为什么否决）",
    "最终架构决策",
    "关键代码或架构图",
    "踩过的坑",
    "复盘：哪些可迁移，哪些不可直接迁移",
], size=14, gap=9)
rule(s, 5.45)
body(s, "第三段和第七段是这七段里最难写、也最能区分人的两段。", 5.7, size=15, color=INK)
example(s, 6.2, "只有最终方案的作品集，看起来像一份说明书；"
                "带着被否决方案和迁移边界的作品集，才能看出你做过判断。",
        "别册三 §2.3", h=1.0)

# ══════════════════════════ 上机 ══════════════════════════

sec("上机 ③", "Lab-05 Evals 框架 ＋ CI 门禁",
    "故意引入一个劣化改动，CI 门禁必须阻断；恢复后必须放行。"
    "这是这五天里唯一一次有明确红绿时刻的上机。", "15:00–16:30 · 90 分钟")

# 任务书
s = sl("上机 ③", "任务书")
heading(s, "Lab-05 要跑出的那一红一绿")
table(s, ML, 1.95, [
    ["#", "任务", "验收判据"],
    ["①", "跑通三层评估集，看基线分数", "三层各出一个分数，门禁放行"],
    ["②", "注入劣化改动", "护栏被关闭 ＋ 检索结果被注入噪声 ＋ 一个历史故障被重新引入"],
    ["③", "重跑门禁", "必须阻断，且能说清是四条阻断条件里的哪几条触发的"],
    ["④", "恢复改动，重跑门禁", "必须放行"],
], [0.5, 5.4, 5.5], hl={3, 4}, rowh=0.62)
body(s, "环境：`labs/lab-05-evals/`，有 key 与无 key 双路径均已验证。", 4.65,
     size=14.5, w=10.9)
example(s, 5.25, "第 ③ 项的“能说清是哪几条触发的”是这次上机真正的验收点。"
                 "门禁红了不算通过——红了并且你能指着日志说出"
                 "“回归集第 N 条复发，零容忍，硬阻断”才算。",
        "下册附录 A；verify.sh 5/5", h=1.4)

# 环境准备
s = sl("上机 ③", "环境准备")
heading(s, "开跑之前")
bullets(s, 2.0, [
    ("双路径都验证过", "有 key 走真实模型路径，无 key 走离线打分路径。两条都已实测通过，"
                        "不需要为了这次上机去申请密钥。"),
    ("先看基线", "第 ① 项不要跳过。没有基线，第 ③ 项的红灯说明不了任何问题——"
                  "可能它本来就是红的。"),
    ("注入是三处，不是一处", "护栏、检索、回归各一处。分别注入、分别观察，"
                             "比三处一起注入更能看出门禁的结构。"),
], size=13.5, gap=12)
example(s, 5.0, "第三条是这次上机的设计意图：如果三处一起注入、门禁一次全红，"
                "你学到的只是“门禁能红”。分开注入，你才能看出四条阻断条件"
                "各自守的是什么。",
        "本课程据下册 §8.5 设计", h=1.35)
footer(s, "labs/lab-05-evals/，verify.sh 5/5，有 key 与无 key 双路径均已验证")

# 要看出什么
s = sl("上机 ③", "要看出什么")
heading(s, "三个注入各自触发的是不同的阻断条件")
table(s, ML, 1.95, [
    ["注入的改动", "该触发哪条阻断", "为什么"],
    ["护栏被关闭", "对抗集通过率", "对抗集测的是系统的下限，护栏是下限的实现"],
    ["检索结果被注入噪声", "检索指标相对回退 ＋ 黄金集综合分", "检索退化会同时打到两条线上"],
    ["一个历史故障被重新引入", "回归集通过率", "零容忍，不看比例——这是四条里唯一硬性的"],
], [3.0, 4.2, 4.2], hl=3, rowh=0.7)
body(s, "如果三个注入只触发了一条阻断，说明另外两条的阈值定得太松，或者压根没接上。",
     4.5, size=15, w=10.9, color=INK)
example(s, 5.15, "这也是这次上机想让你带走的判断：门禁不是一个开关，是四条独立的线。"
                 "四条线各自能被什么打红，比“门禁跑通了”重要得多。"
                 "回去接自己项目的门禁时，先问的是这个。",
        "本课程据下册 §8.5 设计", h=1.4)

# 面试题库
s = sl("M26", "2.1 题库")
heading(s, "八道系统设计题，每一道都能在这五天里找到出处")
table(s, ML, 1.95, [
    ["题", "考察点", "在这五天的哪一节"],
    ["端到端 300ms 内返回，数据在内网", "全链路延迟预算拆解", "今天 M25 案例四"],
    ["遗留系统只认 mTLS，云端只会 OAuth2", "协议桥接是“终结＋转换”不是透传", "第 3 天 M17"],
    ["Webhook 会重复推送、乱序、突发峰值", "幂等、削峰、顺序保证之间的取舍", "第 3 天 M17"],
    ["混合检索召回率上不去", "三分法诊断 ＋ 翻译成业务语言", "第 3 天 M18 ＋ 第 2 天 M7"],
    ["该用 RAG、微调还是长上下文", "三者并非互斥选项", "今天 M20"],
    ["多智能体死循环或超预算", "Agent 可靠性工程", "今天 M20"],
    ["完全断网，交付离线安装包", "供应链安全环节：签名与物料清单", "今天 M21"],
    ["上线 30 天采用率不到 20%", "区分技术、流程、政治三类根因", "第 2 天 M7"],
], [3.4, 4.0, 4.0], rowh=0.44)
example(s, 5.55, "这张表也是这五天的一次自检：八道题里有几道你现在能当场画出架构、"
                 "说清取舍？答不上来的那几道，回去看对应那一节。",
        "别册三 §2.1", h=1.15)

# 门禁落地
s = sl("上机 ③", "回去怎么落")
heading(s, "把门禁接进自己项目，按这个顺序")
steps(s, 2.1, [
    ("①", "先接回归集那一条", "它零容忍、判据最清楚、最不需要标定阈值。"
                               "而且它一定能立刻发挥作用——你手上一定有过历史故障。"),
    ("②", "再接检索指标相对回退", "用相对值而不是绝对阈值，绕开了“阈值定多少”这个难题。"),
    ("③", "然后接黄金集综合分", "这一条要先有黄金集，而黄金集要业务专家标注——"
                                 "所以它排第三，不是因为不重要。"),
    ("④", "最后接对抗集分级", "它需要对抗集先按风险场景分好类。"),
], h=2.6)
example(s, 5.05, "这个顺序的逻辑是“从判据最清楚的往判据最模糊的走”。"
                 "反过来先接黄金集，会卡在“阈值定多少”上，"
                 "然后整件事就搁置了——这是门禁最常见的死法。",
        "本课程据下册 §8.5 归纳", h=1.4)
footer(s, "四条线各自能被什么打红，比“门禁跑通了”重要得多")

# 收尾
s = sl("00", "收束")
heading(s, "今天留下三件东西")
for i, (k, t) in enumerate([
        ("一条判据", "有些事情不能用比例衡量。不可逆工具一次误触发就判失败，"
                      "历史故障复发零容忍——这两条是同一种判断。"),
        ("一次拆分", "“支持离线”要拆成签能不能离线、验能不能离线两问分别验证。"
                      "任何厂商的能力声明都可以这样拆。"),
        ("一张表", "延迟预算分解表。任何“用户在等一个实时结果”的场景，"
                    "先做这张表，再谈优化哪一段。")]):
    card(s, ML + i * 3.85, 2.2, 3.55, 2.4, "第 %d 件" % (i + 1), k, t)
rule(s, 5.0)
body(s, "证据和控制到今天为止都攒完了。明天是最后一天：拿四层骨架当尺子，"
        "去量两个真实的开源项目。", 5.25, size=15)
body(s, "明天不是“学一个工具”，是学怎么验收一个工具——"
        "以及判断它能不能进你的交付。", 5.95, size=15, color=INK)
footer(s, "《前线部署工程师（FDE）：把 AI 交付到真实世界》下册第 7–10、12–17 章")


if __name__ == "__main__":
    import pathlib
    out = pathlib.Path(__file__).resolve().parent / "第4天-护栏评估与国产化.pptx"
    n = D.save(str(out))
    print("已生成：%s（%d 页，16:9）" % (out, n))
