#!/usr/bin/env python3
"""第 3 天 · 接得上、数得清

听众：FDE
覆盖：下册第 1–5 章 ＋ §6.1–6.2
上机：Lab-04 混合检索 RAG ＋ 质量诊断

下册第 6 章在这里只讲前半（七要素落在哪几层、三条硬规矩的工程形态）；
后半（Semantica 参照实现）挪到第 5 天，和别册二并列成两个开源样本。
"""
from deck_kit import *   # noqa: F403

# 这一天用到书里的插图（build/figures.tex）。列在这里一次渲染完，
# 也是一份索引：这一天在哪几处直接用了书里的图。
figures.render(['fdmetabutton'])

D = Deck()
DAY = "第 3 天 · FDE 专场"


def sl(num=None, label=None, right=DAY, bg=GROUND):
    s = D.slide(bg)
    if label:
        eyebrow(s, num, label, right)
    return s


def sec(num, title, sub, kicker):
    s = D.slide(PANEL)
    section(s, num, title, sub, kicker)
    return s


def objectives(mod, title, items, foot=None):
    s = sl(mod, "本节目标")
    heading(s, title, size=30)
    body(s, "这一节结束时，你应该能做到下面这几件事。", 1.95, size=15)
    tf = tb(s, ML, 2.5, CW, 3.4)
    for i, it in enumerate(items):
        rich(tf, [("%d　" % (i + 1), {'color': VERIFY, 'bold': True,
                                      'mono': True, 'size': 14}),
                  (it, {})], size=15, color=INK2, first=(i == 0),
             spacing=1.45, space_before=0 if i == 0 else 13)
    if foot:
        footer(s, foot)
    return s


# ══════════════════════════ 开场 ══════════════════════════

s = D.slide(PANEL)
eyebrow(s, None, "FORWARD DEPLOYED ENGINEER · 五天实战课程")
tf = tb(s, ML, 1.75, 11.4, 2.4)
line(tf, "接得上、数得清：", 50, INK, bold=True, first=True, spacing=1.12)
line(tf, "从工程基线到本体落地", 50, INK, bold=True, spacing=1.12)
body(s, "第 3 天　上册讲判据，下册讲怎么用代码过。今天从生产级工程基线起步，"
        "走过规约驱动、企业集成、数据管道，落到七要素对应哪几层上。", 4.4, size=17, w=10.6)
rule(s, 5.5)
for i, (v, k) in enumerate([("5", "个模块"), ("4", "小时讲授"),
                            ("1.5", "小时上机"), ("7", "个要素落层")]):
    tf = tb(s, ML + i * 2.55, 5.75, 2.4, 0.9)
    line(tf, v, 30, INK, bold=True, mono=True, first=True)
    line(tf, k, 11, MUTED, space_before=2)
footer(s, "教材：下册第 1–5 章 ＋ §6.1–6.2")

# 上下册关系
s = sl("00", "承接")
heading(s, "上册回答每一关该怎么设，下册回答怎么用代码过")
table(s, ML, 1.95, [
    ["关卡", "上册（判据）", "下册（实现）", "今天讲哪个"],
    ["第 2 关 定界", "范围文档、可行性三角", "规约驱动开发；规格、变更与执行纪律", "M16"],
    ["第 3 关 切片", "薄切片、48 小时打法", "工程基线、企业集成、数据管道、本体", "M15 M17 M18 M19"],
    ["第 4 关 验收", "三层评估集、指标口径", "评估的工程实现", "第 4 天"],
    ["第 5 关 上线", "灰度、硬约束、回滚、交接", "护栏与可观测性、离线部署包", "第 4 天"],
], [2.0, 3.2, 4.4, 1.8], hl=2, rowh=0.58)
body(s, "两册各自完整，不互相依赖着读。今天这五个模块全部挂在第 2、3 关上——"
        "它们是同一件事的工程侧。", 4.6, size=15, w=10.9)
example(s, 5.3, "昨天下午讲第 3 关时说过：这一关攒的是证据的第二笔，"
                "而且是唯一一笔来自真实运行的。今天讲的全部内容，"
                "都是为了让那一笔证据能真的跑出来。",
        "上册第 6 章、下册第 1–6 章", h=1.25)

# 骨架定位
s = sl("00", "骨架定位")
heading(s, "今天五个模块，各自挂在骨架哪一层")
table(s, ML, 1.95, [
    ["模块", "骨架层", "在攒哪一类结果"],
    ["M15 生产级工程基线", "过程 · 第 3 关", "证据——代码本身要能被检验，否则跑出来的结果没人敢信"],
    ["M16 规约驱动开发", "过程 · 第 2 关", "证据——把“什么算对”从会议室搬进仓库"],
    ["M17 企业集成与协议桥接", "过程 · 第 3 关", "证据——接不上就没有真实运行记录"],
    ["M18 数据管道与向量层", "过程 · 第 3 关", "证据——语料质量决定了那批记录有没有意义"],
    ["M19 本体的工程实现", "资产 · 第 3 关与第 4 关之间", "把证据的形状定下来：哪个动作产生、谁能查"],
], [3.0, 2.9, 5.5], hl=5, rowh=0.56)
body(s, "五个模块全在攒证据。控制和接管要等第 4 天和之后。", 4.85, size=15,
     color=INK, w=10.9)
example(s, 5.4, "这也解释了为什么今天没有一节讲护栏或权限收口——"
                "那是第 5 关的事，攒的是控制。顺序被关卡锁死了：没有证据就上线，"
                "控制保护的是一个没人验证过的东西。",
        "上册 §2.0", h=1.35)

# 今日安排
s = sl("00", "今日安排")
heading(s, "今天六小时怎么走")
timetable(s, 2.05, [
    ["时段", "模块", "内容"],
    ["09:00–09:45", "M15", "生产级工程基线：四条硬线与依赖地狱"],
    ["09:45–10:30", "M16", "规约驱动开发 ＋ 规格、变更与执行纪律"],
    ["10:45–12:00", "M17", "企业集成与协议桥接：韧性四件套、认证、遗留系统"],
    ["13:00–14:00", "M18", "数据管道与向量层：防御式 ETL、分块、检索三分法"],
    ["14:00–14:30", "M19", "本体的工程实现：七要素落在哪几层"],
    ["14:45–16:15", "上机 ②", "Lab-04 混合检索 RAG ＋ 质量诊断"],
])
example(s, 5.85, "上机用的是 Lab-04：BM25 ＋ 向量 ＋ RRF ＋ Rerank 完整链路，"
                 "附检索质量三分法诊断脚本。已实测 7/7 通过，"
                 "混合检索 58/59 对纯向量 53/59。CPU 也能跑，不需要 GPU。",
        "下册附录 A；labs/lab-04-hybrid-rag/", h=1.3)

# 12 个 Lab
s = sl("00", "Lab 总览")
heading(s, "12 个 Lab，这五天会用到其中三个")
table(s, ML, 1.95, [
    ["#", "名称", "归属章", "状态", "本课程"],
    ["Lab-01", "脏 PDF 发票摄取管道", "下册第 5 章", "verify.sh 6/6", "今天讲，不上机"],
    ["Lab-02", "mTLS ＋ OAuth2 认证桥接", "下册第 4 章", "verify.sh 7/7", "今天讲，不上机"],
    ["Lab-03", "遗留 API 封装为 MCP Server", "下册第 7、13 章", "verify.sh 8/8", "明天讲"],
    ["Lab-04", "混合检索 RAG ＋ 质量诊断", "下册第 5、7、16 章", "verify.sh 7/7", "今天上机"],
    ["Lab-05", "企业 Evals 框架 ＋ CI 门禁", "下册第 9 章", "verify.sh 5/5", "明天上机"],
    ["Lab-11", "工作流发现演练", "上册第 4、5 章", "verify.sh 11/11", "第 1 天已上机"],
], [1.1, 3.9, 2.5, 2.0, 1.9], hl=4, rowh=0.5)
example(s, 5.4, "全部 12 个都已实现且验收脚本通过。剩下九个（离线部署包、可观测性、"
                "国产栈、对账智能体、毕业项目等）留给课后——第 4 天讲 90 天上岗计划时"
                "会给出建议顺序。",
        "下册附录 A", h=1.3)
footer(s, "依赖关系图与环境准备见下册附录 A §A.2、§A.3")

# 正文代码可不可以照抄
s = sl("00", "读法")
heading(s, "今天所有代码都带一行标注，先说清楚怎么读")
table(s, ML, 2.0, [
    ["标注", "含义", "能不能照抄"],
    ["取自 Lab-XX，verify.sh N/N 实测通过", "在这本书自己的环境里跑过，退出码为 0", "可以，但仍要按你的环境改配置"],
    ["逐字摘自官方文档", "原样引用，未改写", "可以，注意版本"],
    ["示意性骨架，未经实测验证", "写出来是为了说清结构，没跑过", "不要直接用于生产"],
], [3.6, 4.0, 3.8], hl=1, rowh=0.62)
quote(s, 4.35, "第三类占的比例不小。这不是偷懒——把没跑过的代码标成跑过的，"
               "比不给代码更糟。", size=15.5)
example(s, 5.5, "这条读法和第 1 天讲的可信度分层是同一件事：一手可验证／多源交叉／"
                "二手推测／笼统说法。今天换了个载体，判据没变——先问它是怎么来的。",
        "下册导读；上册 §1.2", h=1.3)

# ══════════════════════════ M15 工程基线 ══════════════════════════

sec("M15", "生产级工程基线",
    "在自己电脑上跑得好好的。这句话是整个下册的起点——"
    "探索代码和交付代码不是同一种东西，差别可以逐条列出来。",
    "09:00–09:45 · 下册第 1 章")

s = sl("＊", "本节金句")
maxim(s, "可复现先于聪明。",
      "重建不出来的效果不是效果，是一次演示。这一条排在下册第一章第一节，因为后面每一章的结论都要靠它才站得住。",
      "逐字", "下册 §1.0 章标题", size=40)

objectives("M15", "读完这一节你能做什么", [
    "拿一段探索期写的代码，逐条指出它离生产代码差哪六处。",
    "用四条硬线自检自己的代码，每条给出能机械判定的合规标准。",
    "在客户的半隔离网络环境里把依赖装上，而不是卡在 pip install。",
    "在合同签署之前，就把底座和现场层的物理边界画在仓库和构建里。",
], "下册第 1 章")

# 六处差异
s = sl("M15", "1.1 两种 Python")
heading(s, "同一个功能，两种工程文化，六处差异")
table(s, ML, 1.95, [
    ["#", "探索期写法", "生产写法", "对应的现场事故"],
    ["1", "路径硬编码在代码里", "用参数注入，路径由调用方决定", "机器换了"],
    ["2", "无类型标注，靠猜", "完整类型标注，调用处就报错", "字段类型变了"],
    ["3", "print() 糊在标准输出", "结构化日志，能按字段检索和告警", "出问题只能重跑一遍肉眼核对"],
    ["4", "隐式类型转换失败", "显式捕获并分类成语义清晰的异常", "脏数据来了"],
    ["5", "负数金额没人管", "记警告日志留痕，判断权交给下游", "退款订单出现了"],
    ["6", "I/O 和计算耦合在一段脚本里", "拆成两层，纯计算能单独测", "需要在 CI 里跑测试却没法造数据"],
], [0.5, 3.4, 4.3, 3.2], rowh=0.5)
example(s, 5.5, "第 5 条最能说明什么叫“生产写法”：负数可能是退款也可能是脏数据。"
                "探索版本直接汇总进总营业额，退款和正常消费混在一起没人发现。"
                "生产版本记警告日志留痕，把判断权交给下游——而不是替业务方悄悄做了决定。",
        "下册 §1.1", h=1.5)

# 六条不是风格洁癖
s = sl("M15", "1.1 为什么")
heading(s, "这六条不是风格洁癖，每条对应一次真实事故")
table(s, ML, 2.0, [
    ["这条没做", "事故长什么样"],
    ["路径硬编码", "机器换了。交付当天在客户的机器上跑不起来，现场改代码"],
    ["无类型标注", "字段类型变了。上游把金额从字符串改成数字，代码不报错，结果全错"],
    ["print 而非结构化日志", "出问题只能重跑一遍肉眼核对，而生产数据不一定还在"],
    ["隐式类型转换", "脏数据来了。悄悄产生空值混进后续计算，没有任何一处报警"],
    ["负数金额没人管", "退款订单出现了。退款和正常消费混在一起汇总，没人发现"],
    ["I/O 和计算耦合", "需要在 CI 里跑测试，却必须先往磁盘写一个真文件才能验证逻辑"],
], [3.4, 8.0], hl={4, 5}, rowh=0.5)
example(s, 5.55, "第四行和第五行是同一类：它们都不报错。第 2 天讲的静默失败率、"
                 "M18 要讲的语义漂移，说的也是这一类——"
                 "会报错的问题都不算最难的问题。",
        "下册 §1.1；上册 §8.5", h=1.3)

# 四条硬线
s = sl("M15", "1.2 四条硬线")
heading(s, "四条线各自独立，互不替代")
table(s, ML, 1.95, [
    ["硬线", "合规标准不是……", "而是"],
    ["类型标注", "有没有写 : str", "签名里还有没有裸露的 Any；能不能用严格模式跑过而不报错"],
    ["结构化日志", "有没有调用 logging 模块", "日志能不能被当结构化数据检索——把变量拼进一句人话里是反例"],
    ["显式错误处理", "有没有 try/except", "每个 except 分支要么记日志、要么重新抛出，不允许空 pass"],
    ["可测试性", "有没有写测试", "能不能在不改生产代码、不连任何外部资源的前提下跑通核心分支"],
], [1.8, 3.2, 6.4], rowh=0.66)
body(s, "有类型标注不代表有错误处理，能跑单测不代表日志可检索。四条要分开自检。",
     4.85, size=15, color=INK, w=10.9)
example(s, 5.4, "第二条的反例很具体：把订单号用字符串插值拼进一句人话里，"
                "记成“处理了订单 12345”。订单号变成了这句话的一部分，"
                "日志系统只能用正则去猜，没法按字段精确过滤或聚合。"
                "正确写法是把它作为一个独立字段传进去。",
        "下册 §1.2", h=1.35)

# except pass
s = sl("M15", "1.2 显式错误处理")
heading(s, "吞掉的异常等于把故障原因永久抹掉")
contrast(s, 2.0,
         ["except Exception: pass",
          "裸 except: pass",
          "在实验代码里很常见——反正一次失败不影响整体探索。"],
         ["记日志：留下这次失败的字段化证据。",
          "重新抛出：让上层知道发生了什么。",
          "两者都做：留痕并把决策权交给调用方。"],
         h=2.0, wtitle="生产代码里不允许", rtitle="每个 except 分支必须做的")
body(s, "但在生产代码里，吞掉的异常等于把故障原因永久抹掉。下次同样的问题出现时，"
        "排查者要从零开始。", 4.3, size=15, w=10.9, color=INK)
example(s, 4.95, "第 2 天讲的“静默失败率”，源头往往就在这里。"
                 "悄无声息的失败比排着长队更糟——而 except pass 正是最常见的一种"
                 "制造静默失败的写法。",
        "下册 §1.2；上册 §8.5", h=1.3)

# 可测试性
s = sl("M15", "1.2 可测试性")
heading(s, "把依赖注进去，而不是在函数体里去拿")
body(s, "合规标准很机械：单元测试能不能在不改动生产代码、不连接任何外部资源"
        "（数据库、API、文件系统）的前提下，跑通对应函数的核心分支。", 1.95, size=15, w=10.9)
contrast(s, 2.75,
         ["函数体里直接读全局配置。",
          "函数体里调用远程配置中心。",
          "测试之前得先起一个配置中心。"],
         ["风险阈值通过参数传进来。",
          "测试时传入一个常量就能跑。",
          "不需要真的连一次配置中心。"],
         h=1.9, wtitle="不可测", rtitle="可测")
example(s, 4.95, "这四条示例都刻意脱离了具体业务场景，是为了说明它们是可以独立检验的"
                 "通用标准，不依赖你在做哪个客户的哪个项目。真正棘手的不是记住这四条，"
                 "是在客户现场第一天，面对一张从没见过的表，判断数据本身到底能不能用——"
                 "那是下一节。",
        "下册 §1.2", h=1.55)

# SQL 诊断
s = sl("M15", "1.3 SQL 诊断")
heading(s, "现场第一天，面对一张没见过的表")
body(s, "五个 SQL 模板，别等业务方口头告诉你“这个字段很少为空”。", 1.95, size=15, color=INK)
table(s, ML, 2.5, [
    ["模板", "查什么", "为什么"],
    ["空值率", "对每个关键字段各算一次", "口头描述和实际分布经常对不上"],
    ["重复率", "先看总体，再钻到具体重复的主键上", "主键不唯一是最常见的“数据能用吗”一票否决项"],
    ["分布偏移", "按天摊平某个分类字段的占比，跟历史基线比", "能看出上游系统变更或字段口径漂移的信号"],
    ["外键孤儿", "子表里存在、父表里查不到的记录", "几乎每个“数据同步”或“分批迁移”场景都会有"],
    ["时间戳异常", "时序矛盾、未来时间戳、哨兵值时间戳", "三类各自指向不同的上游问题"],
], [1.6, 4.6, 5.2], hl=5, rowh=0.52)
example(s, 5.5, "时间戳三类异常各自的含义：更新时间早于创建时间，通常是 ETL 批次"
                "乱序写入或时区转换错误；创建时间晚于当前时间，通常是时区没转换；"
                "早于 1990 年这类明显的哨兵值，通常是某个默认值没被正确替换成真实业务时间。",
        "下册 §1.3", h=1.55)

# SQL 只摆事实
s = sl("M15", "1.3 SQL 诊断")
heading(s, "SQL 只负责把事实摆出来，不负责替你下结论")
body(s, "这五个模板不是终点，是起点。具体到哪个空值率、哪个重复率算“有问题”，"
        "取决于字段的业务含义。", 1.95, size=15, w=10.9)
contrast(s, 2.7,
         ["一个可选字段空值率 60%。",
          "——很正常。"],
         ["一个主键字段空值率 0.1%。",
          "——就该警惕。"],
         h=1.3, wtitle="同样是空值率", rtitle="判断标准完全不同")
body(s, "这个判断标准要靠跟业务方对话来确认。", 4.3, size=15.5, color=INK)
example(s, 4.85, "这一节其实是上册第 1 关那套现场调研 SOP 的技术侧：Day 4 见系统管理员，"
                 "拉最小三列事件日志。拉到之后先跑这五个模板，"
                 "再带着结果去 Day 5 找业务方核对——而不是先问“数据质量怎么样”。",
        "下册 §1.3；上册 §4.1", h=1.55)

# 依赖地狱
s = sl("M15", "1.4 依赖地狱")
heading(s, "客户环境的依赖地狱：三类半隔离场景")
table(s, ML, 1.95, [
    ["场景", "现象", "标准做法"],
    ["锁不住依赖树", "requirements.txt ＋ pip freeze 只锁顶层版本，锁不住依赖的依赖",
     "用带 lock 文件的工具，把整棵依赖树的精确版本和哈希都固定下来"],
    ["连不上公共源", "客户网络只让走白名单代理", "在能联网的机器上打包成 wheelhouse，把目录搬进隔离环境"],
    ["Python 版本对不上", "监管行业客户的“黄金镜像”可能锁在 3.8 甚至更早", "先核对主流栈的最低版本要求，再谈方案"],
    ["私有源配置", "所有安装要走内部镜像", "配 index-url／extra-index-url；自签证书环境用 trusted-host"],
], [1.9, 4.4, 5.1], rowh=0.72)
example(s, 5.15, "三个已核实的版本约束，说明“版本上移”不是危言耸听："
                 "MCP Python SDK 2.0.0 要求 >=3.10；某向量库客户端在 1.16.2 移除了 "
                 "3.9 支持；LangGraph v1 迁移指南原文写得很直接——所有包现在都要求 3.10 或更高。",
        "下册 §1.4", h=1.4)

# wheelhouse
s = sl("M15", "1.4 离线安装")
heading(s, "wheelhouse 打包：四个参数指定目标平台")
body(s, "官方文档给出的组合是 `--platform`、`--python-version`、`--implementation`、"
        "`--abi` 四个参数配合 `--only-binary=:all:`。", 1.95, size=15, w=10.9)
quote(s, 2.6, "关键是：用来精确指定目标机器的平台标签，而不是用当前机器的标签。", size=16)
body(s, "这一条最容易踩：在自己的 macOS 上打包，搬到客户的 Linux x86_64 上装不了——"
        "而报错信息通常只说“找不到匹配的发行版”，不会告诉你是平台标签的问题。",
     3.45, size=14.5, w=10.9)
rule(s, 4.3)
body(s, "这一节解决的是“能连内网但只让走白名单”和“能装但版本对不上”两类问题。",
     4.55, size=15, color=INK)
example(s, 5.1, "如果客户环境是完全物理隔离、连白名单代理都没有，"
                "wheelhouse 打包方式会被扩展成一整套离线交付包的核心机制。"
                "那正是昨天研讨的复盘一处理的场景——第 4 天讲离线部署包时会走完整流程。",
        "下册 §1.4、第 8 章；上册第 15 章", h=1.45)

# 底座与现场层
s = sl("M15", "1.5 物理边界")
heading(s, "底座与现场层的边界不在合同里画，在仓库和构建里画", size=29)
bullets(s, 2.15, [
    ("两个仓库，依赖方向单向", "现场层按版本号依赖底座的发布件，底座不许反过来引用现场层的任何东西。"),
    ("构建产物是两件，不是一件", "底座产出带版本号的依赖包或镜像层，现场层产出随项目交付的源码包。"),
    ("版本戳要能证明“先于项目”", "底座包的版本号要能对应到一个早于合同签署日的提交或 tag。"),
    ("许可声明随包走", "底座包里带 LICENSE/NOTICE，写清楚授权给谁、授权范围、能不能转授。"),
], size=13.5, gap=11)
quote(s, 4.85, "撤场评审会上，口头解释的证明力是零。", size=18)
example(s, 5.75, "第三条最容易被忽略，也最要命：如果底座是在项目做到一半时"
                 "从现场层里“抽”出来的，版本戳就证明不了它先于项目存在——"
                 "而“本项目产生的成果全部归甲方”这类条款，管的正是项目期内产生的东西。",
        "下册 §1.5；上册 §14.3", h=1.4)

# M15 清单
s = sl("M15", "本节清单")
heading(s, "M15 清单：交付一份代码之前")
checklist(s, 1.95, [
    "路径、阈值、配置都是参数注入的吗？还是有一条写死在代码里？",
    "严格模式的类型检查跑得过吗？签名里还有裸露的 Any 吗？",
    "日志能按字段检索吗？还是把变量拼进了一句人话里？",
    "每个 except 分支要么记日志、要么重新抛出了吗？有没有空 pass？",
    "单测能不能在不连任何外部资源的前提下跑通核心分支？",
    "依赖锁到整棵树了吗？目标平台的 wheelhouse 打过一次吗？",
    "底座和现场层是两个仓库、两份构建产物吗？底座的版本戳早于合同签署日吗？",
], size=13.5, gap=11)
footer(s, "下册第 1 章本章清单")

# ══════════════════════════ M16 规约驱动 ══════════════════════════

sec("M16", "规约驱动开发与工具选型",
    "第 9 天，AI 把生产库删了。上册第 2 关讲的范围文档，就是这一章讲的规格的前身——"
    "区别在载体：范围文档是一份会过期的文件，规格是仓库里能进 CI 的活文件。",
    "09:45–10:30 · 下册第 2、3 章")

s = sl("09", "本节金句")
maxim(s, "先定“做对了长什么样”，再动手做。",
      "规格不是文档，**是能被引用、被检查、能进 CI 的契约**。12 个 Lab 的 verify.sh 就是这句话的可执行形态。",
      "逐字", "下册 §3.9；契约定义见 §2.1", size=34)

objectives("M16", "读完这一节你能做什么", [
    "把一份口头需求写成一份能验收的规格，每条行为都有编号和验收方式。",
    "把团队规范固化成宪法，让它一次生效、每个功能薄薄一层。",
    "按改动大小分档，决定这次走几步流程。",
    "先判断缺哪一种能力，再去找工具——而不是按“哪个更先进”选。",
    "识别 OpenSpec 那两个进客户内网之前必须知道的坑。",
], "下册第 2、3 章")

# 平时怎么写
s = sl("M16", "2.1 规格")
heading(s, "规格不是文档，是能验收的契约")
table(s, ML, 2.0, [
    ["", "平时怎么写", "规约驱动怎么写"],
    ["起手", "想到哪写到哪，或者直接让 AI 写", "先写一份能验收的规格"],
    ["过程", "边写边改、口头对齐", "照规格生成 → 逐条对着验收"],
    ["结果", "演示很快，交付很难，没人接手", "可运行、可验收、可维护、可交接"],
], [1.4, 4.6, 5.4], hl=3, rowh=0.62)
body(s, "两个比喻能说清为什么需要它：", 4.15, size=15, color=INK)
bullets(s, 4.6, [
    ("大模型是一个读过海量代码的超级实习生", "见多识广、不知疲倦、一说就懂，但不知道你这里的规矩。"),
    ("上下文窗口是一张办公桌", "一次只摊得开这么多张纸。给得准、够，它就照做。"),
], size=14, gap=10)
example(s, 5.95, "上册第 5 章那份范围文档，和这一章的规格是同一件事，"
                 "从会议室搬进了仓库。搬进来之后它多了三样东西：能被检查、能被引用、能进持续集成。",
        "下册 §2.1；上册第 5 章", h=1.25)

# 六块地基
s = sl("M16", "2.2 六块地基")
heading(s, "写好规格的六块地基（这页四块，红线两块见下页）")
bullets(s, 2.0, [
    ("从用户视角写行为，不从实现视角写方案", "“作为设备管理员，我想要在借出时自动校验设备"
                                              "当前是否可借，以便同一台设备不会被重复借出。”"),
    ("每条行为都要能被验收", "写不出验收方式的行为，等于没写。这一条和上册第 2 关"
                             "“通过标准必须能用是／否回答”是同一条。"),
    ("明确写出不做什么", "“本期只查格式，不查重、不查学术不端。”"),
    ("给编号", "编号让规格能被引用、被追溯、被逐条核对。没有编号的规格没法做一致性核查。"),
], size=13.5, gap=11)
quote(s, 5.0, "第三条是防止 AI 超范围发挥最有效的一条。", size=17)
example(s, 5.9, "规格里不写“不做什么”，模型会按它读过的海量代码里的常见做法"
                "自行补上一堆你没要的东西——而且补得很合理，很难一眼看出来。",
        "下册 §2.2", h=1.15)

# 不变量与退路
s = sl("M16", "2.2 六块地基")
heading(s, "第五、六块：用户故事那套写法漏掉的两样")
table(s, ML, 1.95, [
    ["", "写什么", "缺了会怎样"],
    ["⑤ 不变量", "任何时候都不允许发生什么", "本章开头那件事：AI 把生产库删了"],
    ["⑥ 退路", "谁批准、怎么撤、撤不回来的怎么办", "出事之后只能现场想办法"],
], [1.9, 4.4, 5.1], hl={1}, rowh=0.62)
body(s, "④ 范围边界说的是“这一期不做”，下一期可能做；⑤ 不变量说的是"
        "“什么时候都不许”。把红线写进范围边界，扩范围时它会跟着一起被解开。", 3.75)
quote(s, 4.6, "写不变量的检验办法：把它交给一个完全按字面执行、不会揣摩意图的"
              "执行者，你敢不敢让它跑。", size=16)
example(s, 5.6, "每一条不变量都要指名到具体动作（DROP、无 WHERE 的 DELETE、"
                "出站地址、写日志的字段），不能写“不得进行危险操作”——"
                "“危险”是判断，判断没法执行；DROP 是动作，动作能拦。",
        "下册 §2.2；落到护栏上见 §8", h=1.25)
footer(s, "前四块整理自交大高金内训材料，⑤⑥ 两块是本书增补")

# 宪法
s = sl("M16", "2.3 宪法")
heading(s, "把团队规范固化成机器能读的约束")
table(s, ML, 1.95, [
    ["类别", "写进宪法的内容举例"],
    ["技术栈", "允许用什么、生产禁用什么"],
    ["数据模型", "每张表必须含创建时间、创建人、更新时间、更新人四个审计字段"],
    ["接口契约", "统一响应体结构与错误码；受保护接口必须校验会话与权限"],
    ["安全", "密钥只从环境变量读，绝不硬编码；敏感数据脱敏"],
    ["成本", "单次最大 token 数、单会话轮次上限、会话超时、日限额"],
], [1.6, 9.8], hl={4, 5}, rowh=0.52)
quote(s, 4.85, "重的写一次，每个功能薄薄一层。这是规约驱动不变成瀑布的第一条解法。",
      size=16)
example(s, 5.75, "“数据模型”那一行直接对应第 2 天讲的七要素第七项——证据。"
                 "四个审计字段是最低配的“执行后留下什么可审计结果”。"
                 "把它写进宪法，等于每张新表都自动带上这一层。",
        "下册 §2.3；上册 §7.3", h=1.35)

# 命令流水线
s = sl("M16", "2.4 命令流水线")
heading(s, "每条命令落一份产物")
table(s, ML, 1.95, [
    ["命令", "干什么", "产物"],
    ["/constitution", "立项目约法三章：技术栈、代码规矩、红线", "宪法文件"],
    ["/specify", "把大白话需求变成编号、可测的规格", "规格文件"],
    ["/clarify", "写第一行代码前，把歧义一个个问死，答案写回规格", "回填规格"],
    ["/plan", "依规格出技术方案四件套", "方案、数据模型、接口契约、验收剧本"],
    ["/tasks", "拆成可执行、可验收的任务（纵向切片）", "任务清单"],
    ["/implement", "照设计建库、搭骨架、写实现", "代码与制品"],
    ["/converge", "收敛，跑通端到端门禁", "门禁结果"],
], [2.2, 6.0, 3.2], hl=3, rowh=0.46)
example(s, 5.55, "为什么 /clarify 必须在 /plan 之前：规格最大的问题是“欠规格”——"
                 "看起来写完了，实际上留了一堆没人问的歧义。"
                 "歧义不在写代码之前问死，就会在写代码的过程中被 AI 自行猜一个答案填上。",
        "下册 §2.4", h=1.4)

# 纵向切片
s = sl("M16", "2.4 纵向切片")
heading(s, "任务要纵向切片，不要横向分层")
contrast(s, 2.0,
         ["先做完所有数据模型。",
          "再做完所有接口。",
          "再做完所有界面。",
          "做完第一层之前，拿不出任何能验的东西。"],
         ["一个任务穿过数据、接口、界面三层。",
          "做完就能验收一条完整行为。",
          "每个任务 2–5 分钟到半天不等。",
          "风险在第一个任务就暴露。"],
         h=2.4, wtitle="横向分层", rtitle="纵向切片")
example(s, 4.75, "这和上册第 3 关那条“竖着切，不是分层搭”是同一条判据，"
                 "换到了任务拆解这一层。同一件事在这五天里会出现四次："
                 "薄切片、行走骨架、纵向切片、以及第 4 天讲评估时的分层评估集。",
        "下册 §2.4；上册 §6.1", h=1.45)
quote(s, 6.4, "记住这条判据的最短形式：每一刀都要穿透到底。", size=14)

# 分档
s = sl("M16", "2.6 分档")
heading(s, "按改动大小分档——这是最高频的实操问题")
table(s, ML, 2.0, [
    ["场景", "建议流程"],
    ["小功能", "复用宪法 → 轻规格 → 原型 → 薄方案 → 任务 → 实现；跳过澄清、核对、门禁"],
    ["有歧义", "上面加回澄清"],
    ["多人 / 高风险", "再加回一致性核对和端到端门禁"],
], [2.4, 9.0], rowh=0.6)
body(s, "不分档的直接后果是团队抵触：小改动也要写一堆文档，第三次之后就没人愿意走流程了。",
     4.2, size=15, w=10.9, color=INK)
example(s, 4.85, "还有一条同样高频：跑完 /implement 不等于做完。"
                 "生成出来的代码要逐条对着规格验收——规格里写了几条可测行为，"
                 "就要有几条验证记录。这一步跳过了，规约驱动就退化成了"
                 "“用更长的提示词让 AI 写代码”。",
        "下册 §2.6、§2.7", h=1.45)

# 会不会变成瀑布
s = sl("M16", "2.5 瀑布")
heading(s, "两个最常被提出的痛点")
card(s, ML, 2.0, 5.55, 2.1, "痛点一", "界面看得太晚",
     "业务方对不着屏幕提意见。解法是把可点击原型提到方案之前——"
     "消歧之后先出原型，让业务方早看到界面，而不是等实现完了才第一次看到。")
card(s, ML + 5.85, 2.0, 5.55, 2.1, "痛点二", "流程太重，像瀑布",
     "反复写文档。解法是分档：重的（宪法）写一次，每个功能薄薄一层；"
     "小功能跳过澄清、一致性核对和端到端门禁。", accent=VERIFY)
body(s, "两条解法的共同点：不是砍掉环节，是把环节按改动大小重新分配。", 4.5,
     size=15, w=10.9, color=INK)
example(s, 5.1, "这和上册第 3 关那条“过早抽象和过晚抽象都是浪费”是同一种判断："
                "流程本身也是一种抽象，对着一个改三行代码的需求走完整流水线，"
                "就是过早抽象。",
        "下册 §2.5；上册 §6.4", h=1.35)

# 跑完不等于做完
s = sl("M16", "2.7 验收")
heading(s, "跑完实现命令不等于做完")
body(s, "生成出来的代码要逐条对着规格验收——规格里写了几条可测行为，"
        "就要有几条验证记录。", 2.0, size=16, color=INK, w=10.9)
contrast(s, 2.7,
         ["“跑完了，看起来没问题。”",
          "“功能都实现了。”",
          "把规格当成写给 AI 的提示词。"],
         ["规格第 3 条：输入 X，期望 Y —— 实测 Y，通过。",
          "规格第 7 条：不做重复检测 —— 检查未越界，通过。",
          "把规格当成验收清单，逐条打勾。"],
         h=1.95, wtitle="退化成“用更长的提示词写代码”", rtitle="规约驱动")
example(s, 4.9, "这一步跳过了，规约驱动就退化成了“用更长的提示词让 AI 写代码”——"
                "写规格的成本付了，验收的收益一分没拿。",
        "下册 §2.7", h=1.2)
quote(s, 6.3, "规格的价值在验收那一刻兑现，不在写下来那一刻。", size=15)

# 三套工具
s = sl("M16", "3.1 三种能力")
heading(s, "三者解决的不是同一个问题")
table(s, ML, 1.95, [
    ["", "Spec Kit", "OpenSpec", "Superpowers"],
    ["定位", "规格流水线", "变更提案制", "工程纪律层"],
    ["回答什么问题", "这个新功能要做成什么样", "这次改动改了系统的哪些行为", "AI 干活时按什么规矩来"],
    ["适合什么时候", "从零造功能、团队要统一产物格式", "已有系统上做增量改动、要留变更轨迹", "想约束 AI 的干活方式"],
    ["重量", "重", "轻", "轻，但强制"],
], [1.8, 3.2, 3.5, 2.9], rowh=0.72)
body(s, "它们不是三选一，是三个不同的层。可以同时用。", 4.85, size=15, color=INK)
example(s, 5.4, "按症状选：需求说不清、做出来不是想要的 → 缺规格；"
                "规格写了但和代码对不上、没人敢信 → 缺变更轨迹；"
                "小改动也要写一堆文档、团队开始抵触 → 缺的是分档规矩，工具不用换。",
        "下册 §3.2、§3.6", h=1.4)

# Spec Kit
s = sl("M16", "3.2 Spec Kit")
heading(s, "Spec Kit：从零造功能的流水线")
body(s, "它的价值在于把“想清楚”这件事拆成了有产物的步骤。每一步都落一份文件，"
        "下一步只能基于上一步的产物往下走——顺序倒过来写，工具不认。", 1.95, size=15, w=10.9)
steps(s, 2.85, [
    ("①", "澄清在方案之前", "歧义不在写代码之前问死，就会被 AI 自行猜一个答案填上。"),
    ("②", "原型在方案之前", "消歧之后再搭原型，原型才建立在去歧义的规格上。"),
    ("③", "任务纵向切片", "每个任务穿过数据、接口、界面三层，做完就能验收一条完整行为。"),
], h=1.7)
body(s, "重量是它的代价：对一个改三行代码的需求，走完整流水线的成本远高于收益。",
     4.85, size=14.5, w=10.9)
example(s, 5.45, "所以选它的判据不是“它好不好”，是“这次改动值不值得走这套流程”。"
                 "这正是分档表要解决的问题——工具不用换，换的是走几步。",
        "下册 §3.3、§2.6", h=1.25)

# OpenSpec
s = sl("M16", "3.3 OpenSpec")
heading(s, "现状规格不是手写的，是归档时从增量合并出来的")
body(s, "`openspec init` 只创建配置文件；现状规格目录要等第一次归档才被建出来。"
        "这不是 bug，是它的设计。", 1.95, size=15, w=10.9)
table(s, ML, 2.7, [
    ["增量操作", "含义", "约束"],
    ["ADDED Requirements", "新增的行为", "—"],
    ["MODIFIED Requirements", "改掉的行为", "必须写出改完后的完整内容，不是只写差异"],
    ["REMOVED Requirements", "废弃的行为", "必须写 Reason 与 Migration"],
    ["RENAMED Requirements", "只改名字", "用 FROM:/TO: 格式"],
], [3.4, 2.6, 5.4], rowh=0.5)
example(s, 5.35, "这一招直接治“规格与代码漂移”。漂移的根源是回写规格的成本太高——"
                 "把回写做成归档流程的一部分，是“规格永远是最新的”这件事"
                 "第一次有了机械保证，而不是靠人自觉。",
        "下册 §3.4", h=1.35)

# OpenSpec 两个坑
s = sl("M16", "3.3 两个坑")
heading(s, "两条实测踩出来的坑，进客户内网之前必须知道")
card(s, ML, 2.0, 11.4, 1.35, "坑一 · 场景必须正好四个井号", None,
     "用三个井号或者写成列表项，它不会报错，会静默失败——校验通过，但那条场景不算数。"
     "官方文档把这一条标成 CRITICAL，本书实测确认。", accent=WRONG, bsize=14.5)
card(s, ML, 3.55, 11.4, 1.45, "坑二 · 它默认上报匿名使用统计", None,
     "关掉的方式是环境变量 OPENSPEC_TELEMETRY=0，或者用配置命令关闭。"
     "进客户内网之前先关掉，并且写进你的软件物料清单。", accent=WRONG, bsize=14.5)
example(s, 5.3, "“这个工具会不会往外发数据”是安全评审必问的一条。"
                "答“我没注意”和答“默认会，我们关了，关法如下”是两种结果。"
                "第 5 天讲怎么评估一个开源项目时，这一条会变成一张正式的评估表。",
        "下册 §3.4；实测于 1.10.0，Node 22", h=1.45)

# Superpowers
s = sl("M16", "3.4 Superpowers")
heading(s, "第三层：给 AI 定规矩，跟用不用规格无关")
table(s, ML, 1.95, [
    ["技能", "强制什么"],
    ["brainstorming", "动手之前先把设计问清楚，分段确认，不许直接开写"],
    ["writing-plans", "把工作拆成 2–5 分钟一个的任务，带确切文件路径和验证步骤"],
    ["using-git-worktrees", "在隔离的工作区和新分支上干活，先跑一遍测试建基线"],
    ["test-driven-development", "红 → 绿 → 重构：先写会失败的测试，再写最少的代码让它过"],
    ["systematic-debugging", "四阶段根因分析，不许“试试看这样改行不行”"],
    ["verification-before-completion", "说“做完了”之前必须拿出验证过的证据"],
], [3.6, 7.8], hl={5, 6}, rowh=0.5)
body(s, "这些技能是必须触发的闸口，不是可选建议。", 5.15, size=15, color=INK)
example(s, 5.7, "最后一条是这几天反复出现的同一条纪律的第四种形式："
                "第 1 关的“受访者当面复述过”、第 3 关的“客户方能自己现场复现”、"
                "第 6 关的“客户团队能完成一次常见变更”，"
                "以及这里的“拿出验证过的证据”。",
        "下册 §3.5", h=1.35)

# 宪法是共享契约
s = sl("M16", "3.7 共享契约")
heading(s, "宪法是共享契约，不是某个人的偏好")
body(s, "一份写在仓库里、进了版本控制的宪法，和一份写在某个人脑子里的编码习惯，"
        "区别在于前者能被评审、被引用、被追责。", 1.95, size=15, w=10.9)
contrast(s, 2.75,
         ["“我们团队一般都这么写。”",
          "新人问一次，老人答一次。",
          "改了没人知道，也没人反对得了。"],
         ["宪法进版本库，改动走 PR。",
          "同事对照宪法评审后合并。",
          "改一条规矩要有人同意——这就是契约。"],
         h=2.0, wtitle="口头规范", rtitle="共享契约")
example(s, 5.1, "这一条对交付特别重要：撤场的时候，宪法是少数几样"
                "“客户自己看得懂、也改得动”的产物之一。"
                "它和最小可执行业务模型一样，属于第 6 关那句"
                "“客户团队能在你离场之后完成一次常见变更”的前提条件。",
        "下册 §3.8；上册 §2.0、§10.4", h=1.45)

# 和 12 个 Lab 的关系
s = sl("M16", "3.8 和 Lab 的关系")
heading(s, "这套东西和这本书的 12 个实验是同一件事")
body(s, "每个 Lab 都有一份 verify.sh——它就是那个 Lab 的规格的可执行形式。"
        "跑通不是“看起来能用”，是退出码为 0。", 1.95, size=15, w=10.9)
table(s, ML, 2.75, [
    ["规约驱动里的概念", "在 Lab 里的对应物"],
    ["规格：可测的行为列表", "README 里的验收标准，逐条可判定"],
    ["门禁：端到端跑通才算过", "verify.sh，退出码为 0 才算过"],
    ["宪法：团队约束", "各 Lab 共享的环境要求与密钥规范——密钥只从环境变量读"],
], [4.4, 7.0], rowh=0.56)
example(s, 4.85, "这一条也解释了这本书自己的写法纪律：正文里标着"
                 "“verify.sh N/N 实测通过”的代码，是真的跑过；"
                 "标着“示意性骨架”的，是没跑过。两者在书里从不混着写。",
        "下册 §3.9", h=1.35)
quote(s, 6.4, "规格不落成一个能跑出退出码的东西，就还是一份会过期的文档。", size=14)

# Git 协作规范
s = sl("M16", "3.6 协作")
heading(s, "多人协作时，规格和分支要对上")
body(s, "一个人用规约驱动是自律，多个人用才需要协作规范。核心是让“规格改了”"
        "这件事和“代码改了”走同一条评审路径。", 1.95, size=15, w=10.9)
steps(s, 2.75, [
    ("①", "写规格 / 改代码", "在本地跑通，规格和实现一起改。"),
    ("②", "开 PR 指向开发分支", "规格的改动和代码的改动在同一个 PR 里，一起评审。"),
    ("③", "对照宪法评审后合并", "评审的依据是宪法，不是评审者的个人偏好。"),
], h=1.7)
body(s, "规格单独改、代码单独改，就会出现“规格说要做 A，代码做了 B，"
        "两个 PR 都通过了评审”这种谁都没错的情况。", 4.7, size=14.5, w=10.9)
example(s, 5.45, "这一条和 OpenSpec 的归档机制是同一个目标的两条路："
                 "一条靠流程约束（同一个 PR），一条靠工具约束（归档时才合并回现状规格）。"
                 "两条都能用，但必须选一条——两条都不用，规格和代码一定会漂移。",
        "下册 §3.7、§3.4", h=1.45)

# M16 清单
s = sl("M16", "本节清单")
heading(s, "M16 清单：这一节要带走的动作")
checklist(s, 1.95, [
    "手上项目有宪法吗？技术栈、数据模型审计字段、密钥规范、成本上限写了吗？",
    "最近一份规格里，有没有明确写出“不做什么”？",
    "规格的每条行为都有编号和验收方式吗？",
    "分档表定了吗？小功能走几步、有歧义加哪一步、高风险再加哪一步？",
    "跑完实现之后，有没有逐条对着规格打勾的验证记录？",
    "如果用 OpenSpec：场景是四个井号吗？遥测关了吗？写进物料清单了吗？",
], size=14, gap=12)
example(s, 6.05, "最后一条的第二问是安全评审必问的一条，也是第 5 天那张"
                 "《开源组件进场评估表》的第一行。",
        "下册第 2、3 章本章清单", h=1.0)

# ══════════════════════════ M17 企业集成 ══════════════════════════

sec("M17", "企业集成与协议桥接",
    "同一笔款，扣了两次。这一章讲的全部内容，都是为了让这句话不发生——"
    "以及发生之后能查清楚是哪一次。", "10:45–12:00 · 下册第 4 章")

objectives("M17", "读完这一节你能做什么", [
    "设计一个幂等键，并说清它为什么必须拦“正在处理中”的并发重试。",
    "在四种抖动策略里选一种，并说出无抖动方案为什么是明显的输家。",
    "把端到端超时预算往下分，而不是每一跳各定各的。",
    "在四种可用的 OAuth2 授权方式里选对一种，并识别出已被弃用的两种。",
    "按 DNS、TCP、TLS、应用层认证四层顺序排查“连不上”。",
    "在五种遗留系统对接方式里选一种，并说出它的典型故障模式。",
], "下册第 4 章")

# 二十一次（下册第 4 章开头第二个场景）
s = sl("M17", "4.0 开场")
heading(s, "同一件事，做了二十一次")
body(s, "一家公司的财务总监——不是工程师——用 AI 编程工具，两天把一套 SaaS 送上生产。"
        "后来工程师翻账单，发现某一天的曲线直冲云霄：光那一天的 AI 花费，"
        "就超过整个服务器机房跑一整月。", 1.9, size=14.5, w=10.9)
steps(s, 2.72, [
    ("①", "调大模型", "返回 200，成功。所以这一次照常计费。"),
    ("②", "写数据库", "代码引用了一个本该建好、实际还没建的字段，报“列不存在”。"),
    ("③", "整个任务失败", "前面全成功，只有最后一步倒下，批处理被判失败。"),
    ("④", "自动重试", "回到 ①。同一个租户，同样跑完，同样计费，同样倒下。"),
], h=1.75)
body(s, "原文把成因拆成三个相乘的因子：确定性失败 × 自动重试 × 非幂等。"
        "前两个单独都不致命——是第三个把前两个连成了一台碎钞机。", 4.68, size=14.5,
     w=10.9)
example(s, 5.32, "古法编程时代有它的对应物：一个没人注意的死循环，一个忘了关的数据库连接。"
                 "它们都有天花板，撞到顶就崩，崩了你就知道了——那个“撞到顶”其实是"
                 "这类故障的免费报警器。按调用计费的外部模型没有天花板：它不崩，"
                 "它一直成功，一直计费。",
        "下册 §4.0；junueno.dev 2026-06-29 复盘，单一来源，本书采信机制不采信金额",
        h=1.45)

# 韧性四件套
s = sl("M17", "4.1 韧性四件套")
heading(s, "四件套各管一段，缺一件链条就断")
steps(s, 2.1, [
    ("①", "幂等性", "决定重试是否安全。没有它，后面三件都不能用。"),
    ("②", "退避", "决定重试的节奏。固定间隔会造成惊群，把刚喘过气的服务重新打垮。"),
    ("③", "超时预算", "决定什么时候放弃。端到端预算要往下分，不是每一跳各定各的。"),
    ("④", "熔断与降级", "决定放弃之后系统怎么继续活下去。"),
], h=2.1)
body(s, "顺序不能乱：没有幂等就不该重试，没有退避的重试是压垮下游的最后一根稻草，"
        "没有超时预算的退避会让上游的截止时间检查形同虚设。", 4.5, size=14.5, w=10.9)
example(s, 5.3, "这一节的每一条都对应第 2 天讲的“撤不回来的后果”那张表。"
                "扣款是典型的“已经写回业务系统的数据”——难撤，要有补偿动作，"
                "而补偿本身还会留痕。所以最省事的做法是让它一开始就不会重复执行。",
        "下册 §4.1；上册 §9.4", h=1.4)

# 幂等
s = sl("M17", "4.1 幂等性")
heading(s, "唯一安全的动作是用同一个幂等键重试")
body(s, "客户端发起一次扣款请求，网络在响应返回前断开。客户端不知道服务端到底有没有"
        "处理成功。这时候唯一安全的动作是用同一个幂等键重试，"
        "而不是猜一下再决定要不要重试。", 1.95, size=15, w=10.9)
card(s, ML, 3.0, 11.4, 1.4, "最容易被漏掉的一处", None,
     "如果不拦截“正在处理中”的并发重试，两个几乎同时到达的重试请求会同时穿透缓存、"
     "同时执行两遍业务逻辑，幂等性照样被破坏。", accent=WRONG, bsize=14.5)
example(s, 4.7, "Webhook 接收端复用的是同一套机制：去重键应该用事件自带的唯一 ID，"
                "不要对整个请求体做哈希。同一个事件如果供应商在重试时改了个别元数据字段"
                "（比如重试次数计数器），内容哈希会不一致，但事件 ID 不会变。",
        "下册 §4.1、§4.2", h=1.45)
quote(s, 6.35, "幂等性是这四件套的地基。它不成立，后面三件都是在加速一次重复扣款。",
      size=14)

# 幂等键三条规则
s = sl("M17", "4.1 幂等键")
heading(s, "幂等键设计的三条容易被忽视的规则")
bullets(s, 2.0, [
    ("键由调用方生成，不由服务端生成", "服务端生成的键在“请求没到达”这种情况下根本不存在，"
                                        "而那正是最需要幂等的场景。"),
    ("键要覆盖业务语义，不只覆盖请求内容", "同一个用户对同一笔订单的两次不同扣款，"
                                            "请求体可能完全一样，但它们是两次业务动作。"),
    ("键要有明确的有效期", "永久保留意味着存储无限增长；太短意味着一次长时间的网络分区之后，"
                            "重试会被当成新请求。"),
], size=13.5, gap=12)
rule(s, 5.0)
body(s, "三条都对了，还有一处最容易漏：拦住“正在处理中”状态的并发重试。",
     5.25, size=15, color=INK)
example(s, 5.8, "有效期怎么定，没有标准答案，但有一条判据：它要长于"
                "“调用方可能重试的最长时间窗口”。这个窗口由退避策略的上限决定——"
                "所以幂等键的有效期和退避配置必须一起定，不能分开拍。",
        "下册 §4.1", h=1.3)

# 退避
s = sl("M17", "4.1 退避")
heading(s, "三种抖动策略，以及那个明显的输家")
table(s, ML, 1.95, [
    ["策略", "怎么算", "实测结论"],
    ["无抖动", "退避时间就是理论值", "调用次数最多、耗时最长——明显的输家"],
    ["Full Jitter", "退避时间在 0 到理论值之间完全随机", "更省客户端工作量"],
    ["Equal Jitter", "一半固定退避 ＋ 一半随机，避免被抖成 0", "调用次数略高于 Full Jitter 且耗时更长"],
    ["Decorrelated Jitter", "新的抖动上限依赖上一次的随机值", "耗时略短；和 Full Jitter 的取舍并不十分明确"],
], [2.4, 4.2, 4.8], hl=1, rowh=0.6)
body(s, "固定间隔重试的致命问题：下游短暂过载时，所有客户端会在同一个时间点扎堆重试，"
        "形成惊群，把刚刚喘过气的服务重新打垮。", 4.55, size=14.5, w=10.9)
example(s, 5.35, "还有一个容易被忽略的边界：只重试“瞬时性”错误，不能重试所有异常。"
                 "把参数错误、认证失败这类“重试了也不会变好”的错误一起塞进重试循环，"
                 "只会白白拖长故障恢复时间，还可能因为不断打向一个明确拒绝你的端点，"
                 "触发对方的限流封禁。",
        "下册 §4.1", h=1.5)

# 超时预算
s = sl("M17", "4.1 超时预算")
heading(s, "端到端预算要往下分，不是每一跳各定各的")
body(s, "一次用户请求如果承诺 5 秒内响应，这 5 秒要在调用链路的每一跳之间分配。"
        "不要让网关、认证服务、遗留系统适配层、数据库各自独立设置“看起来合理”的超时值。",
     1.95, size=15, w=10.9)
quote(s, 2.85, "下游的重试次数要受剩余预算约束，而不是固定重试 N 次。", size=17)
body(s, "如果一跳内部还在做指数退避重试，退避时间必须和剩余预算比对。"
        "预算耗尽就必须放弃重试、直接失败返回——不能让退避逻辑“礼貌地”等满固定的重试轮数。",
     3.7, size=14.5, w=10.9)
example(s, 4.6, "这条和第 4 天讲的 300 毫秒生命线是同一件事的两个尺度："
                "那个案例把 300 毫秒拆成网关 10、语义缓存 5、混合检索 40、"
                "重排 30、生成 170、序列化 15，再留 15 毫秒缓冲给抖动。"
                "预算表本身就是架构决策的输出。",
        "下册 §4.1、§16.4", h=1.55)

# 熔断
s = sl("M17", "4.1 熔断")
heading(s, "三态状态机，以及降级的三条路")
steps(s, 2.05, [
    ("Closed", "闭合", "正常放行请求，同时统计失败率；失败率超阈值时跳转到 Open。"),
    ("Open", "打开", "直接拒绝所有请求，不再打向下游——这是保护下游、给它喘息时间的关键动作。"),
    ("Half-Open", "半开", "放行少量试探请求；成功回到 Closed，失败退回 Open 并重新计时。"),
], h=1.9)
body(s, "降级的三条路，按代价从低到高：", 4.3, size=15, color=INK)
bullets(s, 4.75, [
    "返回最近一次成功的缓存结果。代价最低，前提是业务能接受数据略微陈旧。",
    "切换到功能更简单的备用路径，比如放弃个性化推荐，返回通用兜底列表。",
    "把请求转成异步排队而不是同步失败。用户体验从“立刻报错”变成“稍后处理”。",
], size=13.5, gap=9)
example(s, 6.3, "第三条适合本来就不要求实时的操作。选哪条不是技术判断，"
                "是业务判断——所以它属于第 2 关定界的内容。",
        "下册 §4.1", h=1.0)

# Webhook
s = sl("M17", "4.2 Webhook")
heading(s, "重放防御要签名 ＋ 时间戳，缺一个都不够")
body(s, "只做签名校验，防不住“重放一条几天前被截获的合法请求”。"
        "签名本身没有过期概念，一条真实签发过的请求可以被攻击者原样重放，"
        "签名照样能验证通过。", 1.95, size=15, w=10.9)
bullets(s, 2.85, [
    "验证签名。",
    "验证时间戳与当前时间的差值是否在一个短窗口内——常见做法是 5 分钟量级。",
], size=14.5, gap=10)
rule(s, 3.85)
body(s, "乱序处理：不能假设网络会按发送顺序投递。防御手段是让每条事件携带一个"
        "单调递增的版本号，处理前先比对——新事件的版本号必须大于当前已记录的才允许覆盖。",
     4.1, size=14.5, w=10.9)
example(s, 4.95, "一个容易被忽略但很关键的设计：乱序事件和重复事件都要返回 200，"
                 "而不是错误状态码。多数供应商把非 2xx 响应当作“投递失败”信号，会触发重试。"
                 "对一条“已处理”或“过期”的事件返回 4xx/5xx，只会引来没完没了地重投"
                 "一条注定不会被处理的旧事件。",
        "下册 §4.2", h=1.6)

# Webhook 接收器
s = sl("M17", "4.2 接收器")
heading(s, "高可用 Webhook 接收器的四道处理")
steps(s, 2.1, [
    ("①", "验签 ＋ 校时间戳", "两者缺一不可。只验签防不住重放一条几天前被截获的合法请求。"),
    ("②", "按事件 ID 去重", "不要对整个请求体做哈希——供应商重试时改了元数据字段，"
                             "内容哈希就不一致了。"),
    ("③", "比对版本号", "新事件的版本号必须大于已记录的才允许覆盖，否则丢弃。"),
    ("④", "立刻返回 200，业务转异步", "接收和处理分开。同步处理会撞上供应商的响应超时。"),
], h=2.25)
body(s, "第 ④ 步的意思是：接收端的职责只有“确认收到并落盘”，业务处理进队列。",
     4.65, size=15, w=10.9, color=INK)
example(s, 5.25, "这四道处理里有三道会“丢弃”事件——重复的、过期的、乱序的。"
                 "它们全部要返回 200 而不是错误码，否则供应商会没完没了地重投"
                 "一条注定不会被处理的事件。",
        "下册 §4.2", h=1.35)

# OAuth2
s = sl("M17", "4.3 认证桥接")
heading(s, "四种可用的授权方式，两种已被移出规范")
table(s, ML, 1.95, [
    ["Grant", "适用场景", "关键约束"],
    ["Authorization Code ＋ PKCE", "有用户参与、需要用户授权同意的场景",
     "OAuth 2.1 草案要求所有客户端类型都必须使用 PKCE，不再仅限公共客户端"],
    ["Client Credentials", "机器对机器，没有终端用户参与", "只涉及两方，不涉及资源所有者"],
    ["Device Authorization", "无浏览器或输入受限的设备", "客户端展示用户码，用户在另一台设备上授权"],
    ["Refresh Token", "用长期令牌换短期访问令牌", "生产环境应启用轮换；检测到已作废令牌被复用应撤销整个令牌家族"],
], [2.7, 3.6, 5.1], rowh=0.68)
example(s, 5.0, "已被正式移出 OAuth 2.1 的两种：Implicit grant 与"
                "“用户名+密码直接换令牌”（ROPC）。后者在遗留系统对接现场很实用——"
                "如果客户的老系统集成方案还在要求你用这种方式对接，那就是遗留设计。"
                "应该主动向客户指出，而不是照单全收当成“客户的正式要求”接进新系统。",
        "下册 §4.3", h=1.6)

# mTLS
s = sl("M17", "4.3 mTLS")
heading(s, "mTLS 的两个高频故障，都跟证书本身没关系")
card(s, ML, 2.0, 5.55, 2.4, "证书链不完整", "同一套证书在不同客户端表现不一致",
     "服务端只配置了信任根证书，但客户端出示的证书链中间缺了中间 CA 证书。"
     "部分客户端库不支持自动补链，验证失败；另一些支持自动补拉，却能验证通过。"
     "这种诡异现象的根源往往就是链不完整，只是被部分客户端的自动补链能力掩盖了。",
     accent=WRONG)
card(s, ML + 5.85, 2.0, 5.55, 2.4, "SNI 缺失或路由错配", "证书链没问题，握手仍然失败",
     "TLS 握手阶段服务端依赖客户端携带的 SNI 决定出示哪张证书——"
     "这个环节发生在 HTTP 层的 Host 头可见之前。老旧客户端库可能压根不发送 SNI，"
     "多租户网关就会返回一张不匹配的默认证书。", accent=WRONG)
example(s, 4.75, "排查 SNI 问题最直接的手段：用 openssl s_client 手动指定 SNI 发起握手，"
                 "对比不指定时握手拿到的是不是同一张证书。两次拿到的证书不一样，"
                 "说明服务端确实依赖 SNI 路由。",
        "下册 §4.3、§4.4", h=1.4)

# 凭据轮换
s = sl("M17", "4.3 凭据轮换")
heading(s, "核心原则是重叠窗口，不是一刀切替换")
body(s, "无论轮换的是 TLS 证书、OAuth2 客户端密钥还是长期 API Key，都不应该"
        "“签发新的、同时立即吊销旧的”。", 1.95, size=15, w=10.9)
steps(s, 2.65, [
    ("①", "先签发新凭据", "新旧并存，两套都能验证通过。"),
    ("②", "确认已在所有消费方生效", "分批灰度轮换，把爆炸半径控制在可回滚的范围内。"),
    ("③", "再吊销旧凭据", "确认全部切换完成之后才动这一步。"),
], h=1.7)
body(s, "硬切换的那一刻，必然有一部分尚未完成配置刷新的调用方会被拒绝。",
     4.65, size=15, color=INK, w=10.9)
example(s, 5.25, "这正是 Lab-02 验收标准里“证书轮换过程中零失败请求”要考察的能力——"
                 "40 并发跨轮换窗口，failures=0。另外一条建议：长期有效的静态密钥"
                 "应尽量替换为短生命周期的动态凭据，把泄露的影响窗口从"
                 "“直到下次人工轮换”压缩到密钥本身的有效期之内。",
        "下册 §4.3；labs/lab-02-auth-bridge/，verify.sh 7/7", h=1.6)

# 排查决策树
s = sl("M17", "4.4 排查顺序")
heading(s, "把“连不上”拆成四层逐层定位")
steps(s, 2.1, [
    ("①", "DNS", "解析到的是哪个地址？内网可能做了分离视界解析，同一域名内外结果不同。"),
    ("②", "TCP", "端口通不通？防火墙白名单申请周期以周计，技术方案定稿当天就要提。"),
    ("③", "TLS", "证书链完整吗？内网自签 CA 的根证书要手动导入每一个消费方的信任库。"),
    ("④", "应用层认证", "凭据对不对？这一层必须建立在前三层都排除的前提下才有意义排查。"),
], h=2.15)
quote(s, 4.6, "这棵树的价值不在于覆盖所有情况，而在于排查顺序本身。", size=16)
example(s, 5.5, "很多工程师习惯一上来就怀疑认证配置，因为认证报错信息最显眼。"
                "实际上认证层的问题必须建立在前三层都已经排除的前提下才有意义排查，"
                "否则很容易在错误的层级里打转。走 VPN 分离隧道导致的 DNS 问题，"
                "报错通常是“连接超时”而不是“域名解析失败”。",
        "下册 §4.4", h=1.5)

# 企业网络三条
s = sl("M17", "4.4 企业网络")
heading(s, "三个隐形的环境问题")
bullets(s, 2.0, [
    ("企业代理", "显式代理需要客户端主动配置代理地址；透明代理在网络层拦截、客户端无感知。"
                  "这个区别决定了你要不要改代码。"),
    ("分离视界 DNS", "很多企业内网对同一个域名做了内外分离解析。承载集成逻辑的容器"
                      "没有正确接入客户内网 DNS，会解析到错误的地址——"
                      "报错通常是“连接超时”而不是“域名解析失败”。"),
    ("自签证书链", "内网 CA 签发的证书天然不在任何标准信任库里。两类常见故障："
                    "客户只给了叶子证书没给中间链；证书里的主机名字段和实际访问的主机名不一致。"),
], size=13, gap=11)
rule(s, 5.15)
body(s, "防火墙白名单申请是流程问题，不是技术问题：周期往往要走几天到几周。"
        "技术方案定下来之后应该立刻提交申请，不要等集成开发完成、临上线才发现网络不通。",
     5.4, size=14, w=10.9)
example(s, 6.25, "第二条的排查陷阱：走 VPN 分离隧道时，只把办公流量导入隧道，"
                 "API 调用走了直连——很容易被误判为网络层问题而不是 DNS 层问题。",
        "下册 §4.4", h=1.05)

# 你听到的 vs 实际含义
s = sl("M17", "4.4 现场翻译")
heading(s, "客户说的每一句，追问一层")
table(s, ML, 1.95, [
    ["你听到的", "通常的实际含义", "对排期意味着什么"],
    ["“网络是通的”", "办公网到那台服务器是通的。你的容器在另一个网段，未必",
     "别把这句当结论，自己在目标机器上按四层树跑一遍"],
    ["“你申请一下就行”", "有一张要走审批的工单，周期以周计", "技术方案定稿当天就提，不要等开发完再提"],
    ["“这个我们有接口”", "存在一份接口文档。不保证那个接口现在还在跑", "先要一个能调通的样例请求，再排期"],
    ["“等下个变更窗口”", "生产变更集中在固定时段，错过要等下一轮", "把窗口日期倒排进计划，别按自然日算工期"],
    ["“数据可以给你”", "可以给你看。未必可以拷出来，更未必可以进训练流程", "在第 2 关就把“看、拷、训”三件事分开确认"],
], [2.2, 4.6, 4.6], hl=5, rowh=0.62)
example(s, 5.6, "这张表的用法不是背下来，是每听到一句就追问一层：谁批、多久、批不下来谁受影响。"
                "这三个问题问出来，对方要么给你一个具体的人名和日期，"
                "要么当场发现这件事其实没人负责。",
        "下册 §4.4", h=1.35)

# 遗留系统五种方式
s = sl("M17", "4.5 遗留系统")
heading(s, "五种对接方式，各自的典型故障模式")
table(s, ML, 1.95, [
    ["方式", "实时性", "典型故障模式"],
    ["SOAP", "准实时", "WSDL 版本漂移导致字段不匹配；信封过大导致老应用服务器连接重置"],
    ["固定长度报文", "批量或准实时", "字段错位导致的静默数据损坏，不抛异常、难以及时发现"],
    ["文件交换", "批量、异步，分钟到小时级", "读取到未写完整的文件；重复文件导致的重复处理"],
    ["数据库直连", "实时", "上游 schema 变更完全不会通知你；绕过应用层的业务校验和触发器"],
    ["MCP 封装", "准实时", "第 4 天展开，Lab-03 实测 8/8"],
], [2.2, 3.0, 6.2], hl=2, rowh=0.6)
example(s, 5.2, "第二行最危险，因为它不会报错：如果某个字段的长度定义理解错了，"
                "解析代码只会把后续所有字段错位地读出一堆“看起来合理但完全错误”的值。"
                "这比一次明显的连接失败危险得多——它可能在生产环境里默默跑很久才被发现。",
        "下册 §4.5", h=1.5)

# 比喻：错位的那一颗扣子（书 · 比喻 3）
s = sl("M17", "4.5 定长报文")
figplate(s, "fdmetabutton", "下册 §4.5")

# 不适用场景
s = sl("M17", "4.5 选型")
heading(s, "选型时先问“它什么时候不适用”")
table(s, ML, 1.95, [
    ["方式", "对遗留系统的侵入性", "什么时候完全不适用"],
    ["SOAP", "低——走它官方暴露的接口", "遗留系统压根没有暴露任何服务接口时无从谈起"],
    ["固定长度报文", "低——只需按既有报文规范收发", "数据结构频繁变化的场景，每次变更都要双方协同升级解析逻辑"],
    ["文件交换", "极低——甚至不需要知道对方是谁", "需要实时响应的交互场景完全不适用"],
    ["数据库直连", "高——绕过它的应用层", "商业软件授权协议禁止时；schema 变更不通知你时"],
], [1.9, 4.0, 5.5], hl=3, rowh=0.62)
body(s, "“对遗留系统的侵入性”这一列决定的不是技术难度，是审批难度："
        "侵入性越低，需要对方配合改的东西越少，白名单和评审就越好过。",
     4.5, size=14.5, w=10.9)
example(s, 5.3, "所以现场的实际选型顺序经常和技术优劣排序相反：先看哪条路"
                "需要对方做的改动最少，再看它的故障模式能不能接受。"
                "这不是妥协，是把 M17 开头那句“接得上才谈得上别的”当真。",
        "下册 §4.5", h=1.35)

# 文件交换与数据库直连
s = sl("M17", "4.5 两条具体的坑")
heading(s, "文件写完了没有，以及为什么别直连数据库")
card(s, ML, 2.0, 5.55, 2.25, "文件交换", "大文件写入不是原子操作",
     "接收端在文件还没写完的时候就开始读取，会读到一份不完整的数据。"
     "标准解法是“完成标记文件”约定：数据文件写完之后，再额外写一个空的标记文件，"
     "接收端只在看到标记文件时才认为数据文件可以安全读取。")
card(s, ML + 5.85, 2.0, 5.55, 2.25, "数据库直连", "技术上最简单，工程上耦合最深",
     "遗留系统的 schema 变更完全不会通知你。绕过应用层意味着绕过了它的业务校验和"
     "触发器逻辑。很多商业软件的授权协议明确禁止绕过应用层直连数据库，"
     "这么做可能直接导致原厂支持失效。", accent=WRONG)
body(s, "文件交换还有三条配套约定：文件命名与去重、处理完归档而不是删除、"
        "失败的文件放进专门的隔离目录而不是留在待处理目录里反复被扫描到。",
     4.55, size=14.5, w=10.9)
example(s, 5.35, "最后一条最容易被跳过：留在待处理目录里的失败文件会被每一轮扫描重新处理一次，"
                 "产生一串一模一样的失败日志，把真正的新问题淹掉。",
        "下册 §4.5", h=1.2)

# 国内 IM
s = sl("M17", "4.6 国内 IM 平台")
heading(s, "把办公软件当集成端点，三件事和常规 API 不一样")
bullets(s, 2.0, [
    ("回调是加密的 XML，不是 JSON", "平台把消息推给你的时候做 AES 加密，以 XML 形式 POST 到"
                                     "你配置的地址。解不开先查环境，别在解密逻辑里找原因。"),
    ("被动响应的窗口很短", "服务器几秒内收不到响应就断开连接并重新发起请求。"
                            "长任务必须转异步——重试是平台替你发起的，你没有选择权。"),
    ("凭据和入口在甲方 IT 手里", "应用密钥、可信 IP 名单、回调地址的配置权限，"
                                  "通常挂在客户管理员账号下。"),
], size=13, gap=11)
quote(s, 4.9, "判断标准可以很简单：现场能不能给你一个平台访问得到的公网入口。"
              "给不了，就别在回调模式上耗时间。", size=15)
example(s, 5.95, "第二条是幂等在另一个场景里的化身。第 2 天讲采用率时说过"
                 "“别让人多记一个入口”——嵌进办公软件是提高采用率最直接的一招，"
                 "但它把权限体系和回调可达性两个新问题带了进来。",
        "下册 §4.6；上册 §10.2", h=1.35)

# M17 清单
s = sl("M17", "本节清单")
heading(s, "M17 清单：对接一个新系统之前")
checklist(s, 1.95, [
    "幂等键设计好了吗？“正在处理中”的并发重试拦了吗？",
    "退避用了抖动吗？只重试瞬时性错误了吗？",
    "端到端超时预算分下去了吗？下游重试受剩余预算约束吗？",
    "熔断的三态和降级路径定了吗？降级选哪条是业务判断，写进范围文档了吗？",
    "白名单申请提了吗？——技术方案定稿当天就该提，周期以周计。",
    "四层排查顺序跑过一遍了吗？还是一上来就在怀疑认证配置？",
    "凭据轮换有重叠窗口吗？分批灰度了吗？",
], size=13.5, gap=11)
footer(s, "下册第 4 章本章清单")

# M17 自检
s = sl("M17", "自检")
heading(s, "两道自检题")
qa(s, 2.0,
   "客户说“网络是通的”，你在容器里 curl 目标接口超时。第一步查什么？",
   "查 DNS，不是查认证。容器可能在另一个网段、可能没接入客户内网 DNS、"
   "可能走了分离隧道。按 DNS → TCP → TLS → 应用层认证的顺序来——"
   "认证层的问题必须建立在前三层都排除的前提下才有意义排查。")
rule(s, 4.15)
qa(s, 4.4,
   "同一个 Webhook 事件，你已经处理过一次。第二次收到时该返回什么？",
   "返回 200，附一个说明性的状态字段。多数供应商把非 2xx 当作投递失败会触发重试，"
   "对一条已处理的事件返回 4xx/5xx，只会引来没完没了地重投。")
footer(s, "下册第 4 章自检问题")

# ══════════════════════════ M18 数据管道 ══════════════════════════

sec("M18", "数据管道与向量层",
    "“模型不行。”——这句话是这一章的开场，也是这一章要反驳的。"
    "大多数“模型不行”的判断，查到底是文档在解析阶段就已经被读乱了。",
    "13:00–14:00 · 下册第 5 章")

s = sl("＊", "本节金句")
maxim(s, "先查数据，再怪模型。",
      "六层诊断梯：源 → 解析 → 分块 → 索引 → 检索 → 生成。**说“模型不行”之前，先指出错在哪一层**——换十个模型也读不对一张被拉平的表。",
      "逐字", "下册 §5.0 章标题", size=40)

objectives("M18", "读完这一节你能做什么", [
    "写一份能跑的数据契约，而不是一份 Word 里的字段说明。",
    "区分三种 schema 漂移，并说清哪一种必须硬阻断。",
    "给一条校验失败的记录选一条处理路径，而不是只有丢弃和硬塞两个极端。",
    "按文档类型选分块策略，并说出判据。",
    "用四个维度做 embedding 选型，并识破榜单分数的口径陷阱。",
    "拿到一个错误回答，用三分法定位根因在检索、数据还是生成。",
], "下册第 5 章")

# 数据契约
s = sl("M18", "5.1 防御式 ETL")
heading(s, "数据契约是嵌在管道里、每次运行都会执行的断言")
body(s, "不是一份 Word 文档里的字段说明。契约至少包含三层：", 1.95, size=15, color=INK)
for i, (k, t) in enumerate([
        ("结构层", "字段名、类型、是否可空"),
        ("语义层", "取值范围、枚举约束、跨字段一致性，比如“结束日期不能早于开始日期”"),
        ("时效层", "这批数据的新鲜度约定，多久没更新算过期")]):
    card(s, ML + i * 3.85, 2.5, 3.55, 1.6, None, k, t, bsize=12)
body(s, "第三层最容易被漏掉。前两层管的是“这批数据对不对”，第三层管的是"
        "“这批数据是不是这批”——上游停更了三天，前两层一条都不会报警。",
     4.4, size=14.5, w=10.9)
example(s, 5.2, "Lab-01 的做法可以直接照搬：Pydantic 模型即数据契约，"
                "字段约束直接写成类型标注和校验器。除格式校验外还有溯源校验——"
                "字段值必须能在原文里找到字面依据。",
        "下册 §5.1；labs/lab-01-invoice-pipeline/，verify.sh 6/6", h=1.45)

# Lab-01 对应
s = sl("M18", "5.1 落到 Lab-01")
heading(s, "这套分类策略在 Lab-01 里长什么样")
steps(s, 2.1, [
    ("①", "契约即代码", "解析出来的字段先过 Pydantic 强类型校验，"
                         "对应契约的结构层与部分语义层断言。"),
    ("②", "用模型做修复", "校验失败时把具体的错误信息回灌进 Agent 的上下文，"
                           "让它尝试自纠错重新抽取。"),
    ("③", "修复结果重新校验", "不能因为是模型输出就免检——修复结果依然要重新过一遍契约。"),
    ("④", "多次失败进隔离", "不允许污染下游。"),
], h=2.25)
body(s, "第 ② 步是“修复”路径的一种特殊形式：用模型代替确定性规则做修复。",
     4.65, size=15, w=10.9, color=INK)
example(s, 5.25, "除格式校验外还有一层溯源校验：字段值必须能在原文里找到字面依据。"
                 "这一条比格式校验更能挡住幻觉——格式对、数值合理、但原文里根本没有这个数，"
                 "只有溯源校验能发现。",
        "下册 §5.1；labs/lab-01-invoice-pipeline/，verify.sh 6/6", h=1.4)

# 三种漂移
s = sl("M18", "5.1 Schema 漂移")
heading(s, "漂移分三种，处理方式完全不同")
table(s, ML, 1.95, [
    ["类型", "判据", "处理"],
    ["增量漂移", "旧字段集合是新字段集合的子集", "不阻断，记一条告警。默认丢弃未声明字段，不自动接纳"],
    ["破坏性漂移", "契约里声明为必需的字段缺失，或类型转换会抛异常",
     "立即阻断该批次——这是整套防御体系里唯一“必须硬阻断”的类型"],
    ["语义漂移", "取值超出契约声明的枚举集合，或统计分布相对历史基线显著偏移",
     "不阻断，但强制隔离并告警"],
], [1.8, 4.6, 5.0], hl=2, rowh=0.72)
body(s, "增量漂移“自动接纳”看似省事，实际是把“要不要用这个新字段”的决策权"
        "交给了不受控的上游。", 4.5, size=14.5, w=10.9)
example(s, 5.25, "语义漂移最危险，因为它最容易在“看起来一切正常”的情况下"
                 "悄悄污染下游好几天才被发现。最典型的例子是某个金额字段的单位"
                 "从“元”变成了“分”却没人通知——结构没变、类型没变、校验全过。",
        "下册 §5.1", h=1.45)

# 四条路径
s = sl("M18", "5.1 脏数据")
heading(s, "四条处理路径，判据要写清楚，不能凭经验拍")
table(s, ML, 1.95, [
    ["路径", "判据", "典型场景"],
    ["丢弃", "记录本身就是噪声，且丢弃后不影响下游统计口径完整性", "上游导出文件末尾的空行、测试探针记录"],
    ["修复", "错误模式已知且有确定性修复规则，修复后能通过契约校验", "日期写成非 ISO 格式；全半角字符归一化"],
    ["隔离", "无法确定性修复，但记录本身可能有价值", "发票金额是负数，无法判断是符号错误还是红冲发票"],
    ["告警", "与前三条叠加使用，不是并列的第四条独立路径", "单批次隔离率超过 5%，说明上游可能发生了系统性变化"],
], [1.2, 5.2, 5.0], hl=4, rowh=0.68)
body(s, "只有“扔进死信队列”和“硬塞进下游”两个极端选项的管道，工程上是偷懒。",
     4.85, size=15, w=10.9, color=INK)
example(s, 5.45, "Lab-01 里“修复”这条路径有一个特殊形式：校验失败时把 Pydantic 的"
                 "错误信息原样回灌给模型做自纠错——用模型代替确定性规则做修复。"
                 "但修复结果依然要重新过一遍契约校验，不能因为是模型输出就免检。",
        "下册 §5.1", h=1.4)

# Airflow 版本现状
s = sl("M18", "5.2 编排")
heading(s, "两个日期容易读反，读反了排期就错半年")
bignum(s, ML, 2.2, "2025-10-22", "2.x 进入“限维护期”", CAVEAT, 30, w=5.3)
bignum(s, ML + 5.7, 2.2, "2026-04-22", "2.x 完全停止支持", WRONG, 30, w=5.3)
body(s, "“进入限维护”和“停止支持”中间隔了半年。写文档或做升级排期时如果把两者搞混，"
        "会导致“以为还有一年缓冲期，结果三个月后就断供安全补丁”。", 3.85, size=15, w=10.9)
example(s, 4.7, "这条本身不重要，重要的是它的形状：任何一个“到某某日期为止”的承诺，"
                "都要问清楚是哪一档——限维护、停止新功能、停止安全补丁、完全下架，"
                "是四件不同的事。客户的信创改造排期上会反复遇到同一类问题。",
        "下册 §5.2", h=1.5)

# 增量同步与回补
s = sl("M18", "5.2 增量与回补")
heading(s, "回补是把过去的调度周期重新排一遍，不是临时脚本")
card(s, ML, 2.0, 5.55, 2.15, "增量同步", "靠调度周期，不靠全量扫一遍再比对",
     "让每次运行只处理“这个调度周期内新增或变更”的数据。"
     "全量拉取再自行去重比对不仅浪费资源，当数据量增长后迟早会拖垮调度周期。")
card(s, ML + 5.85, 2.0, 5.55, 2.15, "回补", "让调度器为过去的区间重新生成并执行",
     "而不是让工程师手写一个“遍历日期循环调用某个函数”的临时脚本。", accent=VERIFY)
body(s, "这个设计上的取舍有实际后果：如果团队图省事写了游离于调度体系之外的补数脚本，"
        "这些补数记录不会出现在运行历史里。", 4.4, size=14.5, w=10.9)
example(s, 5.15, "后续排查“这段数据是什么时候、用哪个版本的逻辑跑出来的”，"
                 "会失去可追溯性。这一条和第 2 天讲的“证据”那一要素是同一件事——"
                 "跑过什么、什么时候跑的、用的哪个版本，本身就是要留下来的证据。",
        "下册 §5.2；上册 §7.3", h=1.45)

# 幂等重跑
s = sl("M18", "5.2 幂等重跑")
heading(s, "一个任务能不能安全重跑，看三个条件")
steps(s, 2.1, [
    ("①", "写入是幂等的", "同一区间跑两遍，结果和跑一遍一样——不是追加两份。"),
    ("②", "只依赖调度区间", "任务的输入完全由调度区间决定，不依赖“现在几点”这类外部状态。"),
    ("③", "副作用可重复", "发消息、调外部接口这类副作用，要么幂等，要么被隔离在重跑范围之外。"),
], h=1.9)
body(s, "少一个都不算真幂等。三个条件里第二条最容易被违反——"
        "代码里出现 now() 或者“取最新一批”，回补就一定跑不出正确结果。",
     4.35, size=14.5, w=10.9, color=INK)
example(s, 5.15, "这和 M17 讲企业集成时那套幂等是同一件事，只是这次的重试发起方"
                 "是调度器而不是网络。判据也一样：重复执行不产生第二条记录。",
        "下册 §5.2、§4.1", h=1.3)

# 文档解析
s = sl("M18", "5.3 文档解析")
heading(s, "四类难点，本质不同，不能用同一套方案硬扛")
table(s, ML, 1.95, [
    ["难点", "本质是什么问题"],
    ["复杂 PDF（多栏排版、图文交错、页眉页脚干扰）", "阅读顺序问题。PDF 本身没有语义结构，只有坐标"],
    ["嵌套表格（合并单元格、跨页表格、表中表）", "结构还原问题。拆成纯文本，行列关系全部丢失"],
    ["扫描件（图片型 PDF，无文本层）", "识别精度问题。识别错误率直接决定下游能拿到多干净的文本"],
    ["混排版式（表格＋图片＋正文交叉）", "版面区域检测问题。划分错了，后续都是在错误区域上做无用功"],
], [4.6, 6.8], rowh=0.6)
example(s, 4.85, "这一层的成本是隐形的：demo 阶段用几份干净 PDF 就能跑通，"
                 "一接入客户真实语料库解析准确率骤降。团队往往第一反应是去调 embedding "
                 "或者重排参数，查了几周才发现问题根本不在检索链路——"
                 "文档解析阶段就已经把表格拆散、把段落顺序读乱了。",
        "下册 §5.3", h=1.6)

# 两个国产工具
s = sl("M18", "5.3 工具对照")
heading(s, "两个国产开源工具的机制对照")
table(s, ML, 1.95, [
    ["工具", "机制", "输出格式", "适用难点"],
    ["PP-StructureV3", "版面区域检测、表格识别、公式识别、图表理解以及多栏阅读顺序恢复",
     "Markdown ＋ 带坐标 JSON", "复杂 PDF、混排版式"],
    ["MinerU", "三路后端：传统流水线 / 多模态大模型引擎 / 混合引擎",
     "结构化提取", "嵌套表格、混排版式；可按文档质量切换后端"],
], [2.4, 4.6, 2.3, 2.1], rowh=0.9)
body(s, "三路后端的取舍：传统流水线速度快但对复杂版式泛化能力有限；"
        "多模态引擎对嵌套表格和不规则版式更鲁棒但推理成本更高；混合是二者的折中。",
     4.35, size=14, w=10.9)
example(s, 5.15, "这个分层设计本身就是一个可迁移的架构思路，和具体工具无关。"
                 "两个工具的机制描述均为一手核实，可作为选型参照——"
                 "但准确率数字仍需自行在客户语料上实测，不能拿官方跑分当结论。",
        "下册 §5.3", h=1.45)

# 选型判据
s = sl("M18", "5.3 选型")
heading(s, "先问文档质量和结构复杂度，不要先问模型好不好")
body(s, "选文档解析方案时最容易犯的错误，是先去比较不同工具的“准确率跑分”。"
        "该先做的是诊断清楚自己的文档属于哪一类难点、严重程度如何。", 1.95, size=15, w=10.9)
quote(s, 2.75, "同一个工具在“排版规整的扫描合同”和“歪斜模糊、印章盖住关键字段的手写补充条款”"
               "上的表现可能天差地别。跑分数字脱离具体语料，谈不上参考价值。", size=14.5)
body(s, "一个可迁移的架构思路：不要指望一个后端啃下所有文档类型。"
        "简单文本用便宜快速的传统流水线，复杂版式再升级到多模态模型——"
        "给不同复杂度的文档路由到不同处理强度的后端。", 4.0, size=14.5, w=10.9)
example(s, 4.95, "扫描件质量分级也是同一套思路：清晰扫描件直接走标准流水线；"
                 "轻度模糊／倾斜的走预处理（去噪、纠偏）再识别；"
                 "严重降质、印章遮挡关键字段的，识别结果标记为低置信度并路由到人工复核。"
                 "这就是防御式 ETL 那四条路径在解析层的复用。",
        "下册 §5.3、§5.1", h=1.55)

# 分块判据
s = sl("M18", "5.4 分块")
heading(s, "按文档类型选分块策略")
table(s, ML, 1.95, [
    ["文档类型", "自然结构单元", "推荐策略"],
    ["合同", "条款／款项（有编号层级）", "层级分块，按“章-条-款”的编号结构切分"],
    ["手册／技术文档", "标题层级", "层级分块，每个块附带其所属标题路径作为元数据"],
    ["表格", "行／逻辑行组", "整表作为一个块，或按行组切分且每个块携带表头"],
    ["代码", "函数／类／模块", "语法感知分块，按函数、类边界切分，不用固定字符数"],
    ["对话记录／工单记录", "一轮问答或一个完整会话", "按会话边界或滑动窗口切分"],
], [2.4, 3.6, 5.4], rowh=0.5)
example(s, 5.15, "最后一行的判据最能说明问题：单条孤立发言经常语义不完整——"
                 "“好的，那就这么定”脱离上下文毫无意义，需要保留紧邻的上下文轮次"
                 "才能被独立检索和理解。",
        "下册 §5.4；本表为工程归纳，非单篇论文的逐类型实证", h=1.4)

# 重叠比例
s = sl("M18", "5.4 一个存疑的数字")
heading(s, "“重叠比例取 10%–20%”——查无实据")
body(s, "这是网上流传最广的分块参数建议之一。本书调研没有找到任何可靠的一手技术文档"
        "或论文给出这个区间的实证依据，只有大量文章反复转抄同一个未溯源的结论。",
     1.95, size=15, w=10.9)
quote(s, 2.95, "方向上可能是对的：重叠确实能缓解“一句话被切断”的问题。"
               "但没有实证支撑的具体数字不能拿来当选型依据。", size=15)
body(s, "处理原则和第 1 天讲的可信度分层完全一致：标注它是二手推测，"
        "自己在客户语料上跑一遍对比，用自己的数字。", 4.15, size=15, w=10.9, color=INK)
example(s, 4.9, "同类的还有“中文必须用 jieba 分词否则召回骤降”。本书自己跑了一遍——"
                "前半句是真的（54 掉到 10），后半句是假的（字符 bigram 57 反而比 jieba 54 更好）。"
                "一个未溯源的说法，方向对、结论错，这是最难识破的一种。",
        "下册 §5.4、§5.5", h=1.55)

# 三种分块不是排名
s = sl("M18", "5.4 分块")
heading(s, "三者不是效果排名，是根据两个条件做的选择")
table(s, ML, 1.95, [
    ["策略", "怎么切", "什么时候用"],
    ["固定长度", "按字符数／token 数切分，可选重叠", "没有明显结构边界的纯文本"],
    ["语义分块", "按语义相似度动态确定切分点", "没有显式结构标记、但内容确实有主题边界"],
    ["层级分块", "按文档自带的结构标记切分，可多层嵌套", "结构化文档——文档自己已经把边界标出来了"],
], [1.8, 4.6, 5.0], hl=3, rowh=0.6)
bignum(s, ML, 4.3, "88.1% / 7.0%", "固定长度：召回率 / 精确率", MUTED, 26, w=5.3)
bignum(s, ML + 5.7, 4.3, "87.3% / 8.0%", "语义分块：召回率 / 精确率", MUTED, 26, w=5.3)
example(s, 5.55, "语义分块精确率略高但召回率反而略低，幅度都不到 1 个百分点，"
                 "是权衡而非碾压。文档有可靠结构标记，优先层级分块；"
                 "没有结构标记但有主题边界，语义分块；两者都没有，固定长度兜底。",
        "下册 §5.4；Chroma 官方研究报告，同一评测集", h=1.45)

# embedding 选型
s = sl("M18", "5.5 Embedding 选型")
heading(s, "四维选型框架，缺一不可")
table(s, ML, 1.95, [
    ["维度", "关键问题", "判据"],
    ["向量维度", "维度越高效果越好，但成本同步上升，值不值得",
     "存储成本与索引构建／查询延迟大致随维度线性增长，边际收益要和增量成本对比"],
    ["语言支持", "纯中文，中英混排，还是更多语种",
     "纯中文场景下，中文专项模型不一定比多语言模型差，需要实测"],
    ["领域适配", "通用语料训练的模型能不能覆盖客户的专业术语",
     "对垂直领域黑话、缩写、内部代号的表征质量很难靠榜单预判，只能拿真实语料抽测"],
    ["成本", "API 调用计费 vs 自托管，以及向量重算的一次性成本",
     "换模型意味着全量语料重新计算向量——这个一次性成本要在选型阶段就估进决策"],
], [1.6, 4.0, 5.8], hl=4, rowh=0.68)
example(s, 5.1, "最后一条是这四维里唯一一个“事后无法补救”的：换模型不是改个配置，"
                "是全量重算向量并重建索引。这也是骨架第四层那句"
                "“向量索引换一次 embedding 模型就得全量重算”的工程出处。",
        "下册 §5.5；上册 §2.0", h=1.45)

# 榜单口径
s = sl("M18", "5.5 口径陷阱")
heading(s, "多语言榜单第一，不等于中文效果第一")
body(s, "一个真实的核实缺口：某中文 embedding 系列没有单独公布中文子榜的分数。"
        "官方博客通篇搜索，中文子榜字样只出现在重排模型的子指标表格里，"
        "embedding 模型本身没有列出中文子榜总分。", 1.95, size=14.5, w=10.9)
quote(s, 3.05, "已核实的“多语言榜单第一、70.58 分”是多语言综合榜单，"
               "不能直接当成“中文效果榜单第一”来引用。这是两个不同的榜单口径。", size=15)
body(s, "另一个更普遍的提醒：实时榜单排名不代表“永远第一”。本书引用的所有排名数字"
        "都标注了发布时点，不代表你阅读时刻的现状。", 4.35, size=14.5, w=10.9)
example(s, 5.15, "还有一个容易被忽略的硬约束：一个 2023 年发布的常用中文模型"
                 "最大序列长度只有 512。对长文档场景（合同、长篇技术手册）是明显短板。"
                 "选型前要先确认这个限制会不会成为瓶颈，而不是等接入后才发现长文档被截断。",
        "下册 §5.5", h=1.5)

# 中文分词实测
s = sl("M18", "5.5 一次实测")
heading(s, "“中文必须用 jieba 分词”——一半对，一半错")
table(s, ML, 1.95, [
    ["分词方式", "总分", "语义改写类", "埋编号类"],
    ["不分词（按空格切）", "10/59", "0/8", "0/40"],
    ["单字 unigram", "47/59", "7/8", "29/40"],
    ["字符 bigram", "57/59", "6/8", "40/40"],
    ["jieba 分词", "54/59", "3/8", "40/40"],
], [3.4, 2.4, 2.8, 2.8], hl=3, rowh=0.56)
body(s, "“不分词召回骤降”是真的，而且比想象的更极端——54 掉到 10。"
        "中文文本按空格切几乎切不出任何词元，整段话成了一两个巨型 token，词频统计完全失效。",
     4.4, size=14, w=10.9)
example(s, 5.2, "但“必须用 jieba”是假的——字符 bigram（57）反而比 jieba（54）更好。"
                "差距集中在语义改写类：jieba 把“核销”“时限”切成完整词之后，"
                "用户问的“多久”“到账”一个都对不上；bigram 切出的碎片反而有更多机会撞上。",
        "下册 §5.5；Lab-04 语料 1241 篇、59 条查询，只换分词方式其余全部固定", h=1.6)

# 客户端 API 变更
s = sl("M18", "5.5 一类容易踩的坑")
heading(s, "向量库客户端的接口在变，抄旧代码会翻车")
table(s, ML, 2.15, [
    ["变更", "后果"],
    ["旧的检索与推荐方法已在 1.16.0 移除，一律改用统一的查询接口", "照抄网上教程直接报方法不存在"],
    ["滚动遍历的过滤参数名和查询接口的过滤参数名不一致", "参数名写错不报错，过滤条件静默失效"],
], [6.2, 5.2], rowh=0.7)
body(s, "第二行是这两条里更危险的一条：参数名不匹配时，多数客户端库会把它当成"
        "一个未知的关键字参数忽略掉，过滤条件形同虚设——检索结果看起来正常，"
        "只是没过滤。", 4.05, size=14.5, w=10.9)
example(s, 4.95, "这类坑没法靠记忆规避，只能靠两条纪律：锁版本（M15 那条），"
                 "以及每次升级依赖之后重跑一遍验收脚本。"
                 "Lab-04 的 verify.sh 就是干这个用的。",
        "下册 §5.5、§1.4", h=1.4)

# 三分法
s = sl("M18", "5.6 三分法")
heading(s, "“回答错了”不是一个根因，是三个")
steps(s, 2.1, [
    ("Step 1", "知识库里有没有", "压根没有能回答这个问题的内容，那不是检索算法的问题，"
                                  "是内容缺失。补内容是唯一解法，不需要往下走。"),
    ("Step 2", "检索有没有命中", "知识库里确实有，检查这次检索返回的上下文里有没有包含它。"
                                  "没命中 → 检索失败；命中了但内容本身是错的 → 数据污染。"),
    ("Step 3", "模型有没有忠实转述", "知识库有、检索也命中了正确材料，回答依然错——"
                                      "问题出在生成环节。这就是模型幻觉。"),
], h=2.4)
body(s, "三种根因对应三个完全不同的动作：修检索算法或索引配置、修数据源头、修提示词或换模型。",
     4.8, size=15, w=10.9, color=INK)
example(s, 5.4, "不能三种都用同一个“再调一下参数”应付过去。这是这一章最实用的一条——"
                "下午上机的 Lab-04 里就有一个三分法诊断脚本，三个函数各对应一步。",
        "下册 §5.6；labs/lab-04-hybrid-rag/", h=1.35)

# 三分法的下一步
s = sl("M18", "5.6 下一步动作")
heading(s, "三种根因对应三个完全不同的动作")
table(s, ML, 2.0, [
    ["根因", "现象", "动哪一层"],
    ["检索失败", "该找的文档没被找到", "修检索算法或索引配置：分词、混合权重、重排深度"],
    ["数据污染", "文档被找到了，但内容本身是错的、过时的或自相矛盾的", "修数据源头——改检索参数一点用都没有"],
    ["模型幻觉", "文档找到了、内容也对，模型却没有忠实转述", "修提示词或换模型；对应第 4 天讲的忠实度指标"],
], [1.6, 4.6, 5.2], hl=2, rowh=0.66)
body(s, "第二种最容易被误诊成第一种，因为两者在用户侧的表现一模一样：回答是错的。",
     4.35, size=15, w=10.9, color=INK)
example(s, 4.95, "定位到第三种之后还能再细一层：把回答拆解成若干条陈述，"
                 "逐条检查能否从检索到的上下文里推出。找不到支持依据的那一条，"
                 "就是幻觉的具体位置——不是“这个回答有幻觉”，是“第几句话是幻觉”。",
        "下册 §5.6、第 9 章", h=1.5)

# M18 清单
s = sl("M18", "本节清单")
heading(s, "M18 清单：接一份新语料之前")
checklist(s, 1.95, [
    "数据契约写成能跑的断言了吗？三层都有吗——结构、语义、时效？",
    "三种漂移分别怎么处理定了吗？破坏性漂移是硬阻断，其余两种不是。",
    "脏数据的四条路径判据写下来了吗？还是只有丢弃和硬塞两个极端？",
    "文档属于哪一类解析难点？诊断过了吗，还是直接开始比工具跑分？",
    "分块策略是按文档类型选的吗？依据是文档有没有可靠的结构标记。",
    "embedding 选型四个维度都过了吗？换模型的一次性重算成本估进去了吗？",
    "三分法诊断脚本接进管道了吗？出错时能自动指出根因在哪一层吗？",
], size=13.5, gap=11)
footer(s, "下册第 5 章本章清单")

# M18 自检
s = sl("M18", "自检")
heading(s, "两道自检题")
qa(s, 2.0,
   "上游新增了一个字段，管道该阻断还是放行？",
   "放行，记一条“发现未声明字段”的告警，默认丢弃未声明字段、不自动接纳。"
   "自动接纳看似省事，实际是把“要不要用这个新字段”的决策权交给了不受控的上游。"
   "唯一必须硬阻断的是破坏性漂移——必需字段缺失，或类型转换会抛异常。")
rule(s, 4.3)
qa(s, 4.55,
   "用户反馈“这个问题回答错了”，你先做什么？",
   "先查知识库里到底有没有能回答这个问题的内容。没有就是内容缺失，"
   "补内容是唯一解法，不用往下走三分法。跳过这一步直接调检索参数或提示词，"
   "是最常见的一种把几周时间花在错误方向上的做法。")
footer(s, "下册第 5 章自检问题")

# ══════════════════════════ M19 本体工程实现 ══════════════════════════

sec("M19", "本体的工程实现",
    "七要素定完了，落到什么上。原则只有一条：可替换优先。"
    "这一章插在数据管道之后、Agent 编排之前，因为本体是 Agent 动作的作用对象。",
    "14:00–14:30 · 下册 §6.1–6.2")

objectives("M19", "读完这一节你能做什么", [
    "把七要素各自落到一个具体的技术层上，并说出优先选什么。",
    "回答“该不该上图数据库”——判据是查询形态，不是关系数量。",
    "把“动作必须写回”拆成幂等、事务边界、补偿三件工程事。",
    "说清这一章为什么插在数据管道之后、Agent 编排之前。",
], "下册 §6.1–6.2")

# 七要素落层
s = sl("M19", "6.1 落层")
heading(s, "七要素各自落在哪一层")
table(s, ML, 1.95, [
    ["要素", "落在哪一层", "优先选"],
    ["对象", "数据模型", "开放 Schema——JSON Schema、OpenAPI，或干脆就是数据库表结构"],
    ["状态", "数据模型 ＋ 状态机", "状态与迁移条件显式建表，不要散在业务代码的 if 里"],
    ["关系", "存储层", "关系数据库优先，确有复杂关系需求再上图数据库"],
    ["动作", "执行层", "工作流引擎、规则引擎、服务 API"],
    ["判据", "执行层", "规则引擎或策略引擎，与动作分离"],
    ["权限", "授权层", "IAM、RBAC/ABAC、策略引擎"],
    ["证据", "事件层", "消息队列 ＋ 事件日志 ＋ 审计存储"],
], [1.2, 2.8, 7.4], hl={5, 7}, rowh=0.46)
example(s, 5.5, "再加两条跨层的：Agent 通过工具接口调动作，不直接写底层数据库——"
                "Agent 能做的动作集合，必须等于人能做的动作集合，不能多出一条旁路。"
                "以及：元数据要能导出、能版本控制、能做差异审查——"
                "这是“本体属于客户”的工程落点。",
        "下册 §6.1；上册 §7.3、§7.5", h=1.55)

# 为什么插在这里
s = sl("M19", "6.1 位置")
heading(s, "为什么插在数据管道之后、Agent 编排之前")
body(s, "因为本体是 Agent 动作的作用对象。", 2.0, size=17, color=INK)
steps(s, 2.6, [
    ("上一章", "数据管道", "数据怎么进来、怎么切、怎么向量化——那是原料。"),
    ("这一章", "本体", "这些原料构成哪些对象、对它们能做什么、谁批准。"),
    ("下一章", "Agent 编排", "怎么把动作串起来——那是执行。"),
], h=1.75)
quote(s, 4.6, "顺序倒过来的典型症状：Agent 能查任何表、能改任何字段，"
              "每加一个新场景就要在提示词里补一段“不要动 XX 表”。"
              "那不是本体，那是口头约定。", size=15)
example(s, 5.85, "第 4 天讲 Agent 编排和护栏时，会看到这一层缺席的代价："
                 "护栏只能在提示词层面写，而提示词层面的约束"
                 "“成立与否取决于模型这一次表现如何”。",
        "下册 §6.1、第 7、8 章", h=1.2)

# 先别上图数据库
s = sl("M19", "6.1 关系层")
heading(s, "先别上图数据库——判据是查询形态，不是关系数量")
body(s, "“本体天然是图”这句话本身没错，但它推不出“所以要用图数据库”。"
        "一个最小可执行业务模型里的关系类型通常只有几十条——设备属于产线、"
        "工单关联设备、审批人属于部门。这个量级关系数据库装得下，"
        "而且客户的 DBA 已经会维护它。", 1.95, size=14.5, w=10.9)
contrast(s, 3.15,
         ["“找出这个工单关联的设备和它所在的产线”",
          "——这是两个 JOIN。"],
         ["“找出这台设备三跳以内所有受影响的下游工位”",
          "——这是图查询：变长路径遍历，而且深度不定。"],
         h=1.4, wtitle="不该上图数据库", rtitle="该上图数据库的唯一信号")
quote(s, 4.85, "前者用图数据库，你付出的是一整套新的运维、备份、监控和人员技能，"
               "换回来的是零。", size=15.5)
example(s, 5.85, "“平台只是本体的一种运行方式”在这一层的含义是：先用客户已经在运维的"
                 "东西实现，等它真的不够用了再换。换的时候有对象模型和关系定义在手，"
                 "换的是存储不是本体。",
        "下册 §6.1；上册 §7.5", h=1.25)

# 三条硬规矩的工程形态
s = sl("M19", "6.2 硬规矩")
heading(s, "“动作必须写回”在工程上是三件事")
for i, (k, t) in enumerate([
        ("幂等", "动作会被重试——网络超时、消息重投、用户手抖点两次。"
                  "每个写回动作要有幂等键，重复执行不产生第二条工单。"
                  "M17 那套幂等设计在这里是同一套东西，只是这次的调用方可能是 Agent 而不是人。"),
        ("事务边界", "一个动作可能要同时改几张表、发一条消息。"
                      "边界画在哪里决定了失败时系统停在什么状态上。"),
        ("补偿", "有些写回撤不回来。撤不回来的动作要在模型里被单独标出来——"
                  "那张标注表在上线关直接变成灰度方案的输入。")]):
    yy = 1.95 + i * 1.42
    tf = tb(s, ML, yy, CW, 0.4)
    line(tf, "第 %d 件" % (i + 1), 11, VERIFY, bold=True, first=True, space_after=5)
    line(tf, k, 15, INK, bold=True, spacing=1.3)
    tf2 = tb(s, ML, yy + 0.6, CW, 0.75)
    line(tf2, t, 12.5, INK2, first=True, spacing=1.4)
    rule(s, yy + 1.26)
example(s, 6.3, "缺一件都会在生产上咬人——而且咬的方式各不相同："
                "缺幂等是重复执行，缺事务边界是半截状态，缺补偿是撤不回来。",
        "下册 §6.2；上册 §9.4", h=1.05)

# 判据与权限
s = sl("M19", "6.2 判据与权限")
heading(s, "判据要和动作分离，权限不能只在应用层")
card(s, ML, 2.0, 5.55, 2.3, "判据", "规则引擎或策略引擎，与动作分离",
     "判据写进动作的代码里，改一条判据就要改一次动作实现、走一次发版。"
     "分离之后判据是可配置的数据，业务方能看懂、能评审、能自己维护——"
     "而这正是第 6 关那句“客户团队能完成一次常见变更”的前提。")
card(s, ML + 5.85, 2.0, 5.55, 2.3, "权限", "IAM、RBAC/ABAC、策略引擎",
     "权限只在应用层判断，等于给了任何绕过应用层的路径一张通行证——"
     "包括 Agent 直接写库、包括运维直连数据库、包括那条“临时”的批处理脚本。",
     accent=VERIFY)
body(s, "两者的共同点：都要能被导出、被版本控制、被差异审查。", 4.65, size=15,
     color=INK, w=10.9)
example(s, 5.25, "这就是“元数据要能导出、能版本控制、能做差异审查”那条跨层要求的用处。"
                 "撤场评审会上，客户问“这套系统现在的权限规则是什么”，"
                 "能导出一份带版本的策略文件，和只能回答“在代码里”，是两种交付。",
        "下册 §6.1、§6.2；上册 §10.4", h=1.45)

# Agent 与人共享边界
s = sl("M19", "6.2 行动边界")
heading(s, "Agent 能做的动作集合，必须等于人能做的动作集合")
quote(s, 2.0, "不能多出一条旁路。", size=20)
body(s, "这条听起来是常识，但在工程上很容易被违反——因为让 Agent 直接读写数据库"
        "比给它包一层工具接口省事得多，而且在演示阶段完全看不出区别。",
     2.85, size=15, w=10.9)
contrast(s, 3.55,
         ["Agent 直接连数据库读写。",
          "权限靠提示词约束：“不要动 XX 表”。",
          "每加一个新场景就在提示词里补一段。"],
         ["Agent 通过工具接口调动作。",
          "工具接口本身受同一套权限体系约束。",
          "加新场景就是加一个工具，权限自动继承。"],
         h=1.9, wtitle="旁路", rtitle="同一套边界")
example(s, 5.7, "第 4 天讲部署护栏时会看到这条的工程形态：护栏必须能自报强制力。"
                "而“提示词里写了不要动”这种约束，连自报的资格都没有——"
                "它成立与否取决于模型这一次表现如何。",
        "下册 §6.2、第 8 章；上册 §7.3", h=1.4)

# M19 清单
s = sl("M19", "本节清单")
heading(s, "M19 清单：本体落地之前")
checklist(s, 1.95, [
    "七个要素各自落在哪一层，写下来了吗？",
    "关系层选的是关系数据库还是图数据库？判据是查询形态吗？",
    "状态与迁移条件是显式建表的，还是散在业务代码的 if 里？",
    "判据和动作分离了吗？业务方能不能自己看懂、自己改？",
    "权限只在应用层判断，还是有独立的授权层？",
    "Agent 的动作集合和人的动作集合一致吗？有没有旁路？",
    "元数据能导出、能版本控制、能做差异审查吗？",
], size=13.5, gap=11)
example(s, 6.05, "最后一条是“本体属于客户”这句话唯一可验证的形式。"
                 "导不出来的东西，谈不上属于谁。",
        "下册 §6.1、§6.2；上册 §7.5", h=1.0)

# ══════════════════════════ 上机 ══════════════════════════

sec("上机 ②", "Lab-04 混合检索 ＋ 质量诊断",
    "BM25 ＋ 向量 ＋ RRF ＋ Rerank 完整链路，附检索质量三分法诊断脚本。"
    "verify.sh 7/7 实测通过，CPU 也能跑。", "14:45–16:15 · 90 分钟")

# 任务书
s = sl("上机 ②", "任务书")
heading(s, "Lab-04 要跑通什么")
table(s, ML, 1.95, [
    ["#", "任务", "验收判据"],
    ["①", "跑通纯向量基线，记录 Recall@5", "脚本打印基线数字"],
    ["②", "跑通 BM25 ＋ 向量 ＋ RRF 混合链路", "脚本自动对比并打印两者数值"],
    ["③", "换三种分词方式各跑一遍 BM25", "复现课上那张 10 / 47 / 57 / 54 的表"],
    ["④", "拿一条错误回答，用三分法脚本定位根因", "说出是检索失败、数据污染还是模型幻觉"],
], [0.5, 6.0, 4.9], hl=3, rowh=0.6)
body(s, "环境：`labs/lab-04-hybrid-rag/`，语料 1241 篇中文企业文档、59 条查询。"
        "无 GPU 环境走 CPU 路径，已实测通过。", 4.6, size=14.5, w=10.9)
example(s, 5.35, "参考结果：混合检索 58/59，纯向量 53/59。"
                 "但注意——这是在这一份语料上的结果，不是普适基准。"
                 "第 ③ 项做完你就会明白为什么：只换分词方式，同一份语料上的分数"
                 "能从 10 跳到 57。",
        "下册附录 A；verify.sh 7/7", h=1.5)

# 环境准备
s = sl("上机 ②", "环境准备")
heading(s, "开跑之前的三件事")
bullets(s, 2.0, [
    ("跑一遍环境自检脚本", "下册附录 A §A.4 给了一个通用自检脚本，覆盖 12 个 Lab 的公共依赖。"
                            "它先告诉你缺什么，比装到一半报错强。"),
    ("确认走哪条路径", "有 GPU 走标准路径；无 GPU 走 CPU 路径，验收阈值相应放宽，"
                        "README 里写明了放宽了多少、为什么。"),
    ("密钥只从环境变量读", "这是各 Lab 共享的一条硬规矩：永远不落盘、不进日志、"
                            "不写进任何文件。"),
], size=13.5, gap=12)
rule(s, 5.05)
body(s, "第二条那句“README 里写明了放宽了多少、为什么”本身就是一次示范："
        "降级路径要写清楚代价，而不是悄悄换个阈值让它通过。", 5.3, size=14.5, w=10.9)
example(s, 6.15, "这和第 2 天讲验收口径那四问是同一件事——在什么数据上测、"
                 "分母是什么、谁判定、什么时候测。",
        "下册附录 A §A.3、§A.4", h=1.0)

# 要看出什么
s = sl("上机 ②", "要看出什么")
heading(s, "90 分钟里真正要带走的不是那两个数字")
bullets(s, 2.0, [
    ("混合不总是赢", "第 ③ 项跑完对照一下：语义改写类查询上，字符 bigram 是 6/8，"
                      "jieba 是 3/8。同一个“更先进”的方案，在不同查询类型上方向相反。"),
    ("先诊断再优化", "第 ④ 项是这个 Lab 最值钱的部分。拿到一条错误回答，"
                      "先跑三分法定位根因，再决定动哪一层。"),
    ("基线要同口径", "对比必须在同一份语料、同一批查询、同一套评测脚本上做。"
                      "换了任何一样，两个数字就不能并列比较。"),
], size=13.5, gap=12)
example(s, 4.75, "第三条是第 2 天讲验收口径那一节的直接应用：在什么数据上测、"
                 "分母是什么、谁判定、什么时候测。同一套追问，"
                 "这次是你问自己。",
        "下册 §5.5；上册 §8.3", h=1.35)
quote(s, 6.35, "跑不出书上那两个数字不是失败——能说清为什么不一样才是通过。", size=14)

# 三分法脚本
s = sl("上机 ②", "三分法脚本")
heading(s, "第 ④ 项：三个函数，各对应一步")
table(s, ML, 2.0, [
    ["函数", "判断什么", "输出"],
    ["第一步：内容存不存在", "底层精确查找，绕过检索链路直接看知识库里有没有",
     "没有 → 内容缺失，补内容，不用往下走"],
    ["第二步：检索到没到、内容对不对", "看这次检索实际返回的上下文里有没有包含它",
     "没命中 → 检索失败；命中但内容错 → 数据污染"],
    ["第三步：模型有没有忠实转述", "把回答拆成若干条陈述，逐条检查能否从上下文推出",
     "有陈述找不到支持 → 那一条就是幻觉的具体位置"],
], [2.9, 4.4, 4.1], hl=3, rowh=0.8)
example(s, 5.35, "这三步的顺序不能跳。直接从第三步开始查，会把“知识库里压根没有”"
                 "误判成“模型幻觉”——然后花几周去调提示词，"
                 "而正确的动作是补内容。",
        "下册 §5.6；labs/lab-04-hybrid-rag/", h=1.35)

# 收尾
s = sl("00", "收束")
heading(s, "今天留下三件东西")
for i, (k, t) in enumerate([
        ("四层排查顺序", "DNS → TCP → TLS → 应用层认证。认证层的问题必须建立在"
                          "前三层都排除的前提下才有意义排查。"),
        ("三分法", "回答错了不是一个根因，是三个：检索失败、数据污染、模型幻觉。"
                    "三种对应三个完全不同的动作。"),
        ("一条判据", "可替换优先。该上图数据库的信号只有一个——"
                      "核心查询是变长路径遍历，而且深度不定。")]):
    card(s, ML + i * 3.85, 2.2, 3.55, 2.4, "第 %d 件" % (i + 1), k, t)
rule(s, 5.0)
body(s, "明天：生成式系统与智能体编排、部署护栏与可观测性、评估的工程实现、"
        "国产模型与信创，以及四个案例串讲。", 5.25, size=15)
body(s, "明天下午的上机是 Lab-05：故意引入一个劣化改动，看 CI 门禁能不能拦住。"
        "这是这五天里唯一一次有明确红绿时刻的上机。", 5.95, size=15, color=INK)
footer(s, "《前线部署工程师（FDE）：把 AI 交付到真实世界》下册第 1–5 章 · §6.1–6.2")


if __name__ == "__main__":
    import pathlib
    out = pathlib.Path(__file__).resolve().parent / "第3天-工程基线到本体落地.pptx"
    n = D.save(str(out))
    print("已生成：%s（%d 页，16:9）" % (out, n))
