#!/usr/bin/env python3
"""第 5 天 · 读一个开源项目，判断它能不能进你的交付

听众：FDE
覆盖：别册二（DeepSeek Harness）全册 ＋ 下册 §5.3–5.5（Semantica）
上机：开源组件进场评估（A 路径实跑 / B 路径断网做文档评估）

这一天的立意不是“学 DeepSeek Harness”，是拿四层骨架当尺子去量两个真实开源项目。
所以下册第 5 章在这里只讲后半，前半（七要素落层）已经在第 3 天讲过。
"""
from deck_kit import *   # noqa: F403

D = Deck()
DAY = "第 5 天 · FDE 专场"


def sl(num=None, label=None, right=DAY, bg=GROUND):
    s = D.slide(bg)
    if label:
        eyebrow(s, num, label, right)
    return s


def sec(num, title, sub, kicker):
    s = D.slide(PANEL)
    section(s, num, title, sub, kicker)
    return s


def objectives(mod, title, items, foot=None):
    s = sl(mod, "本节目标")
    heading(s, title, size=30)
    body(s, "这一节结束时，你应该能做到下面这几件事。", 1.95, size=15)
    tf = tb(s, ML, 2.5, CW, 3.4)
    for i, it in enumerate(items):
        rich(tf, [("%d　" % (i + 1), {'color': VERIFY, 'bold': True,
                                      'mono': True, 'size': 14}),
                  (it, {})], size=15, color=INK2, first=(i == 0),
             spacing=1.45, space_before=0 if i == 0 else 13)
    if foot:
        footer(s, foot)
    return s


# ══════════════════════════ 开场 ══════════════════════════

s = D.slide(PANEL)
eyebrow(s, None, "FORWARD DEPLOYED ENGINEER · 五天实战课程")
tf = tb(s, ML, 1.75, 11.4, 2.4)
line(tf, "读一个开源项目，", 50, INK, bold=True, first=True, spacing=1.12)
line(tf, "判断它能不能进你的交付", 50, INK, bold=True, spacing=1.12)
body(s, "第 5 天　这一天不教工具。拿这四天建起来的四层骨架当尺子，"
        "去量两个真实的开源项目——一个 agent harness，一个本体平台。", 4.4, size=17, w=10.6)
rule(s, 5.5)
for i, (v, k) in enumerate([("2", "个开源样本"), ("7", "个模块"),
                            ("1.5", "小时上机"), ("1", "张评估表")]):
    tf = tb(s, ML + i * 2.55, 5.75, 2.4, 0.9)
    line(tf, v, 30, INK, bold=True, mono=True, first=True)
    line(tf, k, 11, MUTED, space_before=2)
footer(s, "教材：别册二全册 ＋ 下册 §5.3–5.5")

# 今日安排
s = sl("00", "今日安排")
heading(s, "今天六小时怎么走")
timetable(s, 2.05, [
    ["时段", "模块", "内容"],
    ["09:00–09:20", "开场", "四层骨架当尺子：这一天在量什么"],
    ["09:20–09:50", "M27", "一棵可以拆开的树：可配置和可替换差多少"],
    ["09:50–10:35", "M28", "接得上，才谈得上别的——全册最实用的一章"],
    ["10:50–11:30", "M29", "护栏会自报强制力"],
    ["11:30–12:10", "M30", "模型可见即已记录"],
    ["13:10–13:45", "M31", "从会话日志里长出验收集"],
    ["13:45–14:25", "M32", "把它交出去 · 什么时候不该用它"],
    ["14:25–14:50", "M33", "另一个样本：Semantica 覆盖了七要素的哪五个"],
    ["15:05–16:35", "上机 ④", "开源组件进场评估"],
])
footer(s, "16:35–17:00 全课收尾")

# 不是学工具
s = sl("00", "立意")
heading(s, "这一天不教工具")
contrast(s, 2.0,
         ["这个工具怎么用？",
          "它有哪些功能？",
          "跟别的比它好在哪？",
          "——这些问题网上都有答案，而且明年就过期了。"],
         ["这套东西能拆到什么程度？",
          "拆开之后，哪些零件能交到客户手里？",
          "签合同的时候，谁来维护它？",
          "——这些问题只有做交付的人会问。"],
         h=2.4, wtitle="产品评测会问的", rtitle="FDE 才会问的")
example(s, 4.75, "所以今天讲的两个项目，重点都不在“它有什么功能”，"
                 "而在“它的哪些性质能变成你交付材料里的一行字”。"
                 "功能会变，这些性质变得慢得多。",
        "别册二导读", h=1.35)
quote(s, 6.3, "一句话判断你在哪一档：这套东西，最后归谁维护？", size=15)

# 四层骨架当尺子
s = sl("00", "尺子")
heading(s, "四层骨架当尺子，量的是这四件事")
table(s, ML, 1.95, [
    ["骨架层", "量什么", "今天在哪一节量"],
    ["公理 · 风险转移", "用了它之后，谁的不确定性变小了？谁的责任变清楚了？", "M32"],
    ["过程 · 六道关卡", "它能帮你过哪几关？哪几关它帮不上？", "M27、M28"],
    ["结果 · 证据", "它留下的痕迹，够不够回答“当时它看到了什么”？", "M30、M33"],
    ["结果 · 控制", "它的护栏能不能自报强制力？失败时是拒绝还是放行？", "M29"],
    ["资产 · 验收集", "它有没有帮你攒下那个模型换了也不用重建的东西？", "M31"],
], [2.4, 5.8, 3.2], hl={3, 4, 5}, rowh=0.56)
body(s, "五行里有三行落在第三、四层——这不是巧合。", 4.85, size=15, color=INK)
example(s, 5.4, "工具最容易帮上忙的是过程层（帮你干活快一点），"
                "最难帮上忙的是资产层（帮你留下东西）。"
                "而交付的成败恰恰更多由后者决定。"
                "所以量一个工具的时候，往后两层看，比往前两层看更能分出高下。",
        "上册 §9.0", h=1.5)

# 两个样本
s = sl("00", "两个样本")
heading(s, "今天量的两个项目，形状完全不同")
table(s, ML, 1.95, [
    ["", "DeepSeek Harness", "Semantica"],
    ["它是什么", "一棵可以逐个换零件的 agent 执行层插件树", "从数据源到知识图谱再到智能层的一条管线"],
    ["许可证", "MIT", "MIT"],
    ["成熟度", "开发者预览版，明说会有破坏性变更", "0.6.x，接口会变"],
    ["最强的一层", "控制——护栏自报强制力，失败即拒绝", "证据——W3C 标准溯源，六个文件近五千行"],
    ["最弱的一层", "资产——它不提供验收集，是本书自己推的用法", "动作与权限——七要素里缺的正是最难的两样"],
    ["本册／本章读到什么程度", "读了代码和文档，钉在一个具体提交上", "读了代码和文档，没有运行它"],
], [2.4, 4.5, 4.5], hl={4, 5}, rowh=0.5)
example(s, 5.55, "最后一行值得注意：两处都写明了“读到什么程度”。"
                 "这是第 1 天讲可信度分层那条纪律在这里的形态——"
                 "“我读了文档”和“我跑过”是两个不同的证据等级，不能混着说。",
        "别册二导读；下册 §5.3", h=1.35)

# 两本别册
s = sl("00", "两本别册")
heading(s, "两本别册在方法论上是同一件事，只是拿了两个对象来讲", size=29)
table(s, ML, 2.15, [
    ["", "别册一 · Codex", "别册二 · DeepSeek Harness"],
    ["主要问题", "甲方安全部门问“它会不会乱来”，怎么答", "这套壳能拆到什么程度，拆开怎么交出去"],
    ["对象形态", "一个配置齐全的成品", "一棵可以逐个换零件的插件树"],
    ["许可证", "Apache-2.0", "MIT"],
    ["在中国现场最大的坑", "协议只支持一种形态，多半接不上国产模型", "是开发者预览版，会有破坏性变更"],
    ["成熟度", "高，但边界由它定", "低，但边界由你定"],
], [2.6, 4.4, 4.4], hl=5, rowh=0.5)
body(s, "两本不冲突，甚至互补：别册一最大的坑（接不上国产模型），"
        "正是别册二第 2 章解决的问题。", 4.9, size=15, w=10.9, color=INK)
example(s, 5.55, "两本都不进上下册的目录，原因是分工不同：上下册讲“该做成什么样”，"
                 "别册讲“拿一个具体的东西怎么做到”。"
                 "今天讲的是后者——而后者的对象会过时，方法不会。",
        "别册二导读、§7.3", h=1.3)

# 全景：按尺子重排的功能架构（跨 M27–M32）
s = sl("00", "全景", right="第 5 天 · FDE 专场")
picpage(s, "harness.png", "第一个样本的全景：不是它的组件图，是按尺子重排的",
        "别册二全册 ｜ 开发者预览版，判断随版本变",
        note="接下来五节就是照着这张图往下走。每一块下面那句“问：……”，"
             "才是这一天真正要你带走的东西——下次遇到另一个项目，"
             "你要问的还是这几句。")

# ══════════════════════════ M27 可拆开的树 ══════════════════════════

sec("M27", "一棵可以拆开的树",
    "可配置和可替换不是一回事，而这个区别决定了你的交付物"
    "是一份操作手册还是一份文件。", "09:20–09:50 · 别册二第 1 章")

s = sl("10", "本节金句")
maxim(s, "方法论是工具中立的。换工具重学的是按钮，不是思维。",
      "读到具体产品名的时候顺手问一句：**它在这条链上补的是哪一环。**答得出来，换工具时你知道要找什么替代品；答不出来，那是在学按钮。",
      "逐字", "下册 §3.2", size=31)

objectives("M27", "读完这一节你能做什么", [
    "分清可配置和可替换，并说出它们在评审会上差多少。",
    "用三个角色的划法判断一处能力的边界画得对不对。",
    "跑一次配置树输出，并把它当成交付材料而不是调试信息。",
    "算清“拆掉一个零件”要付的账。",
], "别册二第 1 章")

# 场景
s = sl("M27", "场景")
heading(s, "会议室里那个改不动的开关")
body(s, "安全部门问：这个工具能不能不联网？", 2.05, size=16, color=INK)
body(s, "你说：可以，把域名白名单配成空就行。", 2.6, size=15)
body(s, "对方接着问：谁能改回来？改了谁会知道？", 3.15, size=15)
quote(s, 3.75, "这一问就到底了。配置项是厂商留给你的活动余地，"
               "它的边界不是你划的。", size=16)
rule(s, 4.85)
body(s, "麻烦在于：你手上那个工具的联网能力是内置的。"
        "你能做的只有把它配成空，不能让它不存在。", 5.1, size=15, w=10.9)
footer(s, "别册二第 1 章开篇")

# 两条设计原则
s = sl("M27", "1.1 插件树")
heading(s, "它没有那个碰不到的核心")
card(s, ML, 2.0, 5.55, 2.0, "第一条", "产品的每一部分都是插件",
     "模型适配器是插件，工具注册表是插件，agent 循环本身也是一个包——"
     "不是一个你只能在外面挂钩子的内核。")
card(s, ML + 5.85, 2.0, 5.55, 2.0, "第二条", "扩展的方式是并排挂载，不是打补丁",
     "钩子是别人按自己想象中的用法留的。并排挂载意味着你能挂上一个"
     "他们没想过的用法。", accent=VERIFY)
quote(s, 4.35, "你不需要说服供应商给你留一个开关。", size=18)
example(s, 5.35, "这句话的分量在交付上：你能承诺什么，不再取决于对方的路线图。"
                 "而“能不能承诺”正是第 2 关定界那张范围文档里最难写的一栏。",
        "别册二 §1.1；上册 §11.3", h=1.35)

# seam
s = sl("M27", "1.2 seam")
heading(s, "能力是三个角色，不是一个模块")
table(s, ML, 2.0, [
    ["角色", "干什么", "举例"],
    ["Service Definition", "声明接口", "沙箱能力长什么样"],
    ["Service Provider", "实现接口", "用具体的操作系统隔离机制实现它"],
    ["Consumer", "用这个接口", "执行命令前先把命令包起来"],
], [3.4, 2.6, 5.4], rowh=0.6)
body(s, "这个划法有个直接后果：换实现不动调用方，换调用方不动实现。"
        "能力的边界画在正确的位置上，改造成本会差一个数量级。", 4.15, size=15, w=10.9)
example(s, 4.95, "这不是这个工具特有的道理，是第 1 天讲第 2 关定界时反复说的同一句话——"
                 "边界画在哪儿，决定了后面每一次变更要动多少东西。"
                 "只不过那时候画的是范围文档里的边界，这里画的是代码里的接缝。",
        "别册二 §1.2；上册第 11 章", h=1.45)

# profile / bundle / patch
s = sl("M27", "1.3 交付形态")
heading(s, "profile、组合包、patch：交付形态藏在这里")
steps(s, 2.1, [
    ("组合包", "分发单位", "一组配置条目和挂载它们的代码。"),
    ("profile", "一份具名组装", "列出自己要叠哪些组合包。"),
    ("patch", "定点改一条", "按 id 定位某个条目，越靠后越晚生效，也就越有话语权。"),
], h=1.85)
card(s, ML, 4.25, 11.4, 1.15, "官方文档的原话", None,
     "它打印出的任何条目，都可以由你自己的 patch 替换。",
     accent=VERIFY, bsize=15)
example(s, 5.6, "这句话是这一章能落到交付上的支点。它意味着"
                "“这套东西在你这儿最终跑成什么样”是一份可以被完整打印出来的东西——"
                "而不是散在十几个配置文件和几个环境变量里的隐性状态。",
        "别册二 §1.3", h=1.3)

# dump-config
s = sl("M27", "1.3 一条命令")
heading(s, "每次进现场前先跑一次，把输出存进项目仓库")
body(s, "三个理由：", 2.0, size=15.5, color=INK)
bullets(s, 2.45, [
    ("它是事实，不是意图", "配置文件写的是你想要什么，这个输出是它实际组装成了什么。"),
    ("它能 diff", "下次升级之后再跑一次，多出来和少掉的条目一目了然。"),
    ("它是交付材料", "客户问“现在实际跑的是什么”，你给的是一个文件，不是一段回忆。"),
], size=14, gap=12)
rule(s, 4.75)
body(s, "改完之后再跑一次，diff 出来的应该正好是你想改的那些，"
        "没有多出你没打算改的字段。", 5.0, size=15, w=10.9, color=INK)
example(s, 5.65, "最后这句是验证方式，不是建议。改一条配置带出三条你没注意到的变化，"
                 "是配置系统最常见的坑——而 diff 是唯一能当场发现它的手段。",
        "别册二 §1.3、附录 A", h=1.35)

# 可配置 vs 可替换
s = sl("M27", "1.4 核心对照")
heading(s, "在评审会上，这两者差多少")
table(s, ML, 1.95, [
    ["", "可配置的系统", "可替换的系统"],
    ["你做的动作", "把域名白名单配成空", "不挂载联网工具那个插件"],
    ["你能说的话", "已配置为不允许任何域名", "树里没有这个工具"],
    ["对方能追问的", "谁能改回来？改了谁知道？", "（追问的是别的事了）"],
    ["证明方式", "展示配置文件当前的值", "展示配置树，那一项不在里面"],
    ["出错时会怎样", "配置读取失败可能回落到默认值", "没有的东西不会因为读取失败而出现"],
], [2.2, 4.6, 4.6], hl=5, rowh=0.5)
quote(s, 4.85, "最后一行是要害。一个被配置关掉的能力，仍然在进程里。", size=17)
example(s, 5.75, "这个区别在第 1 天讲过一遍，只是当时讲的是别的事——"
                 "谈判桌上写不下来的边界等于不存在。这里是它的工程版本："
                 "靠一个值维持的边界，和靠结构维持的边界，抗压能力不一样。",
        "别册二 §1.4；上册 §11.3", h=1.4)

# 拆的代价
s = sl("M27", "1.4 代价")
heading(s, "拆掉零件是有代价的")
body(s, "你拆掉的每一个插件，都意味着上游升级时你要自己确认这条路还通不通。"
        "拆得越多，升级的验证面越大。", 1.95, size=15, w=10.9)
contrast(s, 2.75,
         ["拆得多：边界由你定，承诺可以写得很硬。",
          "代价：每次上游升级都要重新验证一遍。",
          "这个成本和你写了多少条 patch 成正比。"],
         ["拆得少：跟着上游走，升级几乎无成本。",
          "代价：边界由对方定，你只能承诺配置项能承诺的。",
          "对方改了默认值，你的承诺可能就不成立了。"],
         h=2.1, wtitle="可替换的代价", rtitle="可配置的代价")
example(s, 5.15, "两边都要付账，只是账单的形状不同。"
                 "选哪边取决于第 2 关那份范围文档里，你到底承诺了什么——"
                 "承诺“不联网”和承诺“已配置为不联网”，是两句不同的话。",
        "别册二 §1.4、§7.1", h=1.4)

# M27 清单
s = sl("M27", "本节清单")
heading(s, "M27 清单")
checklist(s, 2.0, [
    "进现场前跑一次配置树输出，存进项目仓库，注明版本号和日期。",
    "改完 patch 再跑一次，diff 出来的正好是你想改的那些，没有多出你没打算改的字段。",
    "对每一条你在范围文档里写下的边界，问一句：它是靠一个值维持的，还是靠结构维持的？",
    "拆掉的每一个插件，在交付材料里记一行——上游升级时这就是验证清单。",
    "现场配置和开发配置是两份不同的 patch。",
], size=14, gap=13)
example(s, 6.0, "第三条是这一节唯一一条能立刻用在别的项目上的：它不需要你用这个工具。",
        "别册二第 1 章本章清单", h=1.05)

# ══════════════════════════ M28 接得上 ══════════════════════════

sec("M28", "接得上，才谈得上别的",
    "国产网关拒绝请求的原因高度集中在两处，而这两处都有现成的开关，"
    "只是不在设置页面上。这是全册单位篇幅价值最高的一章。",
    "09:50–10:35 · 别册二第 2 章")

objectives("M28", "读完这一节你能做什么", [
    "分清三类端点，并知道哪一类故障最多。",
    "说出“OpenAI 兼容”这四个字兼容的是哪个子集。",
    "用两个开关解决国产网关拒绝请求最集中的两类原因。",
    "按七步顺序排查接不上的问题，而不是从最显眼的报错开始猜。",
    "在进场之前就查清那两件在会议室里问不出来的事。",
], "别册二第 2 章")

# 场景
s = sl("M28", "场景")
heading(s, "密钥对、地址通、每个请求都被拒")
body(s, "这类故障最耗时间的地方在于：它看起来像配置错了，其实是请求的形状不对。",
     2.05, size=16, color=INK, w=10.9)
table(s, ML, 2.75, [
    ["类型", "长什么样", "典型失败"],
    ["公有云国产模型 API", "厂商官方端点", "少，厂商自己会对齐"],
    ["企业网关", "甲方 IT 搭的统一入口，转发到后端", "最多，网关自己实现了一遍协议"],
    ["内网自建推理服务", "推理框架直接暴露端点", "中等，取决于框架版本"],
], [3.0, 4.4, 4.0], hl=2, rowh=0.6)
body(s, "第二行是你在客户现场最常遇到的那一类，也是文档最帮不上忙的那一类——"
        "因为网关是甲方自己实现的，它的行为不在任何一份公开文档里。",
     4.85, size=14.5, w=10.9)
example(s, 5.6, "“OpenAI 兼容”这四个字兼容的是一个子集。"
                "同一个厂商的两个端点、同一个网关的两个版本，"
                "兼容的子集可能都不一样。",
        "别册二第 2 章开篇", h=1.3)

# 兼容的是子集
s = sl("M28", "2.1 兼容子集")
heading(s, "“OpenAI 兼容”这四个字，兼容的是一个子集")
body(s, "它不是一个协议标准，是一种事实上的形状。每个实现方选择兼容其中一部分，"
        "而选哪一部分没有任何约束。", 1.95, size=15, w=10.9)
bullets(s, 2.75, [
    "同一个厂商的两个端点，兼容的子集可能不一样。",
    "同一个网关的两个版本，兼容的子集可能不一样。",
    "文档上写“兼容”，不代表你要用的那个字段在子集里。",
], size=14.5, gap=10)
quote(s, 4.5, "所以“它兼容 OpenAI 协议”这句话，在排查故障时提供的信息量接近零。",
      size=15.5)
example(s, 5.5, "有价值的问法是具体的：这个端点接不接受某个特定角色？"
                "它认哪个字段名？——这两问对应的正好是下一页那两个开关。"
                "把一个笼统的“兼容”拆成几个具体的是非题，是这一章的方法。",
        "别册二 §2.1", h=1.4)

# 两个开关
s = sl("M28", "2.2 两个开关")
heading(s, "本册单位篇幅价值最高的四行配置")
card(s, ML, 2.0, 11.4, 1.75, "在提供方配置里加一段 compat", None,
     "supportsDeveloperRole: false　——　很多网关直接拒绝 developer 这个角色，"
     "因为它们只实现到 system / user / assistant。"
     "maxTokensField: max_tokens　——　只认老字段的服务端会拒绝整个请求。",
     accent=VERIFY, bsize=14.5)
body(s, "两个开关，覆盖国产网关拒绝请求最集中的两类原因。", 4.15, size=15.5, color=INK)
example(s, 4.75, "第一个开关有个特征值得记住：故障表现是普通模型能用，"
                 "只有推理模型失败——因为推理模型的请求里才会出现那个角色。"
                 "看到这个现象，不用查别的，就是它。",
        "别册二 §2.2、附录 A", h=1.4)
quote(s, 6.35, "价值高不是因为它多难，是因为它省下的排查时间和它的长度完全不成比例。",
      size=14)

# 三条要注意的
s = sl("M28", "2.2 三条注意")
heading(s, "关于这两个开关的三件事")
bullets(s, 2.0, [
    ("设置页面里没有这两个字段", "表单只有提供方 ID、地址、协议、密钥、模型。"
                                  "这意味着一个只会点页面的人排不出这个故障——"
                                  "值班交接时要写清楚。"),
    ("开关是断言，不是检查", "官方原话：开关陈述的是关于你的端点的一个断言。"
                             "它不会去探测你的端点支不支持，它只是照你说的做。"),
    ("冒号后面必须给值", "写成只有键名、后面留空会被拒绝。这个设计是对的，"
                          "但第一次踩到会懵。"),
], size=13.5, gap=12)
body(s, "第二条最重要：断言写错了，故障会从“被拒绝”变成“行为不对”，更难查。",
     4.9, size=15, w=10.9, color=INK)
example(s, 5.5, "“它是断言不是检查”这个性质，在这五天里出现过一次同形的："
                "第 2 天讲第 5 关时说过——把“起订量不得低于 500”写进提示词，"
                "模型九成九会遵守。断言和提示词一样，都建立在“对方按你说的来”这个前提上。",
        "别册二 §2.2；上册 §14.2", h=1.45)

# 模型目录
s = sl("M28", "2.3 模型目录")
heading(s, "声明的是你，不是它")
body(s, "模型目录里的能力声明，说的是“我告诉这个工具，这个端点支持什么”，"
        "不是“这个端点真的支持什么”。两者不一致时，报错的位置会很误导人：",
     1.95, size=14.5, w=10.9)
table(s, ML, 2.95, [
    ["现象", "说明", "改哪儿"],
    ["图片在发送前就被拒，还点了模型的名", "该模型没声明图片模态", "给它加上图片模态"],
    ["请求发出去了，提供方拒绝", "声明了端点其实不提供的能力", "从授予它图片能力的那个列表里去掉"],
], [4.4, 3.4, 3.6], rowh=0.72)
example(s, 4.85, "这个坑值得单独记，因为它的表现是“我明明改对了但还在报同一个错”——"
                 "两个方向的错误报出来的错误信息很接近，"
                 "而改错方向会让人越改越远。先分清是发送前被拒还是发送后被拒。",
        "别册二 §2.3", h=1.5)
quote(s, 6.5, "分清“谁拒绝的”，比分清“为什么拒绝”更靠前。", size=14)

# 密钥
s = sl("M28", "2.4 凭据")
heading(s, "密钥挡在配置之外：两条设计")
card(s, ML, 2.0, 5.55, 2.0, "密钥字段是只写的", None,
     "保存之后页面只会收到脱敏描述符，永远不会收到明文。"
     "配置文件里存的是环境变量名，不是密钥本身。", accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.0, "消费方每次操作重新解析", None,
     "绝不跨操作缓存。模型适配器每次模型请求解析一次——"
     "这意味着轮换密钥不需要重启。", accent=VERIFY)
body(s, "这一条是本节最值得学的地方，而且和这个工具无关。", 4.35, size=15.5, color=INK)
example(s, 4.95, "它和这五天里的两处直接咬合：第 4 天讲的日志脱敏——"
                 "那一节讲的是别让密钥进日志，这一节讲的是别让密钥进配置；"
                 "以及 Lab-06 那条不变量——密钥只从环境变量读，"
                 "永远不落盘、不进日志、不写进任何文件。",
        "别册二 §2.4；下册 §10.5", h=1.55)

# 七步排查
s = sl("M28", "2.5 排查顺序")
heading(s, "接不上的时候，按这个顺序查")
tf = tb(s, ML, 2.0, CW, 3.6)
for i, (k, t2) in enumerate([
        ("网络可达", "在目标机器上直接发一次请求，不在自己的笔记本上试。"),
        ("凭据", "环境变量名对不对、值有没有被引号包进去、有没有前后空格。"),
        ("协议形状", "这个端点到底是哪种协议，不是“它说自己兼容”就一定是。"),
        ("两个 compat 开关", "推理模型单独失败 → 角色开关；整个请求被拒 → 字段名开关。"),
        ("模型目录声明", "分清是发送前被拒还是发送后被拒。"),
        ("网关自己的限制", "并发上限、单请求体积上限、超时——这些不在协议里。"),
        ("再回头看日志", "前六步都排除了，才值得逐字读模型请求的组装结果。"),
]):
    rich(tf, [("第 %d 步　" % (i + 1), {'color': VERIFY, 'bold': True,
                                        'mono': True, 'size': 13}),
              (k, {'color': INK, 'bold': True}),
              ("　" + t2, {})], size=13.5, color=INK2,
         first=(i == 0), spacing=1.4, space_before=0 if i == 0 else 9)
example(s, 5.85, "第 1 步值得特别说：在中国现场，请求通不通经常不是模型的问题，"
                 "是这台机器的出站策略。而“在自己笔记本上试通了”"
                 "证明不了目标机器上也通——这和第 3 天那套四层排查是同一条纪律。",
        "别册二 §2.5；下册 §6.4", h=1.35)

# 进场前两件事
s = sl("M28", "2.6 进场之前")
heading(s, "两件在会议室里问不出来的事，要提前查")
card(s, ML, 2.0, 5.55, 2.1, "一", "目标机器的运行时版本",
     "版本上不去、且升级走不通，这是前置条件，不是可以绕的。"
     "在“这个工具能不能用”之前就该问清楚——它不属于技术方案，属于可行性三角。")
card(s, ML + 5.85, 2.0, 5.55, 2.1, "二", "有没有一个能用的模型端点",
     "全内网且没有可用端点，这一章讲的全部内容都不成立。"
     "“接得上，才谈得上别的”——这句话是字面意思。", accent=CAVEAT)
body(s, "这两件事在会议室里问不出来，要提前查。", 4.45, size=15.5, color=INK)
example(s, 5.05, "它们本质上是第 1 天讲的可行性三角在工具选型上的形态："
                 "运行时版本是“权限轴”（升级要走谁的审批），"
                 "模型端点是“数据轴”（这个能力到底存不存在）。"
                 "任何一条红灯，都不该进入正式的方案讨论。",
        "别册二 §2.6；上册 §11.1", h=1.55)

# M28 清单
s = sl("M28", "本节清单")
heading(s, "M28 清单：进场之前和进场之后")
checklist(s, 1.95, [
    "进场前：目标机器的运行时版本查了吗？升级要走谁的审批？",
    "进场前：有没有一个能用的模型端点？地址、协议、凭据三样各自谁给？",
    "接上之后：两个 compat 开关的取值写进交付材料了吗？",
    "接上之后：模型目录里声明的能力，和端点实际提供的能力对过一遍了吗？",
    "凭据：配置文件里存的是环境变量名，不是密钥本身——检查过一遍吗？",
    "值班交接：那两个开关不在设置页面上，这件事写进值班手册了吗？",
], size=14, gap=12)
example(s, 6.05, "最后一条经常被忽略。一个只会点页面的人排不出这个故障——"
                 "而值班的人多半就是那样一个人。",
        "别册二第 2 章本章清单", h=1.05)

# ══════════════════════════ M29 护栏 ══════════════════════════

sec("M29", "护栏会自报强制力",
    "护栏最危险的状态不是没配，是配了、没完全生效、而且没人知道。"
    "这套 harness 最值得学的一处设计，就是把那个错觉变成了一个可读的字段。",
    "10:50–11:30 · 别册二第 3 章")

objectives("M29", "读完这一节你能做什么", [
    "说清沙箱管什么、不管什么，并在会议室里主动划出这条边界。",
    "把“我们配了沙箱”换成一句有证据的话。",
    "判断一个审批实现是失败即拒绝，还是失败即放行。",
    "在评审材料里写下一句插件够不着的边界。",
    "用旋钮的实际取值描述部署状态，而不是用预设名。",
], "别册二第 3 章")

# 场景 + 沙箱只管文件
s = sl("M29", "3.1 先划清楚")
heading(s, "沙箱只管文件效果")
table(s, ML, 2.0, [
    ["模式", "允许什么"],
    ["只读", "只允许必需的写出口"],
    ["工作区可写", "再加上工作区根目录，以及后端自己承诺的临时区"],
    ["完全放开", "绕过隔离"],
], [2.6, 8.8], rowh=0.56)
card(s, ML, 4.05, 11.4, 1.0, "官方文档写明的边界", None,
     "网络与进程可见性不在此处的定义范围内。", accent=CAVEAT, bsize=16)
body(s, "这句话你要能在会议室里主动说出来。", 5.35, size=15.5, color=INK)
example(s, 5.9, "被问“这个沙箱能不能防止它联网”的时候，说“沙箱是开着的”"
                "同时做到了两件坏事：它没回答问题，而且它让对方以为你回答了。",
        "别册二 §3.1", h=1.2)

# 场景
s = sl("M29", "场景")
heading(s, "那台内核有点旧的机器")
body(s, "配置是对的，命令也发对了，沙箱启用了。但这台机器的内核版本偏旧，"
        "隔离机制里有一部分特性用不上。", 2.0, size=15, w=10.9)
body(s, "系统没有报错，因为它确实按你说的启用了。它只是没能全部生效。",
     2.75, size=15, w=10.9)
quote(s, 3.45, "这不是配置错误，也不是实现 bug。"
               "这是强制力不完整，而你没有去问它完不完整。", size=16)
rule(s, 4.55)
body(s, "护栏最危险的状态不是没配，是配了、没完全生效、而且没人知道。",
     4.8, size=15.5, color=INK)
footer(s, "别册二第 3 章开篇")

# 自报强制力
s = sl("M29", "3.2 自报")
heading(s, "它会告诉你自己有没有完全生效")
table(s, ML, 1.95, [
    ["", "常见做法", "这里的做法"],
    ["内核不支持全部特性", "尽量启用，能管多少管多少", "启用，并报告只管住了一部分"],
    ["调用方知不知道", "不知道", "知道，是返回值的一部分"],
    ["你在评审会上能说什么", "“我们启用了沙箱”", "“本机强制力为完全／部分，依据如下”"],
    ["出事之后回头看", "配置是对的，没法解释", "当时就记录了强制力等级"],
], [2.8, 4.0, 4.6], hl=2, rowh=0.6)
quote(s, 4.6, "静默降级是护栏工程里最贵的一类缺陷，因为它把“没防住”伪装成“已防住”。",
      size=16)
example(s, 5.55, "所以进场第一天要做的一件事：把这台机器的强制力等级记下来，"
                 "连同内核版本和日期，一起写进交付材料。这是证据，不是配置。"
                 "拿到“部分”的常见原因是较旧的内核接口版本。",
        "别册二 §3.2、附录 A", h=1.45)

# 意图 vs 事实
s = sl("M29", "3.2 判据")
heading(s, "前者是你的意图，后者才是事实")
contrast(s, 2.0,
         ["“我们配置了沙箱。”",
          "证明方式：展示配置文件。",
          "出事时能说的：配置是对的。"],
         ["“本机实际强制力等级为 X，内核版本 Y，记录于 Z 日。”",
          "证明方式：展示运行时返回的那个字段。",
          "出事时能说的：当时的强制力等级就是这个，有记录。"],
         h=2.1, wtitle="意图", rtitle="事实")
body(s, "第 1 天那句判断在这儿正好用得上：能用“是／否”回答的才是通过标准。"
        "“配置了沙箱”回答不了“它生效了吗”。", 4.4, size=15, w=10.9, color=INK)
example(s, 5.2, "这也是这五天里“证据”这一层反复出现的同一种形状："
                "客户签字的范围文档、真实数据端到端跑通、评估集里的真实历史案例、"
                "以及这里的强制力等级字段。共同点是：它们都不是承诺，是记录。",
        "别册二 §3.2；上册 §9.2", h=1.45)

# fail-closed
s = sl("M29", "3.3 失败时拒绝")
heading(s, "失败时拒绝，不是失败时放行")
table(s, ML, 1.95, [
    ["情况", "松散实现的常见后果", "这里"],
    ["没有任何应答者（比如无人值守跑批）", "超时后默认继续", "拒绝"],
    ["应答者代码抛异常", "异常被吞，继续", "拒绝"],
    ["应答者返回了一个无法识别的值", "当成同意", "拒绝"],
], [4.2, 3.8, 3.4], hl={1, 2, 3}, rowh=0.6, accent=VERIFY)
body(s, "审批结果只有四个值，而且是闭合的——只有“本次允许”是授权，"
        "而且只授权所问的那一个操作。", 4.15, size=15, w=10.9, color=INK)
example(s, 4.85, "沙箱那一侧同理：约束函数要么返回一个真正施加约束的命令行，"
                 "要么失败。它不会返回一个“尽力而为”的版本。"
                 "这两处加起来，就是“失败即拒绝”这条原则在两个不同层面的实现。",
        "别册二 §3.3", h=1.45)
quote(s, 6.45, "这条判断和第 4 天那句“不可逆操作没有通过率 90% 这种说法”同源。",
      size=14)

# never 策略
s = sl("M29", "3.4 分发之前")
heading(s, "有一条边界写在插件够不着的地方")
card(s, ML, 2.05, 11.4, 1.35, "官方文档原话", None,
     "拒绝策略在服务内部、分发之前强制执行，"
     "因此即使后来以“前置”方式注册的应答者也无法绕过它。",
     accent=VERIFY, bsize=15)
body(s, "这句话的分量要从反面看才清楚：一个足够可扩展的系统，"
        "总有人能在你之后再挂一个插件上去。", 3.75, size=15, w=10.9)
example(s, 4.55, "这不是假想的攻击，这是插件系统的日常。"
                 "对做交付的人的意义在于：你可以在评审材料里写一句能站得住的话——"
                 "这条边界不但写得下来，还写在一个插件够不着的地方。",
        "别册二 §3.4", h=1.45)
quote(s, 6.2, "可扩展和可保证是一对矛盾。解开它的方式是把少数几条边界"
              "放到扩展点之前去执行。", size=14)

# 权限预设
s = sl("M29", "3.5 看旋钮")
heading(s, "预设是给人看的，强制在旋钮上")
table(s, ML, 2.0, [
    ["预设名", "沙箱", "审批"],
    ["工作区可写", "工作区可写", "每次询问"],
    ["完全放开", "完全放开", "从不询问"],
], [3.6, 3.9, 3.9], rowh=0.6)
body(s, "预设这一层不拥有任何强制执行——官方文档写得很明确。"
        "它只是把几个旋钮打包成一个名字。", 4.15, size=15, w=10.9, color=INK)
quote(s, 4.8, "看旋钮，不要看预设名。交付材料里写的应该是每个旋钮的实际取值，"
              "不是“我们选了标准安全预设”——后者对下一个接手的人没有任何信息量。",
      size=14.5)
example(s, 6.0, "这条规矩超出这个工具本身：凡是把多个开关打包成一个名字的界面，"
                "都要在交付材料里把包拆开写。",
        "别册二 §3.5", h=1.15)

# 每一次调用
s = sl("M29", "3.6 每次调用")
heading(s, "两句话的差别，在事故复盘会上就是全部差别")
contrast(s, 2.1,
         ["“这个进程整体运行在只读模式下。”",
          "——一句听起来很干净的话。",
          "有人翻出一条被批准的提权记录时，这句话会当场垮掉。"],
         ["“每一次执行按会话解析策略；当前部署的默认模式是只读；",
          "会话可以在日志里记录覆盖值；",
          "被批准的提权是一次单独记录在案的调用。”"],
         h=2.15, wtitle="错", rtitle="对")
body(s, "第二句更长，但它经得起翻记录。", 4.55, size=15.5, color=INK)
example(s, 5.15, "这是这五天里“证据”和“承诺”那条分界线的最后一次出现："
                 "承诺是一句话，证据是一条能被翻出来的记录。"
                 "写承诺的时候要按最终会被翻记录的那一天来写。",
        "别册二 §3.6", h=1.4)

# 三档怎么选
s = sl("M29", "3.7 三档怎么选")
heading(s, "选哪一档，看这次要交出去的是什么")
table(s, ML, 1.95, [
    ["场景", "选哪档", "理由"],
    ["你自己在自己机器上用", "工作区可写", "够用，且不影响正常开发"],
    ["交付到客户环境、有安全评审", "只读 ＋ 明确的写出口清单", "写出口清单本身就是评审材料"],
    ["无人值守跑批", "只读，且审批策略配成拒绝", "没有人在场时，“失败即拒绝”是唯一安全的默认"],
    ["需要它改代码的场景", "工作区可写，但工作区边界要写进交付材料", "边界不写下来，等于没有边界"],
], [3.0, 3.4, 5.0], hl=3, rowh=0.66)
example(s, 4.95, "第三行是最容易配错的：无人值守场景下，"
                 "“超时后默认继续”这种实现会让整个审批机制形同虚设——"
                 "而它平时（有人值守时）看起来工作得很好。",
        "别册二 §3.7、§3.3", h=1.2)
quote(s, 6.15, "护栏要在最没人看着的时候仍然成立，才算护栏。", size=15)

# M29 清单
s = sl("M29", "本节清单")
heading(s, "M29 清单：交付材料里关于护栏的那几行")
checklist(s, 1.95, [
    "本机实际强制力等级、内核版本、记录日期——三样一起写。",
    "沙箱管什么、不管什么——主动写明网络与进程可见性不在其范围内。",
    "每个旋钮的实际取值，不是预设名。",
    "审批在无人值守时的行为：拒绝还是放行？验证过吗？",
    "每条护栏配一条验证命令 ＋ 一段预期的拒绝输出，成对写进材料。",
    "写状态描述时用“每一次执行按会话解析”，不用“整个进程运行在某模式下”。",
], size=14, gap=12)
example(s, 6.05, "第五条是客户离场之后还能自己复核的唯一形式——"
                 "它对着第 6 关那句“客户团队能完成一次效果复核”。",
        "别册二第 3 章本章清单、附录 A；上册 §9.0", h=1.05)

# ══════════════════════════ M30 留痕 ══════════════════════════

sec("M30", "模型可见即已记录",
    "“我们记了日志”不是承诺，“凡是模型看见过的都能从日志重建出来”才是。"
    "这两句话差一个数量级。", "11:30–12:10 · 别册二第 4 章")

s = sl("＊", "本节金句")
maxim(s, "证据落到字段，不是落到日志。",
      "日志会滚动、会被清理、格式随时改。**证据要能被查询、被审计、被当作字段引用**，才算这次交付真的留下了证据这一层。",
      "逐字", "下册 §5.2 三", size=38)

objectives("M30", "读完这一节你能做什么", [
    "分清“我们记了日志”和“凡是模型看见过的都能从日志重建”差多少。",
    "分清三种事件域，知道哪一类重启之后还在。",
    "检查一个审计设计有没有记录被拒绝的尝试。",
    "说清遥测能保证什么、不能保证什么。",
], "别册二第 4 章")

# 那条不变量
s = sl("M30", "4.2 不变量")
heading(s, "复盘会上答不上来的那个问题")
quote(s, 2.0, "它当时看到了什么？", size=22)
body(s, "多数系统回答不了这个问题，因为日志是行为的副产品——"
        "先有行为，再顺手记一笔。这里反过来：", 2.95, size=15, w=10.9)
card(s, ML, 3.65, 11.4, 1.2, "那条不变量", None,
     "模型可见即已记录。抵达模型请求的一切都必须能从日志重建。",
     accent=VERIFY, bsize=16)
example(s, 5.05, "这个方向的差别是本章后面所有内容的地基："
                 "审计完整性在这里不是靠自律维持的，是靠“漏记了功能就不对”维持的。",
        "别册二 §4.1、§4.2", h=1.2)
footer(s, "别册二 §4.2")

# 场景
s = sl("M30", "场景")
heading(s, "复盘会上答不上来的那个问题")
body(s, "一次线上事故的复盘会，所有人都想知道同一件事：", 2.0, size=15)
quote(s, 2.55, "它当时到底看到了什么？", size=22)
body(s, "多数系统回答不了这个问题，因为它们的日志是行为的副产品——"
        "先有行为，再顺手记一笔。记什么、记多细，取决于当初写那行代码的人想到了什么。",
     3.5, size=15, w=10.9)
rule(s, 4.5)
body(s, "结果是：日志里通常有“它做了什么”，很少有“它当时看见了什么”。"
        "而后者才是解释行为的那一半。", 4.75, size=15, w=10.9, color=INK)
footer(s, "别册二第 4 章开篇")

# 三层含义
s = sl("M30", "4.2 三层")
heading(s, "这句话有三层含义，第三层最有力")
table(s, ML, 1.95, [
    ["", "常见说法", "这里能说的"],
    ["承诺的内容", "我们记录了所有操作", "模型看到的全部内容都能从日志重建"],
    ["谁保证", "开发规范", "运行时不变量，违反了会失败"],
    ["漏记的后果", "审计里少一条，可能没人发现", "模型少看见一条，功能立刻不对"],
    ["能回答“它当时看到了什么”吗", "不能", "能"],
], [2.8, 4.2, 4.4], hl=3, rowh=0.6)
body(s, "第三行是这条设计真正的力量所在，也是建议你在评审会上主动讲的一句。",
     4.6, size=15, w=10.9, color=INK)
example(s, 5.25, "它还约束了以后的改动：想给模型多喂一样东西，"
                 "就得同时让它能从日志重建——否则功能本身就跑不通。"
                 "这是把审计要求做成了功能的前置条件，而不是并列的一项。",
        "别册二 §4.2；本册未实测这条不变量在违反时的实际行为", h=1.45)

# 三种事件域
s = sl("M30", "4.3 事件域")
heading(s, "三种事件域，别混着讲")
table(s, ML, 1.95, [
    ["事件域", "是什么", "什么时候用"],
    ["会话事件", "追加到日志、并广播出去的持久事实", "某个事实必须在重新加载后仍然存在"],
    ["Agent 事件", "携带活跃 agent：收件箱、步骤、状态、请求、续跑", "要观察或拦截进行中的工作"],
    ["能力事件", "向某条接缝附加策略和适配器", "挂策略，且不想引入循环依赖"],
], [1.8, 4.8, 4.8], hl=1, rowh=0.66)
body(s, "第一类才是审计的对象。把后两类当成审计日志用，重启之后就什么都没有了。",
     4.4, size=15, w=10.9, color=INK)
example(s, 5.05, "分清这三类的实际用途：客户问“这条记录还在不在”，"
                 "答案取决于它属于哪一类。这个问题在事故复盘时一定会被问到，"
                 "而那时候再去分类已经晚了。",
        "别册二 §4.3", h=1.35)

# 成对留痕
s = sl("M30", "4.4 成对留痕")
heading(s, "被拒绝的尝试也留痕")
body(s, "这是审计设计里的一个常见漏洞——只记录发生了的事，不记录被挡下来的事。"
        "结果是：护栏起作用的证据，恰恰没有被记录下来。", 1.95, size=15, w=10.9)
card(s, ML, 2.85, 5.55, 1.7, "审批", "两条事件配对",
     "请求一条、结果一条，靠一个专门的请求 ID 配对。"
     "配对之后才能回答“这次审批等了多久、谁批的”。", accent=VERIFY)
card(s, ML + 5.85, 2.85, 5.55, 1.7, "钩子", "同理",
     "靠处理器 ID 配对。同一个道理：只有一半的记录，回答不了完整的问题。",
     accent=VERIFY)
example(s, 4.85, "还有一条分离设计值得学：审计事件仅写入日志，"
                 "不进入模型的文本记录。审批记录是给人看的，"
                 "把它塞进模型上下文，既浪费预算，又可能让模型学着去绕。",
        "别册二 §4.4", h=1.45)

# 遥测边界
s = sl("M30", "4.5 遥测")
heading(s, "本地日志是账本，遥测是仪表盘")
contrast(s, 2.0,
         ["它保证送达吗？——不保证。",
          "网络断了怎么办？——丢。",
          "能拿它当审计依据吗？——不能。"],
         ["它落在本地吗？——落。",
          "崩溃之后还在吗？——在，不截断也不伪造。",
          "能拿它当审计依据吗？——能。"],
         h=2.0, wtitle="遥测：尽力而为", rtitle="会话日志：真源")
body(s, "两者的用途不同，不能互相替代。把遥测当审计依据，"
        "第一次网络抖动就会出现“这段时间没有记录”。", 4.3, size=15, w=10.9, color=INK)
example(s, 5.0, "第 3 天讲 OpenSpec 时提过一次同类问题：那个工具默认上报匿名使用统计，"
                "进客户内网之前要关掉、并写进软件物料清单。"
                "遥测这件事在交付上永远是两问：它发什么、能不能关。",
        "别册二 §4.5；下册 §3.4", h=1.5)

# 崩溃之后
s = sl("M30", "4.6 崩溃之后")
heading(s, "崩溃之后，日志不截断也不伪造")
body(s, "长周期任务里，单个轮次可能非常庞大——许多步骤、大量工具输出。"
        "这些事件在崩溃前已经被持久追加。", 1.95, size=15, w=10.9)
card(s, ML, 2.75, 11.4, 1.3, "重启之后它做的事", None,
     "用一个合成的“中断”结束事件关闭这个遗留轮次，"
     "在不改变其前后任何独立事件的情况下配平被中断的执行。",
     accent=VERIFY, bsize=14.5)
body(s, "这两件事缺一不可。只做第一件，日志里会留下一个永远开着的轮次；"
        "只做第二件，就是在改写历史。", 4.4, size=15, w=10.9, color=INK)
example(s, 5.2, "“配平但不改写”是审计日志设计里一条很难得的纪律。"
                "它的反面很常见：崩溃之后重启，日志被截断到最后一个完整记录——"
                "而崩溃前那几步恰恰是你最想看的。",
        "别册二 §4.6", h=1.45)

# 日志可查
s = sl("M30", "4.7 可查")
heading(s, "三分法：这条内容现在在哪儿")
table(s, ML, 2.0, [
    ["标记", "含义"],
    ["current", "它现在就在模型上下文里"],
    ["shadowed", "它曾经在，后来被替换掉了"],
    ["log-only", "它只在原始日志里，从不进模型上下文"],
], [2.4, 9.0], hl=1, rowh=0.56)
body(s, "这个三分法是把日志变成资产的入口——它让你能精确地问出"
        "“当时模型看见的那一份到底是哪一份”。", 4.15, size=15, w=10.9, color=INK)
example(s, 4.9, "这件事有两面，都要说：有用的一面是排障时能自己去找上一次同类故障"
                "是怎么解决的；要当心的一面是——这意味着历史会话是一个可被读取的数据源。"
                "在交付材料里要区分写“进日志”和“进模型上下文”，别用一个词概括。",
        "别册二 §4.7", h=1.55)

# M30 清单
s = sl("M30", "本节清单")
heading(s, "M30 清单")
checklist(s, 1.95, [
    "能不能回答“它当时看到了什么”？拿一次真实会话验一遍。",
    "三种事件域分清了吗？你当审计依据的那一类，重启之后还在吗？",
    "被拒绝的尝试留痕了吗？审批和钩子都是成对事件吗？",
    "遥测发什么、能不能关、关法写进物料清单了吗？",
    "交付材料里区分了“进日志”和“进模型上下文”吗？",
    "历史会话可被读取这件事，跟客户说清楚了吗？",
], size=14, gap=12)
footer(s, "别册二第 4 章本章清单")

# ══════════════════════════ M31 验收集 ══════════════════════════

sec("M31", "从会话日志里长出验收集",
    "本章是本书的推论，不是官方主张。回归用例最好的采集时机是故障正在发生的那一刻，"
    "而这套东西在那一刻已经把现场录下来了。", "13:10–13:45 · 别册二第 5 章")

objectives("M31", "读完这一节你能做什么", [
    "说清回归集缺的从来不是格式，是原料。",
    "从一次真实故障里采出一条能复现的回归用例，走完四步。",
    "用一句话判断一条回归用例有没有用。",
    "说清这套做法为什么在中国现场立得住。",
], "别册二第 5 章")

# 回到骨架第四层
s = sl("M31", "5.1 回到骨架")
heading(s, "三个月后那次换模型")
body(s, "换模型要做的事都是工作量：重算向量、调提示词、重跑压测。"
        "但有一个问题不是工作量：", 1.95, size=15, w=10.9)
quote(s, 2.75, "那怎么判断换完之后是不是还对？", size=20)
body(s, "这一问才是要命的。前面那些是工作量，这一句是你有没有资格说“换完了”。",
     3.7, size=15, w=10.9, color=INK)
rule(s, 4.3)
body(s, "回到骨架第四层那个判据：你在这个客户这里建的东西，哪一样不用重建？"
        "答案是验收集——而验收集缺的从来不是格式，是原料。", 4.55, size=15, w=10.9)
example(s, 5.35, "格式很好写。难的是那一条真实的故障现场：当时的输入、"
                 "当时模型看见的上下文、当时调了什么工具、工具返回了什么、"
                 "护栏当时是什么状态。这五样，事后一样都补不回来。",
        "别册二 §5.1、§5.2；上册 §9.0", h=1.45)

# 事后重构的偏差
s = sl("M31", "5.2 原料")
heading(s, "事后重构的用例有一个系统性偏差")
quote(s, 2.0, "真实故障发生的时候，没有人有空记录。", size=18)
body(s, "等到有空了再回头写用例，它会不自觉地写成“我们修完之后它应该长什么样”，"
        "而不是“当时它到底是什么样”。", 2.95, size=15, w=10.9)
card(s, ML, 3.7, 11.4, 1.1, None, None,
     "这样的用例能防住的，恰好是那个已经不会再发生的故障。",
     accent=WRONG, bsize=16)
example(s, 5.1, "这是回归集最常见的失效方式，而且很难被发现——"
                "因为这条用例是绿的，看起来一切正常。"
                "判断它有没有用的最短路径在后面那一页。",
        "别册二 §5.2", h=1.25)

# 日志里现成有的
s = sl("M31", "5.3 现成的原料")
heading(s, "会话日志正好是故障现场的完整记录")
table(s, ML, 1.95, [
    ["回归用例需要", "事后重构能给的", "会话日志里现成有的"],
    ["当时的输入", "记得大概", "用户消息原文"],
    ["当时模型看见的上下文", "完全没有", "按当时的投影出来的那一份"],
    ["当时调了什么工具、参数是什么", "靠翻聊天记录", "工具调用逐条"],
    ["工具返回了什么", "通常没保留", "工具结果逐条"],
    ["那一轮的边界在哪", "靠人划", "轮次的开始到结束"],
    ["护栏当时的状态", "猜", "沙箱模式、审批策略的折叠值"],
], [3.6, 3.4, 4.4], hl=2, rowh=0.46)
example(s, 5.15, "第二行是最关键的一行：模型当时看见的上下文，"
                 "事后完全构造不出来。而拿原始日志直接复跑也不行——"
                 "你会把一堆从没进过上下文的东西喂进去。"
                 "要的是“按当时的投影”，不是“全部日志”。",
        "别册二 §5.3", h=1.55)

# 不能直接复跑
s = sl("M31", "5.3 一个细节")
heading(s, "为什么不能拿原始日志直接复跑")
body(s, "日志里有全部事件，但模型当时看见的只是其中一个投影——"
        "有些内容从没进过上下文，有些进过后来被替换掉了。", 1.95, size=15, w=10.9)
contrast(s, 2.75,
         ["拿全部原始日志喂进去。",
          "你会把一堆从没进过上下文的东西也喂给它。",
          "复现出来的不是当时那个场景。"],
         ["按当时的投影取那一份。",
          "喂进去的正好是模型当时看见的。",
          "这才叫可复现。"],
         h=1.95, wtitle="看起来对", rtitle="真的对")
example(s, 5.05, "这个区别决定了用例是不是真的可复现。"
                 "而它也解释了为什么“把日志存下来”不等于“能采出回归用例”——"
                 "还要能按当时的投影取出那一份。",
        "别册二 §5.3", h=1.4)

# 四步
s = sl("M31", "5.4 四步")
heading(s, "一条故障怎么变成一条回归用例")
steps(s, 2.1, [
    ("第一步", "故障还热着的时候先记会话 id", "在群里回“我看一下”的同时把 id 贴出来——"
                                                "这是唯一能保证它不被忘掉的做法。"),
    ("第二步", "修完之后从那个会话里取三样", "当时的输入、当时的上下文投影、"
                                              "当时的工具调用与返回。"),
], h=1.7)
steps(s, 4.0, [
    ("第三步", "写判据，不写期望输出", "回归集守的是行为的边界，不是文本的相等。"),
    ("第四步", "挂进能自动跑的地方", "不能自动跑的用例，三个月后一定没人跑。"),
], h=1.7)
example(s, 5.9, "第三步最容易做错：写“输出必须等于这段文字”，"
                "换个模型版本它就红了，而系统其实是对的。"
                "该写的是“输出里不能出现某个字段”“必须转人工”这类边界。",
        "别册二 §5.4", h=1.25)

# 最短判据
s = sl("M31", "5.4 判据")
heading(s, "判断一条回归用例有没有用的最短路径")
card(s, ML, 2.1, 11.4, 1.3, None, None,
     "如果把代码回滚到修复之前，这条用例会不会失败？",
     accent=VERIFY, bsize=18)
body(s, "不失败就是没抓住。这比任何评审都快。", 3.75, size=16, color=INK)
rule(s, 4.35)
body(s, "三条边界条件也要一起记：", 4.6, size=15, color=INK)
bullets(s, 5.05, [
    "会话日志里有客户的业务数据——脱敏之后才能进你的仓库。",
    "有些故障是环境相关的，换台机器复现不了——这类要标注清楚，不要当成不稳定用例删掉。",
    "回归集单调增长，但 CI 时长有预算——分层跑，全量定期跑。",
], size=13.5, gap=9)
example(s, 6.5, "第一条是硬的：这一条不做，这套方法在中国现场立不住。",
        "别册二 §5.5、§5.6", h=0.9)

# 在中国现场立得住
s = sl("M31", "5.6 立得住")
heading(s, "为什么这样东西在中国现场立得住")
body(s, "回想第 1 天讲的三条现实：成果全归甲方；信创要求会让技术栈整个换掉；"
        "总分包结构下乙方随时可能被换。在这三条下面，大多数“资产”都立不住。",
     1.95, size=14.5, w=10.9)
table(s, ML, 2.95, [
    ["", "具体是什么"],
    ["日志在哪", "客户自己的机器上，你不需要把它传到任何地方"],
    ["格式开不开放", "只追加的事件日志，两种常见后端"],
    ["查询工具归谁", "开源许可证，客户可以自己编译、自己改"],
    ["你走了还在不在", "在。它不依赖你的账号、你的服务、你的存在"],
], [2.6, 8.8], hl=4, rowh=0.5)
example(s, 5.25, "这四行合起来，就是“客户换掉你之后这东西还能用”。"
                 "你能带走的是能力和判断；带不走的这一样，正是它立得住的原因。"
                 "第 1 天那句“在中国现场能立住的资产，必须是反锁定的”，"
                 "在这里有了一个具体的形状。",
        "别册二 §5.6；上册 §9.0", h=1.5)

# M31 清单
s = sl("M31", "本节清单")
heading(s, "M31 清单：四条能立刻执行的")
checklist(s, 2.0, [
    "在项目的排障流程里加一步：报故障的同时记会话 id，写进值班手册。",
    "从最近一次真实故障里，采出第一条回归用例，走完四步。",
    "对每条用例做一次回滚验证：回到修复前，它必须失败。",
    "用例写的是判据，不是期望字符串——逐条检查一遍。",
], size=14.5, gap=13)
rule(s, 5.2)
body(s, "第一条是四条里最便宜、也最容易被跳过的：它只要求在群里多打几个字。",
     5.45, size=15, w=10.9, color=INK)
example(s, 6.1, "而正是这几个字，决定了三个月后你有没有原料。",
        "别册二第 5 章本章清单", h=0.95)

# ══════════════════════════ M32 交出去 ══════════════════════════

sec("M32", "把它交出去 · 什么时候不该用它",
    "交付物是一份能评审、能回滚、能被别人执行的文件，"
    "不是一份写着“第 3 步：修改配置”的操作手册。",
    "13:45–14:25 · 别册二第 6、7 章")

s = sl("08", "本节金句")
maxim(s, "上线成功，不等于有人能接手。",
      "值班陪跑是你在跑，不是客户在跑。接管有四级：**交文档是第零级，等于弃养**；判据是第三级——能独立完成一次变更并自己验证。",
      "合成", "上册 §14.6（值班陪跑）＋ 下册 附录 E（接管四级）", size=36)

# patch vs 手册
s = sl("M32", "6.1 交付物")
heading(s, "交付物是文件，不是步骤")
table(s, ML, 1.95, [
    ["", "操作手册", "一份 patch 文件"],
    ["谁执行", "人，照着念", "机器，加载"],
    ["出错的地方", "找不到位置、改错行、少改一处", "语法错会当场失败"],
    ["能不能评审", "只能评审文字", "能 diff，能进代码评审"],
    ["能不能回滚", "靠另一份手册反着来", "删掉这份覆盖层"],
    ["三个月后有人问“为什么这么配”", "靠记忆", "版本库日志"],
    ["在变更窗口里花多久", "逐步执行加确认", "放文件、重启、验证"],
], [3.4, 4.0, 4.0], hl=3, rowh=0.46)
example(s, 5.15, "第三行是本节的重点：一份配置补丁能走客户的代码评审流程。"
                 "而一份写着“第 3 步：修改配置”的手册，评审的只能是文字本身——"
                 "评审人看不出它执行之后系统会变成什么样。",
        "别册二 §6.1", h=1.45)

# 三条交付纪律
s = sl("M32", "6.1 三条纪律")
heading(s, "现场配置的三条纪律")
bullets(s, 2.0, [
    ("现场配置和开发配置来自两份不同的 patch", "两份分开，才能保证交付出去的那份里"
                                                "没有任何开发用的条目。"),
    ("每一份现场 patch 配一份配置树输出", "改完再跑一次，diff 出来的正好是你想改的那些，"
                                          "没有多出你没打算改的字段。"),
    ("按变更窗口那套四件套准备", "编号执行脚本、每步验证命令、预检脚本、回滚脚本。"
                                  "这一章讲的 patch 正好是第一件的内容。"),
], size=13.5, gap=12)
body(s, "验证方式：把这份 diff 交给一个没参与过的同事，"
        "他能说出你改了哪几件事。", 4.9, size=15, w=10.9, color=INK)
example(s, 5.5, "这个验证方式比“自己检查一遍”有效得多，"
                "因为它测的不是“改对了没有”，是“改了什么能不能被别人看懂”——"
                "而后者才是交付物的质量。",
        "别册二 §6.1、附录 A；下册 §10.2", h=1.4)

# headless
s = sl("M32", "6.2 无头模式")
heading(s, "一条命令，一个退出码")
bullets(s, 2.0, [
    "不挂载任何宿主界面、HTTP 服务器、Web 运行时或浏览器插件。",
    "不开监听端口——它在一台不允许开放端口的机器上也能跑。",
    "退出码可判定——它能挂进持续集成，也能挂进变更窗口的执行脚本。",
    "成功时标准错误输出为空——你的脚本可以用“非空即告警”这条最简单的规则。",
    "只提交一个任务，没有交互式后续输入的界面。",
], size=13.5, gap=10)
body(s, "这五条合起来，正好是一个能被脚本调用的东西该有的样子。", 4.75,
     size=15.5, color=INK)
example(s, 5.35, "第二条和第三条对交付的意义最大：不开端口意味着它能过"
                 "“不允许新增监听端口”这条常见的安全基线；"
                 "退出码可判定意味着它能进第 4 天讲的那套 CI 门禁。",
        "别册二 §6.2；下册 §8.5", h=1.4)

# SDK
s = sl("M32", "6.3 SDK")
heading(s, "装进客户已经在用的系统里")
table(s, ML, 2.0, [
    ["参数", "对应这五天讲过的哪件事"],
    ["工作区根", "第 3 章那个“工作区可写”的边界"],
    ["会话日志落在哪里", "第 4 章的账本，第 5 章的原料"],
    ["配置树", "第 1 章那份 patch 过的组装结果"],
], [3.2, 8.2], rowh=0.6)
quote(s, 4.05, "你交付给客户的可以是一个装着 agent 的业务系统，"
               "而不是一个要求客户学着开终端的工具。", size=15.5)
body(s, "这个区别在第 6 关采用上是决定性的。", 5.15, size=15.5, color=INK)
example(s, 5.7, "第 2 天讲采用率崩塌时说过：默认做法是做一个独立门户，"
                "它要求用户在既有工作节奏里插进一串新动作。"
                "装进他们已经每天在用的系统里，这个问题就不存在了——"
                "判据是“用户为了用上这个能力，需要多学几个动作”，理想值是零。",
        "别册二 §6.3；上册 §15.2", h=1.5)

# 兼容钩子的边界
s = sl("M32", "6.4 兼容层")
heading(s, "兼容既有钩子：官方自己把话说得很清楚")
card(s, ML, 2.05, 11.4, 1.35, "官方文档原话", None,
     "原生插件可以完成此桥接的所有工作，功能更强，且具有类型化返回，"
     "没有序列化边界。该桥接只是已映射钩子子集的兼容路径。",
     accent=CAVEAT, bsize=14.5)
table(s, ML, 3.75, [
    ["限制", "细节"],
    ["只覆盖一部分钩子点", "十个里的五个"],
    ["只跑 shell 形式的命令钩子", "其他类型会被解析后跳过，只记一条警告"],
    ["没有工具前审批或改写路径", "钩子可以阻塞，但桥接不会预审批或替换工具输入"],
], [3.6, 7.8], hl=2, rowh=0.5)
example(s, 5.55, "第二行是危险的那一条：跳过时只记一条警告。"
                 "如果你迁移过来的钩子里有一个是安全用途的，"
                 "它会安静地不生效——而你看不出任何异常。",
        "别册二 §6.4", h=1.35)

# 交出去之前留什么
s = sl("M32", "6.6 留什么")
heading(s, "交出去之前要留下什么")
table(s, ML, 1.95, [
    ["留什么", "长什么样", "验证"],
    ["现在实际跑的是什么", "配置树输出，带版本和日期", "文件在客户仓库里"],
    ["护栏的实际强制力", "等级、内核版本、日期三样一起", "这是证据，不是配置"],
    ["每条护栏怎么验", "一条命令 ＋ 一段预期的拒绝输出，成对写", "客户自己能跑一遍"],
    ["回归用例", "从真实故障的会话日志里长出来的那几条", "回滚到修复前必须失败"],
    ["版本与升级责任", "锁定的版本号 ＋ 合同里升级归谁", "写在合同里，不是口头"],
], [2.8, 4.6, 4.0], hl={2, 4}, rowh=0.5)
example(s, 5.15, "这五行正好对着骨架的后两层：前三行是控制的证据，"
                 "第四行是资产，第五行是风险归属。"
                 "一份交付材料能不能过第 6 关，看的就是这五行齐不齐。",
        "别册二 §6.6、附录 A；上册 §9.0", h=1.45)

# M32 交付清单
s = sl("M32", "本节清单")
heading(s, "M32 清单：交付形态")
checklist(s, 1.95, [
    "现场配置是一份 patch 文件，不是一份操作手册。",
    "这份 patch 走过客户的代码评审流程了吗？",
    "配一份配置树输出：diff 出来的正好是你想改的那些。",
    "变更窗口四件套齐了吗：编号执行脚本、每步验证命令、预检脚本、回滚脚本。",
    "如果走无头模式：退出码可判定，成功时标准错误输出为空——验证过吗？",
    "如果嵌进客户系统：工作区根、会话日志位置、配置树三个参数各自指向哪里，写清楚了吗？",
    "如果用兼容钩子桥接：被跳过的那几类钩子，确认过没有安全用途吗？",
], size=13.5, gap=11)
example(s, 6.05, "把这份 diff 交给一个没参与过的同事，他能说出你改了哪几件事——"
                 "这是最有效的一条验证。",
        "别册二第 6 章本章清单", h=1.05)

# 子 agent
s = sl("M32", "6.5 子 agent")
heading(s, "把别的产品也接进来")
body(s, "它允许把外部的 agent 当作子 agent 挂进来。对交付的意义在于："
        "你不必要求客户把已有的工具全换掉。", 1.95, size=15, w=10.9)
contrast(s, 2.75,
         ["“把你们现在用的那套换掉。”",
          "触碰的是客户已有的投入和习惯。",
          "在第 6 关采用上要付很大的账。"],
         ["“你们那套继续用，挂在这里当一个子 agent。”",
          "增量的，不要求对方先放弃什么。",
          "阻力小得多。"],
         h=1.95, wtitle="替换", rtitle="并存")
example(s, 5.05, "这条和“别让人多记一个入口”是同一个方向的两种做法："
                 "一个是把自己嵌进对方的系统，一个是把对方嵌进自己的系统。"
                 "选哪个看谁是主入口，判据仍然是——用户要多学几个动作。",
        "别册二 §6.5；上册 §15.2", h=1.4)

# 破坏性变更算成多少钱
s = sl("M32", "7.1 算成本")
heading(s, "“会有破坏性变更”要算成多少钱")
bullets(s, 2.0, [
    ("你那些 patch 的维护成本", "这个成本和你写了多少条 patch 成正比——"
                                 "而写 patch 又正是这套东西最吸引人的地方。"),
    ("接口变更传导到你自己代码的成本", "你嵌得越深，传导面越大。"),
    ("客户侧升级要走的流程", "在生产环境变更要走评审的单位，升级不是技术动作，是一次流程。"
                             "一年升四次和一年升一次，对客户运维是完全不同的负担。"),
], size=13.5, gap=12)
quote(s, 4.85, "合同的运维期，和它的版本节奏，哪个更长？", size=19)
example(s, 5.75, "一个提前说清楚的风险，比一个没说的风险好管理得多——"
                 "所以“会有破坏性变更”这句声明本身是加分项。"
                 "但“设计得好”和“该用在你这个项目上”之间，隔着一整章的距离。",
        "别册二 §7.1", h=1.35)

# 什么情况下不该用
s = sl("M32", "7.2 停止信号")
heading(s, "什么情况下不该用")
table(s, ML, 1.95, [
    ["情况", "判断", "怎么办"],
    ["目标机器的运行时版本上不去，且升级走不通", "不能用", "先解决它，这是前置条件，不是可以绕的"],
    ["客户要求全内网，且没有可用的模型端点", "不能用", "先解决模型接入"],
    ["合同运维期三年，交付后你就撤场", "谨慎", "锁版本 ＋ 合同写明升级不在服务范围，或换成熟方案"],
    ["客户的软件清单要求组件有商业支持", "不能用", "开源件通常过不了这一关，先问清楚清单口径"],
    ["团队里没人读得懂它的实现语言", "可以用，但别写插件", "只做配置和 patch，边界写进项目计划"],
], [4.4, 1.6, 5.4], hl={1, 4}, rowh=0.6)
example(s, 5.15, "第四行是最容易被跳过的一条，而且它和技术无关："
                 "“组件必须有商业支持”这条要求写在客户的软件清单里，"
                 "不写在技术方案里——所以做技术选型的人经常看不到它。",
        "别册二 §7.2", h=1.4)

# 不对症
s = sl("M32", "7.2 不对症")
heading(s, "缺的不是这个的时候")
table(s, ML, 1.95, [
    ["你缺的是", "判断", "该上什么"],
    ["规格产物格式统一", "不对症", "Spec Kit"],
    ["变更轨迹", "不对症", "OpenSpec"],
    ["AI 干活的规矩", "不对症", "Superpowers"],
    ["沙箱 ＋ 审批 ＋ 留痕，而且要成熟", "不对症", "别册一那套 Codex harness"],
    ["能拆开、能改造、能嵌进自己产品", "对症", "就是它"],
    ["内部工具、验证性项目、你自己维护", "对症，且风险最低", "直接用"],
], [4.0, 2.6, 4.8], hl={5, 6}, rowh=0.5)
body(s, "倒数第三行要认真读：如果你要的是一个配好就不用管的成品，"
        "选一棵可以拆开的树，等于替自己揽了拆和装两件活。", 4.75, size=14.5, w=10.9)
example(s, 5.5, "这张表的用法和第 3 天那张“按症状选工具”一样："
                 "先说清楚自己缺的是什么，再看哪个对症。"
                 "顺序反过来——先挑一个看起来最先进的，再去找它能解决什么问题——"
                 "是选型最常见的失败方式。",
        "别册二 §7.2、§7.3；下册 §3.6", h=1.5)

# 三个与工具无关的停止信号
s = sl("M32", "7.4 三个停止信号")
heading(s, "三个和工具无关的停止信号")
for i, (k, t2) in enumerate([
        ("没人回答“归谁维护”", "这个问题在方案评审上问出来没人接，"
                                "说明这套东西交付之后是无主的。"),
        ("客户只想要“能演示”", "如果这一期的合同终点在第 4 关验收，"
                                "引入一个需要长期维护的可拆组件是负资产。"),
        ("你自己也说不清拆了哪些", "拆到自己都要靠翻代码才知道改了什么的程度，"
                                    "交付材料一定写不清楚。")]):
    card(s, ML + i * 3.85, 2.0, 3.55, 2.5, "信号 %d" % (i + 1), k, t2, bsize=12)
quote(s, 4.9, "说到底，harness 是什么？它是你和模型之间那一层，"
              "决定了模型能碰到什么、碰到之后留下什么。", size=15)
example(s, 5.95, "这三条不针对任何具体工具。它们问的是同一件事："
                 "这次交付之后，这套东西的责任落在谁身上。",
        "别册二 §7.4、§7.5", h=1.1)

# harness 是什么
s = sl("M32", "7.5 收束")
heading(s, "说到底，harness 是什么")
quote(s, 2.1, "它是你和模型之间那一层，决定了模型能碰到什么、"
              "碰到之后留下什么。", size=18)
body(s, "这一层的四个问题，正好对着骨架的后两层：", 3.4, size=15, color=INK)
table(s, ML, 3.85, [
    ["问题", "对着骨架哪一层"],
    ["它能碰到什么？", "控制"],
    ["碰不到什么？谁保证碰不到？", "控制"],
    ["碰过之后留下了什么？", "证据"],
    ["留下的那些，你走了之后还在不在？", "资产"],
], [7.4, 4.0], hl=4, rowh=0.5)
example(s, 6.05, "这四问不针对任何具体工具，明年换一个 harness 照样能用。"
                 "今天讲的所有细节都会过时，这四问不会。",
        "别册二 §7.5；上册 §9.0", h=1.05)

# ══════════════════════════ M33 Semantica ══════════════════════════

sec("M33", "另一个样本：Semantica",
    "第 3 天讲了七要素落在哪几层。这一节拿一个真实的开源本体平台，"
    "对着七要素逐条量一遍——看它覆盖了哪五个，缺了哪两个。",
    "14:25–14:50 · 下册 §5.3–5.5")

objectives("M33", "读完这一节你能做什么", [
    "对着七要素给一个开源本体平台逐条打覆盖度，而不是看它的功能列表。",
    "说清“确定性”这条性质在中国现场为什么值钱。",
    "判断“开源版某某”这类说法要打多少折扣，以及为什么这不算批评。",
    "回答“什么时候不该引入一整套本体平台”。",
], "下册 §5.3–5.5")

# 它是什么形状
s = sl("M33", "6.3 形状")
heading(s, "一条从数据源到智能层的管线")
card(s, ML, 2.05, 11.4, 1.35, "管线形状", None,
     "接入 → 解析 → 归一 → 切分 → 抽取（实体／关系／事件）→ 冲突检测 → 去重 "
     "→ 知识图谱构建 → ［本体 · 推理 · 溯源 · 决策］→ 向量库 ＋ 图存储 → 导出",
     accent=VERIFY, bsize=14)
body(s, "对做交付的人有意义的是中括号里那四块，以及它们的两条性质。", 3.75,
     size=15, color=INK)
card(s, ML, 4.25, 5.55, 1.75, "性质一", "确定性",
     "官方明说：推理引擎、知识图谱构建与溯源层是完全确定性的，使用它们不需要 LLM。")
card(s, ML + 5.85, 4.25, 5.55, 1.75, "性质二", "可替换的存储",
     "两类图后端都支持，向量库也是多后端，官方说法是“全都可换，不用改你的代码”。",
     accent=VERIFY)
example(s, 6.25, "性质二正好对上第 3 天那条“可替换优先”。",
        "下册 §5.3；仓库某个具体提交，本册读了代码与文档，没有运行它", h=1.0)

# 确定性的分量
s = sl("M33", "6.3 确定性")
heading(s, "这一条在中国现场的分量比在别处大")
body(s, "一个不需要调用模型就能跑的审计层意味着三件事：", 2.0, size=15.5, color=INK)
for i, (k, t2) in enumerate([
        ("不产生跨境传输", "第 2 天讲的数据出境三档梯度，这一层不触发。"),
        ("不需要论证模型部署位置", "备案那两道门槛，这一层不落入适用范围。"),
        ("断网也能跑", "涉密和物理隔离场景可用。")]):
    card(s, ML + i * 3.85, 2.55, 3.55, 1.65, None, k, t2, bsize=12)
body(s, "第 2 天讲合规排期时那三个问题，这一层能绕开其中两个。", 4.55, size=15, color=INK)
example(s, 5.15, "这是一个很好的例子，说明“技术选型”和“合规成本”不是两件事。"
                 "同样的功能，用确定性方法实现和用模型实现，"
                 "在合规上是两条完全不同的路径——而这个差别在功能对比表上看不出来。",
        "下册 §5.3；上册 §18.3、§18.4", h=1.5)

# 决策是一等公民
s = sl("M33", "6.3 决策")
heading(s, "它最值得学的设计：决策是图里的一等节点")
body(s, "不是一行日志，是一个可以被检索、被连成因果链、被追溯祖先的节点。"
        "记录一次决策时落的字段：", 1.95, size=15, w=10.9)
table(s, ML, 2.75, [
    ["字段", "对应“证据”那一要素的哪一部分"],
    ["scenario 场景", "基于什么"],
    ["reasoning 推理过程", "为什么这么判"],
    ["outcome 结果", "结果是什么"],
    ["confidence 置信度", "把握有多大"],
], [3.6, 7.8], rowh=0.5)
body(s, "这正好是“谁、基于什么、结果、把握有多大”的结构化版本，"
        "而且能导出成通用的溯源格式——多数合规框架接受它。", 4.95, size=14.5, w=10.9)
example(s, 5.65, "这就是第 2 天讲七要素时那句“证据落到字段”的一个现成实现。"
                 "把它和上册那条“动作证据落五个字段，包括判据在执行那一刻的取值”"
                 "对照着看——差的正好是判据取值那一项。",
        "下册 §5.3；上册 §5.3", h=1.4)

# 七要素覆盖度 · 全景
s = sl("M33", "6.4 全景")
picpage(s, "semantica.png", "第二个样本：拿同一把尺子量一遍",
        "下册 §5.4 ｜ 对某一提交的判断，它处于 0.6.x，接口会变",
        note="和上一张对着看：同一把尺子，两个项目量出来的形状完全不同。"
             "这一天要练的不是记住这两个结论，是下次拿到第三个项目时"
             "还能把这张表填出来。")

# 七要素覆盖度
s = sl("M33", "6.4 核心判断")
heading(s, "把七要素逐条对一遍")
table(s, ML, 1.95, [
    ["要素", "覆盖", "依据"],
    ["对象", "有", "知识图谱构建、实体消解"],
    ["关系", "有", "同上，两类图后端"],
    ["状态", "有", "双时态事实与时点快照，可以查“某天的图长什么样”"],
    ["判据", "有", "策略引擎 ＋ 约束校验"],
    ["证据", "强", "标准溯源格式，六个文件近五千行——比多数商业产品认真"],
    ["动作", "无", "仓库里搜不到写回或动作类型这类概念"],
    ["权限", "无", "只有 API 与存储层的访问控制，没有对象级、动作级的业务授权"],
], [1.2, 1.4, 8.8], hl={5, 6, 7}, rowh=0.46)
example(s, 5.5, "结论要说准确：它覆盖了七要素里的五样，缺的两样正好是最难的两样。"
                "而按第 2 天讲的三条硬规矩，缺了动作就不算业务本体。",
        "下册 §5.4；上册 §5.3", h=1.2)

# 打折扣，但不是批评
s = sl("M33", "6.4 定位")
heading(s, "“开源版 Palantir”这个说法要打折扣")
body(s, "第 2 天讲过：本体论和知识图谱的分界，正是“谁能基于这种关系执行什么动作、"
        "动作如何写回生产系统”——而这恰好是它没做的那一半。", 1.95, size=15, w=10.9)
quote(s, 2.85, "但这不算批评，因为它自己没这么承诺。"
               "官方的定位是“补充你现有的技术栈，而不是替换它”——这个说法是准确的。",
      size=15)
card(s, ML, 4.0, 11.4, 1.1, "所以正确的用法是", None,
     "把它当本体的知识层与证据层，动作层和权限层你自己在它外面接。",
     accent=VERIFY, bsize=15.5)
example(s, 5.35, "这个定位下它很值钱：你省掉的是溯源、冲突检测、去重、多后端存储、"
                 "审计导出这一整摊——这摊活自己写要几个月，"
                 "而且写不好在审计时是致命的。",
        "下册 §5.4", h=1.35)

# 三条限制
s = sl("M33", "6.4 三条限制")
heading(s, "三条要一起记住的限制")
for i, (k, t2, col) in enumerate([
        ("评估模块是空的", "目录存在，但初始化文件全文就是一句“即将推出”，"
                            "状态字段写着未完成，导出列表是空的。"
                            "别因为看到目录名就把评估这一块算进方案。", WRONG),
        ("推理匹配器是简化版", "官方自己在说明里写了：条件匹配在这个版本里“刻意简化”，"
                                "接进生产合规门禁之前，请先用你自己的规则集验证它的输出。", CAVEAT),
        ("0.6.x，接口会变", "要写进合同就锁版本，理由和别册二那套一样。", CAVEAT)]):
    card(s, ML + i * 3.85, 2.0, 3.55, 2.6, "限制 %d" % (i + 1), k, t2,
         bsize=12, accent=col)
body(s, "主动写下第二条那种话的项目不多。照它说的做——"
        "把这句话当成一条必须执行的动作，不是一条免责声明。", 4.95, size=14.5, w=10.9)
example(s, 5.75, "第一条是这三条里最容易踩的：一个叫 evals 的目录，"
                 "在方案评审上被当成“评估能力已具备”写进去，"
                 "到实施阶段才发现里面什么都没有。",
        "下册 §5.4", h=1.25)

# 什么时候不该用
s = sl("M33", "6.5 不该用")
heading(s, "什么时候不该用它")
table(s, ML, 1.95, [
    ["情况", "判断", "怎么办"],
    ["你缺的是完整业务本体（含动作与权限）", "不对症", "它只给知识与证据层，另外两层自己接"],
    ["你缺的是可审计的溯源与决策记录", "对症", "就是它最强的地方"],
    ["客户的软件清单要求组件有商业支持", "不能用", "开源件通常过不了这一关"],
    ["客户要求断网运行、数据不出域", "可以用", "推理、图构建、溯源不需要模型"],
    ["你只有几十个对象、几十条关系", "过重", "先用关系数据库加审计表"],
    ["合同运维期长、交付后你就撤场", "谨慎", "锁版本，合同里写明升级不在服务范围"],
], [4.2, 1.6, 5.6], hl={2, 5}, rowh=0.5)
example(s, 5.35, "倒数第二行要认真读：这一整章讲的所有东西，"
                 "都可以用一张业务表加一张审计表实现。引入一整套本体平台的门槛不是"
                 "“我们有本体”，而是——你的对象和关系是否已经多到，"
                 "用表维护它们本身成了一件出错的事。答不上来这个问题，就先用表。",
        "下册 §5.5；上册 §5.3", h=1.6)

# M33 清单
s = sl("M33", "本节清单")
heading(s, "M33 清单：评估一个开源本体平台")
checklist(s, 1.95, [
    "逐条对七要素填覆盖度表，缺的那几样写进方案，不要含糊带过。",
    "确定性：哪些能力不需要调用模型？这直接决定合规路径。",
    "存储可不可替换？换存储要不要改你的代码？",
    "证据落到了哪些字段？能不能导出成通用格式？",
    "有没有名字看起来齐全、实际是空的模块？逐个打开看。",
    "官方自己写下的限制，逐条当成待执行的动作，不是免责声明。",
    "最后问一遍：你的对象和关系，是不是已经多到用表维护本身成了一件出错的事？",
], size=13.5, gap=11)
example(s, 6.05, "最后一条是整章的收束：答不上来就先用表。"
                 "第 2 天那句“不要叫它企业统一本体”，防的就是这一步的过度设计。",
        "下册第 5 章本章清单；上册 §5.3", h=1.05)

# ══════════════════════════ 上机 ══════════════════════════

sec("上机 ④", "开源组件进场评估",
    "拿今天这把尺子，对一个开源项目做一次完整的进场评估，"
    "产出一页能直接交给安全评审的表。", "15:05–16:35 · 90 分钟")

# 两条路径
s = sl("上机 ④", "两条路径")
heading(s, "按教室的网络条件选一条")
card(s, ML, 2.0, 5.55, 2.4, "A 路径 · 有网有 key", "实跑",
     "装起来、接一个端点、跑一次配置树输出、看一次强制力等级、"
     "发一条消息看会话日志里落了什么。"
     "然后对着评估表逐行填——每一行都有实测依据。", accent=VERIFY)
card(s, ML + 5.85, 2.0, 5.55, 2.4, "B 路径 · 断网", "文档评估",
     "只读官方文档和仓库结构，对着评估表逐行填，"
     "每一行注明依据是文档的哪一处、以及“这一条我没验证过”。", accent=CAVEAT)
body(s, "两条路径的产出物格式完全一样，差别只在证据等级那一列。", 4.7,
     size=15, w=10.9, color=INK)
example(s, 5.3, "B 路径不是 A 路径的降级版。真实项目里，绝大多数进场评估"
                "都是在拿不到环境的情况下做的——"
                "能把“我没验证过”这几个字明明白白写进评估表，本身就是一项能力。",
        "本课程设计", h=1.4)

# 环境准备
s = sl("上机 ④", "环境准备")
heading(s, "A 路径开跑之前")
bullets(s, 2.0, [
    ("先确认运行时版本", "这是前置条件，不是可以绕的。版本上不去就直接走 B 路径。"),
    ("准备一个模型端点", "公有云、企业网关、内网自建都行。接不上就先按七步顺序排查——"
                          "这本身就是评估表第 3 行的内容。"),
    ("先跑配置树输出", "在改任何东西之前跑一次，存下来。这是你的基线。"),
    ("看一次强制力等级", "拿到“部分”不是失败，是评估表第 4 行的答案。"),
], size=13.5, gap=12)
rule(s, 5.05)
body(s, "整个上机过程中，每做一步就填一行表。不要等做完再回忆着填。",
     5.3, size=15, w=10.9, color=INK)
example(s, 5.9, "这条要求本身就是这五天里那条纪律的最后一次练习："
                "记录要在现场做，不是事后补。",
        "本课程设计；别册二 §5.2", h=1.05)

# 评估表
s = sl("上机 ④", "评估表")
heading(s, "十行，每一行都对着骨架的某一层")
table(s, ML, 1.95, [
    ["#", "问题", "骨架层"],
    ["1", "它归谁维护？合同期和它的版本节奏哪个更长？", "公理"],
    ["2", "它能帮你过哪几关？哪几关它帮不上？", "过程"],
    ["3", "接得上吗？端点、协议、凭据三件事各自的结论是什么？", "过程"],
    ["4", "它的护栏能不能自报强制力？失败时是拒绝还是放行？", "控制"],
    ["5", "它默认往外发什么？能不能关？关法写进物料清单了吗？", "控制"],
    ["6", "它留下的痕迹够不够回答“当时它看到了什么”？", "证据"],
    ["7", "痕迹是持久的还是进程内的？重启之后还在吗？", "证据"],
    ["8", "它帮你攒下了什么模型换了也不用重建的东西？", "资产"],
    ["9", "许可证是什么？客户的软件清单接受吗？", "风险"],
    ["10", "拆／改／嵌之后，上游升级的验证面有多大？", "风险"],
], [0.5, 8.1, 2.8], hl={4, 6, 8}, rowh=0.42)
footer(s, "每一行填三样：结论、依据、证据等级（实测 / 读文档 / 未验证）")

# 填写示范
s = sl("上机 ④", "填写示范")
heading(s, "一行填完长什么样")
table(s, ML, 2.0, [
    ["列", "填什么", "示范"],
    ["结论", "一句能被追问的话", "护栏能自报强制力，本机为“部分”"],
    ["依据", "出处，不是感觉", "运行时返回的强制力字段；内核版本 X"],
    ["证据等级", "实测 / 读文档 / 未验证", "实测（2026-08-26，教室机器）"],
], [1.6, 4.2, 5.6], hl=3, rowh=0.66)
body(s, "三列缺一不可。只有结论的表，评审时经不起第二个问题。", 4.4,
     size=15.5, color=INK)
example(s, 5.0, "反例：结论写“护栏很完善”，依据写“官方文档”，证据等级空着。"
                "三处都不合格——结论不可追问，依据不指向具体位置，"
                "证据等级留空默认会被当成“实测”。",
        "本课程设计", h=1.4)

# 交叉检查
s = sl("上机 ④", "最后 20 分钟")
heading(s, "两组交换，找出对方那张表里最经不起追问的一行")
body(s, "按这个顺序问：", 2.05, size=15, color=INK)
bullets(s, 2.5, [
    "哪一行的“依据”那一列写的是感觉，不是出处？",
    "哪一行的证据等级标的是“实测”，但其实只是看到了文档里这么写？",
    "第 1 行答出来了吗？“归谁维护”这个问题，答案里有具体的人或团队吗？",
    "第 8 行答出来了吗？答不出来的话，用它到底为你留下了什么？",
], size=14, gap=10)
example(s, 4.9, "第 8 行是这张表里最难答、也最能分出高下的一行。"
                "大多数工具在这一行上都是空的——这不一定是缺点，"
                "但你要知道它是空的，并且知道那部分要靠别的东西补。",
        "本课程设计；上册 §9.0", h=1.4)
quote(s, 6.5, "一张全是“实测”的评估表，通常是一张没认真做的评估表。", size=14)

# ══════════════════════════ 全课收尾 ══════════════════════════

s = D.slide(PANEL)
section(s, "收尾", "五天讲完了", "最后二十分钟，把这五天收成几句能带走的话。",
        "16:35–17:00")

s = sl("00", "回看")
heading(s, "五天，一张地图")
table(s, ML, 1.95, [
    ["天", "在骨架的哪一层", "带走的东西"],
    ["第 1 天", "公理 ＋ 过程前半", "六道关卡四栏表；读数字先问怎么来的；纯服务和交付的判据"],
    ["第 2 天", "过程后半 ＋ 决策者视角", "立项前 12 问；计价方式就是责任分配；七要素"],
    ["第 3 天", "证据（上）", "四层排查顺序；三分法；可替换优先"],
    ["第 4 天", "证据（下）＋ 控制", "有些事不能用比例衡量；“支持离线”要拆两问；延迟预算表"],
    ["第 5 天", "拿骨架当尺子", "十行进场评估表；可配置和可替换差多少"],
], [1.4, 3.2, 6.8], rowh=0.56)
example(s, 5.5, "五天里出现最多次的一句判断是：能用“是／否”回答的才是标准。"
                "它出现在通过标准、验收指标、门禁条件、护栏强制力、"
                "以及今天这张评估表的每一行上。",
        "上册 §9.2", h=1.35)

# 没讲什么
s = sl("00", "边界")
heading(s, "这五天没讲什么，也说一句")
bullets(s, 2.0, [
    ("没教任何一个产品的操作", "工具会换。今天讲的两个开源项目，明年都可能是另一个样子。"),
    ("没给放之四海的参数", "阈值、比例、显存数字，凡是给了的都标了来源和口径，"
                            "而且都说了要在自己环境上重新标定。"),
    ("没承诺成功率", "课上出现的每个百分比都标了来源。三个流传最广的失败率数字，"
                      "第 1 天就说明了为什么一个都不用。"),
    ("没讲模型本身", "微调、预训练、算法改进都不在范围内。这门课讲的是"
                      "把已有能力落到某一家客户的生产环境里。"),
], size=13.5, gap=12)
example(s, 5.15, "把这四条也算进收获里：知道一门课没教什么，"
                 "和知道它教了什么一样重要——因为前者决定了你什么时候不该拿它当依据。",
        "本课程", h=1.35)

s = sl("00", "带走")
heading(s, "如果只带走三句话")
for i, (k, t2) in enumerate([
        ("客户买的是风险转移", "不是代码。所以计价方式决定了这句话有没有牙齿——"
                                "系统没人用你会少收多少钱，答案是“一分不少”的话，"
                                "责任其实还在客户那边。"),
        ("顺序被关卡锁死", "证据攒在第 2–4 关，控制攒在第 5 关，接管只能在第 6 关。"
                            "没有证据就上线，控制保护的是一个没人验证过的东西。"),
        ("留下的东西必须反锁定", "在“成果全部归甲方”这类条款下，"
                                  "任何以锁住客户为目的的资产都谈不下来。"
                                  "唯一立得住的，是明确增强客户自主权的那一样。")]):
    card(s, ML + i * 3.85, 2.2, 3.55, 2.6, "第 %d 句" % (i + 1), k, t2, bsize=12)
rule(s, 5.15)
body(s, "三句话之外，还有一个动作：回去把手上那个项目在四栏表里定个位，"
        "写下它当前那一关的通过标准。写不出来的，那一关就没过。", 5.4, size=15, w=10.9)
footer(s, "《前线部署工程师（FDE）：把 AI 交付到真实世界》上下册 ＋ 两本别册 ＋ 12 个 Lab")

s = sl("00", "接下来")
heading(s, "回去之后的三条路")
table(s, ML, 2.0, [
    ["你的处境", "先做什么", "在书里的位置"],
    ["手上有正在跑的项目", "对着四栏表定位，补齐当前关卡缺的那份产出物", "上册第 9 章"],
    ["准备接一个新项目", "带着《立项前 12 问》去开第一次会", "上册附录 A"],
    ["想系统补一遍", "按 13 周计划走，每周一个产出物、一个自检点", "别册三第 1 章"],
], [2.8, 5.0, 3.6], rowh=0.62)
body(s, "12 个 Lab 全部已实现、验收脚本全部通过。这五天只跑了其中三个，"
        "剩下九个是现成的练习场。", 4.35, size=15, w=10.9)
example(s, 5.05, "最后一句提醒：这五天讲的每一条判据，都可以在你自己的项目上被证伪。"
                 "证伪了就改，别当教条守着——书里每一章都写清楚了自己的证据强度，"
                 "就是为了让你知道哪几条经得起改，哪几条不经得起。",
        "本课程", h=1.5)
footer(s, "谢谢")


if __name__ == "__main__":
    import pathlib
    out = pathlib.Path(__file__).resolve().parent / "第5天-读一个开源项目.pptx"
    n = D.save(str(out))
    print("已生成：%s（%d 页，16:9）" % (out, n))
